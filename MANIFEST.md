# MANIFEST — Pivot

Referencia del estado actual de la arquitectura. El historial de cambios vive en `CHANGELOG.md` — este archivo describe solo cómo es el proyecto hoy.

## Descripción general

**Rebrand** (pedido explícito, ver CHANGELOG.md) — el producto se llamaba "Mirlxs — Reservas"/"Mirlxs RD" de cara al usuario (`<title>`, metadata, `manifest.json`, banner de instalación); ahora es **Pivot** en todo texto/metadata visible como marca del producto. Sin relación con "Mirlxs" como nombre del equipo de patinaje/derby real que usa la app (sigue igual, ver abajo) ni con la categoría/tier de integrantes "Mirlxs" (variables, lógica, clases CSS, keys de storage, nombres de función — todo eso NO se tocó, es un concepto de dominio distinto que coincide de nombre por casualidad).

App de gestión de un equipo de patinaje/derby (Mirlxs): reservas de clases/mensualidades, calendario de eventos con asistencia (RSVP + rollcall real), sección Equipo (roster con favoritos/grupos por rol, perfil de detalle con stats y termómetro Quindes/Mirlxs), perfil/equipamiento de cada miembro, tareas del club y panel admin ("Mi Liga"). SPA en `index.html` sobre Supabase (Postgres + Edge Function), con 2 mini-SPA hermanas standalone (`inscripcion/`, `registro-express/`) para altas nuevas. Backend legado en Google Apps Script (Code.gs, fuera de este repo) en migración progresiva a una Edge Function de Supabase — lo no migrado todavía cae por proxy a GAS.

## 1. Estructura de archivos

```
reservas/
├── index.html                  Shell de la SPA principal (home/reservas/eventos/tareas/perfil/Mi Liga), solo <link>/<script src>
├── OneSignalSDKWorker.js       Service worker de push notifications (OneSignal)
├── manifest.json               Web App Manifest (PWA)
├── CNAME                       Dominio custom de GitHub Pages
├── assets/                     Logos de terceros con derechos (DeUna, Banco Internacional) para botones de pago en s-pago
│
├── css/
│   ├── colors.css              Fuente única de verdad de colores (ver "CSS" abajo)
│   ├── global.css              Reset, toggles, modales info, bottom sheets (chrome compartido), avatar-pill, banner offline global (`#app-offline-banner`, ver "Banner offline global" en `js/offline.js` más abajo)
│   ├── estilos.css             Toasts, loaders/spinners, pull-to-refresh, keyframes globales, .cta-footer-fixed
│   ├── ui.css                  Componentes reutilizables: card, inputs, btn, opciones, badges
│   ├── nav.css                 Nav superior unificada (.app-nav y variantes fixed/sticky)
│   ├── login.css                Pantalla s1 (Google Sign-In + PIN)
│   ├── home.css                 Home y card de reserva (estados, badges)
│   ├── reservas.css             Flujo de reserva s2–s6; `.fi-pill-div` — separador visual ("|", opacity 0.3) entre acciones dentro de un pill; `.fi-pill-lugar` (contenedor, `padding:0`) + `.fi-pill-accion`/`.fi-pill-accion-static` — pill de ubicación unificado (lugar/Maps + "Ver cómo llegar" del video, ver `_evDetalleInfoHtml()`/js/eventos.js), hijos clicables independientes sin anidar `<a>` en `<a>`
│   ├── perfil.css               Ajustes del perfil (aj-sub-*, date picker ddp-*)
│   ├── equipo.css               Sección Equipo: `.eq-sticky-header` (rediseño, ver "colapso progresivo al scrollear" más abajo) envuelve la nav (`.eq-header-row`, antes `#eq-search-header` sticky por su cuenta con `.app-nav.app-nav-sticky`) Y los 2 paneles deslizables ("Mis estadísticas"/búsqueda+filtros) DENTRO del mismo contenedor sticky (mismo criterio que `.ev-sticky-header`/css/eventos.css, que ya contenía `#ev-mes-panel`/`#ev-busqueda-panel`) — así, al abrirse, quedan pegados arriba por encima del roster que scrollea detrás (`#eq-lista-contenido`, nuevo, envuelve TODO el roster/empty-states, equivalente a `#ev-timeline`) en vez de solo empujarlo; mismo bleed -16px/+32px y z-index:100 que tenía antes `.app-nav-sticky`, PERO gap nav→panel corregido a 18px (`padding-bottom:14px` en `.eq-sticky-header` + `margin-bottom:4px` en `.eq-header-row`) — bug real corregido, "demasiado espacio entre la nav y el contenido de los paneles al desplegarse": la 1ra versión de este wrapper copió a mano el gap que tenía `.app-nav-sticky` antes de que los paneles vivieran DENTRO de él (38px, `padding:22px`+`margin-bottom:16px`) sin notar que Eventos usa un gap bastante más chico (`.ev-sticky-header`/`.ev-header-row`, css/eventos.css, mismos 18px copiados acá); `#eq-misstats-panel` con `border-radius:0 0 16px 16px` (mismo radio que `.eq-misstats-panel-inner`) — bug real corregido, "el contenido de la lista aparece debajo con esquinas rectas al scrollear": el panel ya tenía `overflow:hidden` (mecanismo compartido del acordeón, `.eq-header-panel`) pero sin radio propio esa clip-line quedaba recta, así que al achicarse EN VIVO durante el colapso progresivo por scroll (ver `_eqInicializarCierrePanelesPorScroll()`/js/equipo.js más abajo) la tarjeta redondeada de adentro se cortaba contra un borde cuadrado — sin efecto visual con el panel simplemente abierto/cerrado (nada que recortar ahí), solo durante el arrastre en vivo; lista/acordeones por categoría + INACTIVXS (texto visible, rol interno sigue siendo `'Inactivos'` — ver `js/equipo.js`)/LESIONADXS (con badge de tendencia sobre la foto de cada persona; cards de lista solo muestran puntos totales, sin horas/asistencia/puntos por tarea), Favoritos con slide+fade al aparecer/desaparecer, **headers sticky apilados** (rediseño REAL, ver MANIFEST.md/CHANGELOG.md — "los sticky headers deben apilarse, no reemplazarse") — `.eq-grupo-header--sticky` ya NO usa `position:sticky` (bug real corregido, "cuando aparece un nuevo sticky header el anterior desaparece": `position:sticky` nativo solo queda pegado mientras SU PROPIO `.eq-grupo`/`#eq-favoritos-wrap` todavía tiene alto por scrollear debajo, así que un header se soltaba en cuanto su sección terminaba de pasar en vez de acumularse con el siguiente) — ahora es solo un MARCADOR sin estilo propio (distingue estos 5 headers reales de los `.eq-grupo-header` al vuelo de la vista "por rol", `_eqRenderPorRol()`, que nunca deben stickear), y `_eqActualizarStickyHeaders()`/js/equipo.js maneja el pegado A MANO alternando cada header entre flujo normal y `.eq-grupo-header--stuck` (`position:fixed; z-index:5; background:var(--bg)`, `top`/`left`/`width` inline por instancia) según `window.scrollY` supere o no su propio umbral (recalculado en cada evento de `scroll` de la página, throttled por `requestAnimationFrame`) — así un header STUCK sigue pegado más allá de los límites de su propia sección, acumulándose con los siguientes, hasta que el usuario scrollea de vuelta arriba de su posición original; `.eq-grupo-header-spacer` (nuevo `<div>` vacío antes de cada header, index.html) compensa el alto que el header deja de aportar al flujo al pasar a `fixed` (0px natural, `header.offsetHeight`px stuck) — sin él, el resto del contenido saltaría hacia arriba cada vez que un header se vuelve stuck; también sirve de referencia ESTABLE de la posición natural de cada header (`spacer.offsetTop`, nunca deja el flujo así el header sí lo haga) en los 5 headers reales de la lista (Favoritos/Quindes/Mirlxs/Inactivxs/Lesionadxs) — Favoritos/Lesionadxs pasan de `<div>` a `<button>` (antes sin `onclick`, ahora sí lo necesitan); `#eq-favoritos-wrap` alterna `overflow:hidden`↔`visible` en vivo (`_eqMostrarSeccionFavoritos()`/`_eqOcultarSeccionFavoritos()`/`_eqRenderFavoritos()`, js/equipo.js) — `hidden` permanente (CSS) sigue rompiendo el header de adentro con el mecanismo nuevo, misma razón de fondo con otra víctima (cualquier `overflow`≠`visible` en un ancestro recorta durante el pintado a CUALQUIER descendiente `position:fixed`, no solo a `position:sticky`) si no se relajara a `visible` una vez asentado abierto (`max-height:none`), único de los 5 wraps con este problema (los otros 4 no tienen overflow propio, solo `.eq-grupo-body` — sibling del header, no ancestro — lo tiene), nav con 2 triggers (avatar+"Mis estadísticas" / lupa) + paneles deslizables "Mis estadísticas" (sin foto/nombre) y búsqueda+filtros (panel rediseñado para funcionar igual que Eventos, mismo patrón `.ev-header-burbuja-panel`/`.ev-filtros-row`/`.ev-filtro-trigger`/`.ev-filtro-burbuja`/`.ev-filtro-burbuja-pills`/`.ev-filtro-badge` de css/eventos.css COPIADO a clases `.eq-*` propias — reusa `.bsheet` para el mecanismo de deslizado del panel exterior, `#eq-busqueda-panel`/`.eq-header-panel`, sin cambios ahí salvo el colapso progresivo al scrollear (ver abajo): subtítulo "Filtros" (`.seccion-label`) + 2 pills de categoría con chevron "Puntos"/"Rol" (ya NO 2 títulos de sección fijos "Filtrar puntos según periodo"/"Filtrar por rol", sacados) — estos 2 SÍ son `.eq-filtro-trigger` outlined (borde+texto de marca activo, sin relleno sólido, copiado de `.ev-filtro-trigger`) — que despliegan su propia burbuja al tocarlos (`_eqToggleFiltroBurbuja()`, techo fijo 320px sin medir `scrollHeight`, comportamiento RADIO igual que Eventos — abrir una cierra la otra si estaba abierta, `_eqFiltroBurbujaAbierta` un solo valor, no un mapa); ícono de lupa (`#eq-busqueda-toggle-btn`) con badge numérico (`.eq-filtro-badge`, círculo rojo `--danger`) que cuenta categorías con selección activa (Puntos: "Histórico" o una fecha distinta al mes actual; Rol: cualquier pill tildado), oculto si el resultado es 0; sub-pills reales adentro (Fecha/Histórico, roles) son `.aj-pill`/`.aj-pill.activa` 100% VAINILLA, sin ningún override propio — exactamente el mismo look que los sub-pills de Eventos ("Asamblea", etc. dentro de Lugar/Tipo), que tampoco tienen override — "Fecha" sin chevron (es un sub-pill, no un acordeón); `.eq-busqueda-panel-inner` con el mismo border/stroke que `.ev-header-burbuja-panel`; **colapso progresivo al scrollear** (rediseño, ver MANIFEST.md/CHANGELOG.md — "los 2 paneles deben replicar el mismo comportamiento que el calendario de Eventos al scrollear"; re-ajuste posterior, "el panel de búsqueda/filtros debe ocultarse rápido al scrollear, igual que en Eventos, no progresivo" — ver el bullet de `js/equipo.js` para el detalle completo de la distinción `stats`/`busqueda`) — `.eq-header-panel` sigue con la misma transición `max-height` de siempre (0.28s); SOLO "Mis estadísticas" la pisa en vivo con `transition:none` mientras el dedo arrastra sobre `#eq-lista-contenido` (`_eqInicializarCierrePanelesPorScroll()`/js/equipo.js, puerto 1:1 de `_evInicializarCierreCalendarioPorScroll()`/js/eventos.js), dejando que la transición CSS entre recién al soltar (termina de cerrar o vuelve al alto original) — búsqueda/filtros ya NO pasa por este mecanismo, se cierra de una sola vez con esta misma transición de 0.28s apenas arranca cualquier gesto afuera (`_eqCerrarBurbujaSiFueraDe()`/js/equipo.js, puerto de `_evCerrarBurbujaSiFueraDe()`/js/eventos.js); `#eq-lista-vacia` — bug real corregido, "lista de personas desaparece al usar los filtros" — reusa `.eq-favoritos-vacio` para avisar explícitamente cuando Favoritos/Quindes/Mirlxs/Inactivos/Lesionadxs dan 0 resultados A LA VEZ, en vez de dejar la pantalla en blanco sin ningún aviso, con fade-in/out de opacidad (~250ms, `_eqActualizarListaVacia()`/js/equipo.js) en vez de un `display` seco — mismo principio de "animación de entrada y salida obligatoria" del resto de la app, ver sección 2 de este archivo), perfil de detalle (pills Quindes/Mirlxs+pronombres centradas — **re-ajuste, ver MANIFEST.md/CHANGELOG.md — "mismo tamaño que el pill de pronombres"**: `.eq-perfil-pills-row--top .eq-mis-stats-rol-pill` pisa el padding/font-size/radius chicos de esa clase con los valores EXACTOS de `.aj-pill` (css/perfil.css), scopeado a esta fila —, sección "Estadísticas" como acordeón compacto abierto por default, MOVIDO para quedar justo antes del acordeón "Categoría" (re-ajuste, "reordenar acordeones") — su filtro de período propio (`.eq-periodo-pills`) SALIÓ (re-ajuste, "sin filtros de período en estadísticas del detalle") reemplazado por `.eq-perfil-stats-nota` (texto chico `var(--muted)`, margin `2px 0 12px`, mismo lugar donde vivía `.eq-perfil-stats-periodo`, renombrada) — `.eq-periodo-pills` sigue viva sin cambios en el panel de Filtros de la lista, la única instancia que queda; fila de "Rol" oculta si no hay dato en vez de "Rol no definido" (confirmado ya cumplido, sin cambio de código — mismo criterio en teléfono/email/fecha de ingreso, ninguno con fallback), **auto-scroll al abrir cualquier acordeón** (re-ajuste, pedido explícito) — `eqToggleAcordeon()`/js/equipo.js llama `scrollIntoView({behavior:'smooth', block:'nearest'})` 320ms después de abrir (pasado el `0.3s` de la transición `max-height` de `.eq-acord-cuerpo`), nunca al cerrar), termómetro, **desglose de puntos por concepto** (feat nueva, ver MANIFEST.md/CHANGELOG.md — "subpantalla de desglose de puntos por concepto") — `.eq-desglose-panel`/`.eq-desglose-panel-inner` son el equivalente de `.aj-sub`/`.aj-sub-inner` (css/perfil.css, subsecciones de Ajustes) en esta sección — mismos valores EXACTOS (`position:fixed` a pantalla completa salvo la nav inferior, `z-index:950`, `translateX(100%)`/`opacity:0.85` en reposo, `0.32s var(--ease-axis)`) copiados en vez de reusar `.aj-sub` literal (mismo criterio "cada sección es dueña de sus propias clases" del resto de este archivo) — `_eqAbrirDesglosePuntos()`/`_eqCerrarDesglosePuntos()`/js/equipo.js animan `transform`/`opacity` a mano, esta clase solo aporta el reposo + la `transition`; `.eq-desglose-fila`/`.eq-desglose-fila-icono`/`.eq-desglose-fila-info`/`.eq-desglose-fila-valor` (ícono redondo + título/subtítulo + valor a la derecha, mismo criterio de fila que `.eq-info-fila` con un 3er bloque que esa no tiene) + `.eq-desglose-total` (footer de la lista, separador propio); `.eq-stat-card--tappable` (modificador sobre `.eq-stat-card` ya existente, SOLO en las 3 cards de puntos — horas/asistencia anual quedan sin él, no son tappables) da la affordance de tap (`cursor:pointer` + `:active` con `var(--brand-light)`, mismo tratamiento que el resto de filas tappables de esta sección), **fondo opaco en los paneles deslizables** (bug real corregido, ver MANIFEST.md/CHANGELOG.md — "borde del panel con contenido de la lista transparentándose por debajo") — `.eq-header-panel` (contenedor real que recorta, `overflow:hidden`, durante el colapso progresivo al scrollear) suma `background:var(--bg)` propio, antes ausente — solo `.eq-misstats-panel-inner`/`.eq-busqueda-panel-inner` (la tarjeta redondeada de adentro) pintaban fondo, con un `margin-top:10px` de hueco antes de esa tarjeta que en flujo normal quedaba cubierto por el `background` de `.eq-sticky-header` (su padre) pero podía asomar el roster detrás durante el arrastre en vivo (`_eqInicializarCierrePanelesPorScroll()`, `panel.style.maxHeight` pisado en cada `touchmove`) si el recorte caía justo ahí, **fade out/in de los sticky headers al abrir/cerrar cualquier panel** (bug real corregido, ver MANIFEST.md/CHANGELOG.md — "acordeones se rompen cuando el panel está abierto") — `#s-equipo.eq-panel-abierto .eq-grupo-header--stuck { opacity:0; pointer-events:none; }` + `transition:opacity 0.2s ease` en la regla BASE de `.eq-grupo-header--stuck` (para que anime en los 2 sentidos, ver ese bloque) — `.eq-panel-abierto` la agrega/saca `_eqSincronizarClasePanelAbierto()`/js/equipo.js -- bug real corregido, ver MANIFEST.md/CHANGELOG.md, "glitch visual al colapsar Mis estadísticas: el header sticky aparece desplazado": `_eqCerrarPanel()`/el commit-a-cerrar del arrastre (`_eqInicializarCierrePanelesPorScroll()`) llamaban a esta función DE INMEDIATO al poner `_eqPanelAbierto = null`, sacando `.eq-panel-abierto` (reapareciendo los headers stuck) en el mismo instante en que arrancaba la transición de `max-height` del panel (0.28s) -- sus posiciones seguían siendo las de la nav TODAVÍA abierta, porque `_eqActualizarStickyHeaders()` no corre hasta el `setTimeout(...,300)` de siempre, así que quedaban visibles en el lugar viejo durante todo el colapso. Fix: al CERRAR, la clase ya NO se sincroniza de inmediato -- se queda como estaba (headers ocultos) hasta el mismo `setTimeout(...,300)`, que ahora primero llama `_eqActualizarStickyHeaders()` y RECIÉN DESPUÉS `_eqSincronizarClasePanelAbierto()` -- para cuando los headers reaparecen (fade-in) ya están en su posición correcta, sin ningún frame con la vieja. Al ABRIR (`_eqAbrirPanel()`) sigue sincronizando de inmediato, sin cambios -- ahí sí hace falta ocultar ANTES de que la nav empiece a crecer; los headers en posición NATURAL (sin `--stuck`) no están alcanzados por el selector, no se ven afectados (pedido explícito), **rediseño compacto de "Mis estadísticas"** (ver MANIFEST.md/CHANGELOG.md, mismo bullet de js/equipo.js para el detalle completo) — `.eq-mis-stats-rol-pill`/`.eq-mis-stats-total-pill` comparten UNA declaración (borde + fondo tenue + texto, todo `var(--brand)`, pedido explícito) — la de nivel ahora vive en `.eq-misstats-toggle-pills` (nav, `flex-shrink:0`, nunca se trunca) junto al pill de estado (`.dat-estado-chip`, sin tocar color); `.eq-misstats-toggle-texto` suma `min-width:0` para que el `text-overflow:ellipsis` de siempre funcione de verdad con las pills nuevas compitiendo por espacio. `.eq-mis-stats-puntos-row` (línea-label-pill-línea, reusa `.eq-grupo-linea`/`.eq-grupo-nombre` sin duplicarlas) reemplaza la card de "Puntos totales". `.eq-stat-card--combo`/`.eq-stat-combo-iconos`/`.eq-stat-combo-valor-row`/`.eq-stat-combo-racha-badge` (card única "Asistencia + Racha", 2 íconos + valor + badge de racha inline tappable aparte) reemplazan las 2 cards separadas que había. **Panel "Mis estadísticas" expandido por defecto + título personalizado** (pedido explícito): `#eq-misstats-toggle-texto` deja de ser un texto fijo "Mis estadísticas" -- ahora 2 líneas apiladas (`display:flex;flex-direction:column`), `#eq-misstats-toggle-nombre` (nombre real, `persona.nombreDerby || persona.username`, mismo fallback que ya usa el avatar de al lado, `_eqRenderMisEstadisticas()`) + `.eq-misstats-toggle-sub` ("Estadísticas", `var(--muted)`, más chico) debajo. El panel arranca ABIERTO la primera vez que carga contenido en la sesión (`_eqStatsPanelInicializado`, flag para no pisar un toggle manual en re-renders posteriores del roster/filtros) salvo que `localStorage['pivot_stats_collapsed']==='true'` -- `_eqAbrirPanel()`/`_eqCerrarPanel()` (ambas genéricas para 'stats'/'busqueda') escriben esa key SOLO para `tag==='stats'` en cada toggle manual. Chevron expand_less/more: NO se agregó un swap de ícono nuevo -- ya existía `.eq-misstats-toggle-chevron` rotando 180° vía CSS (`transform`, `.eq-misstats-toggle-btn.activo`), mismo patrón que el resto de los triggers de acordeón/panel de esta app, reusado tal cual. Transición `max-height` de `.eq-header-panel` (0.28s) sin cambios, ya cubría el pedido de "sin cortes abruptos". **Bug real corregido -- "termómetro Quindes/Mirlxs ya no se muestra"**: `_eqStatsContenidoHtml(p)` (contenido compartido de "Mis estadísticas" y el perfil de detalle) ocultaba el termómetro (`.eq-rank-wrap`) con `necesitaEquipoClub = !!(p.necesitaPatines || p.necesitaProtecciones)` -- `necesitaPatines`/`necesitaProtecciones` son STRINGS ("Sí"/"No"/lista parcial, no booleanos), así que ese truthy check daba `true` con cualquier valor no vacío, incluido el string literal `"No"` -- confirmado contra la base real que el 100% del roster tiene `necesita_patines` en "Sí" o "No" (nunca vacío), así que el termómetro no se mostraba para NADIE, al revés de la intención documentada ("solo con equipo propio"). Bug presente desde que esta condición se introdujo (no una regresión reciente, ver `git log -S`). Fix: comparación explícita `p.necesitaPatines === 'Sí' || (!!p.necesitaProtecciones && p.necesitaProtecciones.toLowerCase() !== 'no')`, mismo criterio que ya usa `js/perfil.js` para estos mismos 2 campos. **Colapso de "Mis estadísticas" por umbral de scroll** (pedido explícito, `_eqInicializarColapsoStatsPorScroll()`) -- listener nuevo, distinto del drag-to-dismiss ya existente (`_eqInicializarCierrePanelesPorScroll()`, arriba en este archivo): ese solo corre en `touchmove` (sigue el gesto táctil en vivo, proporcional al arrastre) y nunca dispara con scroll de mouse/trackpad/rueda ni con el scroll inercial tras soltar el dedo (`_eqListaDragActivo` ya volvió a `false` para entonces). El nuevo listener escucha `window` (esta app no tiene contenedor propio con `overflow` -- cada `.pantalla` scrollea a nivel `window`/`body`, un listener en `#eq-lista-contenido` -- el pedido original mencionaba "el contenedor interno de la sección" -- nunca dispararía, no es scrolleable en sí) pero se sale de una si `#s-equipo` no es la pantalla `.activa` (mismo criterio de "no interferir con otras secciones" del pedido, resuelto por guard en vez de por scope del listener) y si `_eqListaDragActivo` (para no pisar el drag en vivo mientras el dedo sigue abajo -- los 2 mecanismos conviven, cada uno cubre un caso distinto). Colapsa una sola vez pasado el umbral (60px) vía `_eqCerrarPanel('stats')` (misma transición `max-height` 0.28s de siempre, sin reimplementar la animación) -- nunca vuelve a expandir solo, eso sigue siendo 100% manual (chevron). **Persistencia solo en el toggle manual**: el `localStorage['pivot_stats_collapsed']` que se guardaba dentro de `_eqAbrirPanel()`/`_eqCerrarPanel()` (genéricas, usadas también por el auto-open inicial y ahora por este auto-colapso) se movió a `_eqTogglePanel()` -- el único caller real de un toque humano en el chevron/trigger -- para que ni el expandido automático al entrar ni el colapso automático por scroll pisen la preferencia guardada (pedido explícito: "si el usuario nunca la tocó [manualmente], siempre expandido al abrir"). **5 bugs reales corregidos de una ronda (pedido explícito)**: (1) `.eq-misstats-toggle-nombre`/`.eq-misstats-toggle-sub` no quedaban alineados a la izquierda entre sí -- `text-align:center` del user-agent sobre `<button>` (`.eq-misstats-toggle-btn`) se heredaba sin que nada lo pisara; `text-align:left` + `align-items:flex-start` explícitos en `.eq-misstats-toggle-texto`. (2) El acordeón "Estadísticas" de la vista de DETALLE de un miembro (`_eqPerfilContenidoHtml()`, roster -> tocar a alguien) nacía siempre `eq-acord-abierto` -- ahora nace colapsado como Categoría/Estado, distinto del panel "Mis estadísticas" propio (único lugar con expandido-por-defecto real). (3) Orden de `_eqPerfilContenidoHtml()`: `filas` (datos de contacto con ícono -- teléfono/email/fecha de ingreso/rol) pasa a ir primero, antes de los 3 acordeones (antes iba entre Categoría y Estado). (4) `#eq-sheet-confirm-admin`/`.eq-confirm-sheet` (confirmación "dar acceso admin") se veía transparente en oscuro -- usaba `background:var(--surface)` (overlay translúcido pensado para apoyarse sobre `--bg` ya pintado, no para ser el único fondo de un sheet flotante) -- pasa a `var(--bg)` sólido + `border` completo (antes solo `border-top`), mismo criterio que `.bsheet` (css/global.css). (5) `adminAgregarAdmin()` (Edge Function) insertaba `fecha`/`invitado_por` en `admins` -- columnas que no existen (verificado contra el schema real: `email`/`invited_by`/`created_at`) -- causaba "could not find 'fecha' column..." al querer dar acceso admin; insert corregido a `{email, invited_by}` (`created_at` tiene default propio). **Bug real corregido -- "lista de asistentes de evento muestra username en vez de nombre derby, sin foto"** (`js/eventos.js`): `_evGrupoAsistenciaHtml()` y el backend (`getEventosRango()`/`_mapaEquipoPorNombre()`) ya estaban bien -- el problema eran 2 pushes optimistas locales a `ev.rsvps` que arma la propia fila de la persona SIN `nombreDerby`/`fotoPerfil` (a diferencia de `_evMarcarAsistenciaAdmin()`, que sí los busca vía `datosRoster`): `_evMarcarAsistencia()` (RSVP manual propio) y el auto-persist de `_evAntReconciliarConReglas()` (asistencia anticipada). Sin esos 2 campos, `_evGrupoAsistenciaHtml()` cae a `p.nombre` (username) y sin foto para ESA fila puntual hasta el próximo reload completo (`_evMapEventoBackend()`, que sí los trae). Fix: los 2 pushes ahora incluyen `nombreDerby: (E.datos && E.datos.nombreDerby) || ''`/`fotoPerfil: (E.datos && E.datos.fotoPerfil) || ''` -- perfil completo de la propia persona logueada, ya en memoria, mismo shape que `getDatosCompletos()`. **Perf real -- jank en Android Chrome al colapsar "Mis estadísticas"/calendario de Eventos** (pedido explícito; se descartó `transform:scaleY()` por deformar texto/números -- ver discusión previa): `_eqAnimarPanel()`/js/equipo.js y `_evAnimarPanel()`/js/eventos.js (mismo shape en los 2 archivos) reemplazan `max-height` por `height` (numérico medido, `scrollHeight`, en los 2 extremos -- sigue disparando layout igual, ninguna de las 2 propiedades es GPU-only) + `transform:translateY` en los HIJOS DIRECTOS del wrapper (`.eq-misstats-panel-inner`/`.eq-busqueda-panel-inner`; `#ev-cal-contenido`/`#ev-mes-pills-row`) -- esa parte sí corre por compositor. `will-change` como clase temporal (`eq-panel-wrapper-anim`/`eq-panel-inner-anim`, `ev-panel-wrapper-anim`/`ev-panel-inner-anim`), sacada sola en `transitionend` (`{once:true}`, sin acumular listeners) -- nunca permanente. Aplica a `_eqAbrirPanel()`/`_eqCerrarPanel()` (Equipo, ambos paneles) y `_evAbrirCalendario()`/`_evCerrarCalendario()`/`_evCalActualizarMaxHeightExterior()` (Eventos, solo el calendario -- el panel de búsqueda+filtros de Eventos, `_evAbrirPanel()`/`_evCerrarPanel()`, queda fuera de este pedido, sigue con `max-height` puro). El drag-to-dismiss en vivo de los 2 (`_eqInicializarCierrePanelesPorScroll()`/`_evInicializarCierreCalendarioPorScroll()`) ahora también envuelve el trabajo de cada `touchmove` en rAF+flag ("ticking", mismo patrón que ya usaba `_eqActualizarStickyHeadersThrottled()` para el resync de headers) -- como mucho 1 escritura real de `height`/`transform` por frame, con el `translateY` de los hijos siguiendo el mismo % de arrastre que el `height` en cada frame. El listener de umbral por scroll (`_eqInicializarColapsoStatsPorScroll()`) suma el mismo rAF+ticking. **Investigación previa, sin tocar**: existe un profiling real con Playwright/`PerformanceObserver('longtask')` documentado en `js/eventos.js` (cerca de `_evNavMesScrollRafPendiente`) que no encontró jank atribuible a los listeners de scroll de esta app -- este fix se aplica igual, a pedido explícito, sin nueva medición en Android real todavía. **Navs más grandes** (pedido explícito, usabilidad mobile): `.app-nav` (css/nav.css) -- `padding` vertical 26px->33px, `min-height` 60px->67px; íconos de acción (`.app-nav-icon-btn .material-symbols-outlined`) 18px->22px. `.app-bottom-nav-item` -- ícono `1.5rem`->`1.6rem`, label `0.71rem`->`0.73rem`; `--bottom-nav-h` (css/colors.css) 66px->71px, junto con sus 2 fallbacks JS `|| 66` (`js/eventos.js`/`js/tareas.js`, se leen solo si `getComputedStyle` fallara) actualizados a `|| 71` -- mismo criterio documentado ya en este archivo para esa variable. **Detalle de evento -- ícono de tipo eliminado**: `.ev-detalle-nav-icono` (el ícono de marca -- ej. silbato -- suelto al lado de la flecha atrás, `_evDetalleStickyHtml()`) se saca del todo, sin reemplazo -- el tipo ya se lee en el texto de al lado (`.ev-detalle-tipo`). **Detalle de evento -- FAB de acciones admin** (pedido explícito, reemplaza los 3 botones sueltos -- Editar/Eliminar/Cancelar -- que vivían en la nav del detalle): `#ev-detalle-fab-menu`/`#ev-detalle-fab-btn` (ícono `edit`, abajo-derecha sobre la bottom nav) -- mismo componente/posicionamiento que `#tar-detalle-fab-menu` (detalle de tarea, ya existente) y misma regla de visibilidad en `ir()`/js/ui.js (`id==='s-eventos-detalle' && !!_adminToken`). Al tocarlo abre `#ev-fab-admin-sheet` (mismo patrón `.bsheet` que `#sheet-gestionar` -- filas de opción con ícono, la de "Cancelar evento" oculta si `ev.estado==='Cancelado'`) en vez de las 3 acciones directo -- cada fila llama a la MISMA función que antes disparaba el botón viejo de la nav (`_evEditarAbrir()`/`_evAbrirSheetBorrar()`/`_evAbrirSheetCancelar()`, sin duplicar esa lógica), resuelto contra `_evDetalleActual` (variable de módulo ya existente) porque el FAB es markup estático, no se regenera por render. Abre/cierra con el mismo mecanismo overlay+`_registrarOverlayAbierto()`/js/ui.js que el resto de los bottom sheets de la app (`ajAbrirSheetLogout()`/js/perfil.js, mismo patrón). **Equipo -- pill de tier al termómetro + divisor "Datos anuales"** (pedido explícito, aplica a la vez en "Mis estadísticas" y en la vista de detalle -- las 2 comparten `_eqStatsContenidoHtml(p)`): el pill de nivel (`.eq-mis-stats-rol-pill`, `p.rol`) se saca de la nav (`#eq-misstats-toggle-pills`, `_eqRenderMisEstadisticas()` -- queda vacío a propósito) y se mueve DENTRO de `.eq-rank-labels` (el termómetro), reemplazando el texto plano del lado que coincide con `p.rol` -- mismo `.eq-mis-stats-rol-pill` (`var(--brand)`, ya destacado, sin variante nueva), el lado opuesto sigue texto plano sin cambios. Divisor "Datos anuales" nuevo, antes de las cards de Horas/Asistencia anual -- mismas clases exactas que el separador "Puntos" (`.eq-mis-stats-puntos-row`/`.eq-grupo-linea`/`.eq-grupo-nombre`), sin el pill de total (acá no hay un valor único que resumir).
│   ├── admin.css                Overrides admin (dark mode del panel)
│   ├── eventos.css              Sección Eventos: timeline, panel de mes, cards, detalle; `.ev-video-sheet`/`.ev-video-contenedor`/`.ev-video-cerrar` — sheet in-app del "Video instructivo" del lugar (ver `_evAbrirVideoInstructivo()`/js/eventos.js)
│   └── tareas.css               Sección Tareas — deliberadamente chico, reusa clases de eventos.css/reservas.css/perfil.css/admin.css/ui.css
│
├── js/
│   ├── config.js                BACKEND, GOOGLE_CLIENT_ID, SUPABASE_URL/ANON_KEY, sha256Hex
│   ├── api.js                   Capa HTTP: api() GET con token, apiPost() POST form-urlencoded. **Timeout de 15s en apiPost()** (pedido explícito, investigando "login trabado en Verificando cuenta" en conexiones móviles lentas) -- `Promise.race([fetch(...), timeoutPromise])`, rechaza con "Tiempo de espera agotado..." si el fetch ni resuelve ni rechaza a tiempo (conexión colgada, sin respuesta del servidor) -- sin esto ese caso quedaba esperando para siempre, ni onSuccess ni onError corrían nunca. No cancela el fetch real si pierde la carrera (sigue en vuelo de fondo), solo libera al caller. Alcance acotado a apiPost() (pedido explícito) -- api() (GET) queda igual, sin timeout, mismo riesgo latente sin resolver ahí todavía.
│   ├── ui.js                    ir()/volver(), nav inferior, overlay stack, toasts, meses
│   ├── eventos.js               Sección Eventos (timeline, RSVP, rollcall admin, asistencia anticipada, venues; grupo "Sin respuesta" del detalle de evento excluye miembros con `estado_miembro==='Ausente'` Y a quien lleve 30+ días sin una marca real de admin (`ultimaAsistencia` nula o vencida, ver bullet de fix más abajo) — `adminBuscarPersonasParaEvento()`, backend, expone `estadoMiembro`/`ultimaAsistencia` por persona para este filtro; `_evRenderTimeline()` YA cubre "pantalla en negro/vacía con filtros sin resultados" desde antes de este pedido — a diferencia del bug real que sí existió en Equipo (5 contenedores `.eq-grupo`/`.eq-favoritos-wrap` SIBLINGS, cada uno ocultándose solo), el timeline de Eventos es UN SOLO render (`_evTimelineItems()` ya mergea eventos+cumpleaños+aniversario+offseason filtrados antes de pintar nada) — `if (items.length===0)` pinta `.ev-lista-vacia` ("No hay eventos ni cumpleaños con estos filtros") con fade (`_evFadeSwap()`) en vez de dejar `#ev-timeline` vacío; verificado, sin bug real que corregir ni código nuevo necesario acá). **Sheet "Cuota pendiente"** (`#sheet-cuota-pendiente`, `_evAbrirSheetCuotaPendiente()`) -- re-ajuste (pedido explícito, ver MANIFEST.md/CHANGELOG.md -- "nuevo texto + acceso a excepción de cuota + Pagar ahora con meses preseleccionados"): mensaje del caso genérico (`!opts.esGracia`) ahora arma la lista real de meses adeudados en prosa ("Pagá agosto y septiembre para poder marcar tu asistencia y seguir viniendo.") vía `_evMesesAdeudadosTexto()`/`_evMesesAdeudados()` (nuevas, cerca de `_evMesPagado()`) -- camina mes a mes desde el siguiente al ÚLTIMO mes cubierto por una reserva mensual no cancelada (a propósito NO reusa `_evVencimientoCuota()`: esa da `null` para una cuota YA EXPIRADA, que es el caso típico acá, tratando "debe 1 mes" igual que "nunca pagó nada") hasta el mes actual, saltando meses ya cubiertos (`_evMesPagado()`), tope defensivo 12 meses. El caso `esGracia` (mensaje sobre 1 mes puntual del evento que se intentó marcar) queda sin cambios. Debajo del botón "Pagar ahora" ahora hay SIEMPRE (antes solo para `modo==='quindes'`, sin motivo real para excluir a mirlxs) un texto chico `var(--muted)` "¿No podés pagar la cuota?" + un link "Pedir una excepción de cuota" (mismo `_evCuotaSolicitarAyuda()`→`abrirWizardExcepcion()` de siempre, js/perfil.js) -- reemplaza el botón "Solicitar ayuda con el pago". "Pagar ahora" en el caso genérico (`_evCuotaPagarAhora()`, sin `E.quindesPendingRsvpEvento`) ahora va SIEMPRE directo al checkout mensual con los meses adeudados preseleccionados (`E.mirlxsMesesPresel`, mismo mecanismo que ya usa `_evFabReserva()` -- `generarMeses()`/js/ui.js lo lee al armar los checkboxes iniciales), acotado al año actual (limitación preexistente de ese mecanismo, `generarMeses()` solo puede representar los 12 meses del año en curso) -- reemplaza el camino anterior (mirlxs abría `evAbrirSheetTipoPago()`, un sub-menú clase/mensual sin preselección; quindes iba a mensual pero sin preseleccionar nada), unificado para los 2 modos. El total pagado queda visible solo (`actualizarTotalS4()`, ya se recalcula en vivo con los checkboxes marcados, sin cambios ahí). **Bug real corregido en el wizard "Pedir excepción de cuota"** (`#wizard-excepcion`, css/perfil.css -- "aparece la vista oscura con solo un buscador, sin poder interactuar"): a `.wizard-excepcion` le faltaba el `z-index` -- el comentario de esa regla ya decía "mismo z-index de sheet grande que la app ya usa 9600+" pero nunca se aplicó, así que el panel quedaba en `z-index:auto` (por debajo de cualquier otro elemento `fixed` con z-index explícito de la pantalla de atrás) mientras su overlay (`#wizard-excepcion-overlay`, 9650) SÍ se pintaba encima de todo y oscurecía el fondo -- de ahí la vista oscura con otro elemento asomando arriba y el wizard real intocable, debajo de todo. Fix: `z-index:9651` (mismo patrón overlay+1 que el resto de los "sheet grande" de esta app, ej. `#ev-sheet-cancelar-overlay` 9700 / `#ev-sheet-cancelar` 9701). **Push notifications de eventos/offseason** (feat nueva, ver "Push notifications" en la sección Edge Function más abajo) — creación/edición de evento y creación de offseason NUNCA pasaron por la Edge Function (fetch directo del navegador a PostgREST, ver el bloque "Guardado final" arriba en este mismo archivo) y `adminCancelarEvento` sigue en GAS (fuera de este repo) — el push en los 4 casos es un wrapper fino (`pushEventoCreado`/`pushEventoCancelado`/`pushEventoEditado`/`pushOffseasonCreado`, Edge Function, gateados por `adminToken`) llamado desde el callback de ÉXITO de cada operación, con los datos que el frontend ya tiene en memoria — no depende de dónde vive la mutación real, solo de que ese callback haya llegado. `_evCrearGuardarTerminar()` dispara `pushEventoCreado` sin `idEvento` (el POST a `venues` usa `Prefer:return=minimal`, no devuelve el id nuevo, y la fila real de `asistencias` la genera después el cron `regenerar_ventana_asistencias()`) — el push de creación queda sin los botones de RSVP directo, solo con el link a Eventos. `_evCancelarEvento()` dispara `pushEventoCancelado` con los datos de `ev` YA en `_EV_EVENTOS` antes de cancelar. `_evEditarConfirmar()` arma el texto "Cambios: ..." con `_evEditarResumenCambios()` (nueva, mismos `_evEditarCambios`/`_evEditarOriginal` que ya arma el resumen en pantalla) — sin push si no hay ningún cambio confirmado. `_evCrearDescansoGuardar()` dispara `pushOffseasonCreado` solo al CREAR (`!editando`), no en cada edición posterior de un descanso ya anunciado. **RSVP → aviso a admins**: `marcarAsistenciaUsuario()` (Edge Function) ahora lee el RSVP anterior de `log_asistencias` (origen `'Usuario'`) antes de insertar la fila nueva — si venía de `'Asistiré'` y pasa a `'No asistiré'`/`'No jugador'`, `sendPush()` a todos los admins (`_adminOneSignalAliases()`, ver Edge Function). **Reserva mensual reagendada/cancelada → aviso a admins**: mismo criterio en `cancelarReserva()`/`reagendarReserva()`, acotado a `reservas.tipo==='mensual'` (reservas de clase puntual quedan afuera, a propósito — alto volumen, bajo impacto para un aviso a admins). Fix animaciones drag-to-close móvil: rAF guard previene race condition que dejaba height fija y bloqueaba transitionend; touchstart bloquea altura y remueve clase -auto para activar overflow hidden. **Bug real corregido -- "auto-persist de reglas de asistencia anticipada escribía filas duplicadas en `log_asistencias`"** (reportado por Victor tras encontrar decenas de filas `origen:'AsistenciaAnticipada'`/`estado:'Asistiré'` repetidas para el mismo evento+persona, algunas a milisegundos de distancia): `_evAntReconciliarConReglas()` corre desde 6 puntos distintos de este archivo (`irEventos()` y 5 recargas más tras guardar/crear/editar) sin ningún lock entre sí -- 2+ pasadas solapadas podían leer el mismo evento como "todavía sin responder" (`ev.miEstado==null`) antes de que la primera terminara su `apiPost`, y cada una disparaba su propio auto-persist. `_evAntAutoPersistEnVuelo` (Set de módulo, nuevo, junto a la función) frena la redundancia DENTRO de la sesión actual -- marca el `idEvento` antes del `apiPost`, lo libera en éxito o error del callback. Es un complemento barato, no la protección real: no ve llamadas desde otra pestaña/dispositivo. La protección autoritativa (cierra la carrera en cualquier escenario) es un no-op idempotente del lado del servidor -- ver sección 5, `marcarAsistenciaUsuario`. **RSVP: botones ocultos a partir de 10 minutos antes del inicio del evento** — `_evRsvpBarraHtml(e)`: al principio de la función, si `e.horaInicio` existe, arma `_inicioEvento` (`Date` local a partir de `e.fecha`+`e.horaInicio`) y devuelve `''` (sin barra de RSVP) si `new Date() >= _inicioEvento - 10min`. Corre ANTES que el resto de los chequeos de la función (cuota, reserva de clase, etc.) — un evento sin `horaInicio` (caso borde, no debería pasar con datos reales) no se ve afectado, salta este bloque entero. **Detalle de evento — pills de info más explícitas + stats sin cards vacías** (`_evDetalleInfoHtml()`/`_evPintarStatsAsistencia()`): el pill `.fi-pill-lugar` (con `mapsUrl`) suma un separador `.fi-pill-div` ("|", css/reservas.css) + ícono `navigation` + texto "Cómo llegar" después del nombre del lugar, mismo link de Maps ya existente — sin `mapsUrl` el pill sigue siendo `<span>` sin ese agregado. Los pills de horario pasan de "`hs`"/"Fin `hs`" a "Inicia `hs`"/"Finaliza `hs`" (`.fi-pill-hora`/`.fi-pill-fin`), texto plano, sin cambio de clase ni lógica. `_evPintarStatsAsistencia()` filtra `grupos` a solo los que tienen `personas.length > 0` antes de pintar `#ev-detalle-stats` — un grupo (ej. "Ausentes") con 0 personas ya no deja una card vacía en la grilla; `#ev-detalle-asistencia-lista` (debajo) sigue pintando TODOS los grupos sin filtrar, a propósito (esa lista ya maneja sus propios vacíos por sección). **Bug real corregido — "jugadores inactivos aparecen en Sin respuesta"** (reportado por Victor: el filtro previo solo excluía `estadoMiembro==='Ausente'`, pero ese campo no se actualiza en vivo — alguien sin marca real hace 30+ días podía seguir figurando `'Activx'` y aparecer igual): `_evRenderDetalleAsistencia()` ahora también excluye por `ultimaAsistencia` — nula, o con 30+ días de antigüedad (`Math.floor((Date.now() - new Date(p.ultimaAsistencia).getTime()) / 86400000) >= 30`), mismo umbral de 30 días que `_eqEstadoEfectivo()`/js/equipo.js y `_evEsInactivo()`/js/eventos.js usan para el mismo concepto. Backend: `adminBuscarPersonasParaEvento()` (`supabase/functions/api/index.ts`) suma `ultimaAsistencia` a la respuesta — el pedido original asumía una columna `equipo.ultima_asistencia`, que NO existe (verificado contra migraciones — `20260829123915_equipo_campos_nuevos.sql` documenta explícitamente que esa fecha no tiene columna propia); corregido para calcularla igual que `adminGetRosterEquipo()`/`getEquipo()` (máximo `fecha_entrenamiento` en `log_asistencias` con `origen='Admin'` y `estado != 'Ninguno'`, agrupado por `nombre_usuario`), sin `.in()` a propósito (mismo criterio que `_ultimaAsistenciaPorPersonaTodas()`, evita el truncado por el límite de 1000 filas de PostgREST ya corregido antes en este archivo). Deployado. **Sheet in-app para "Video instructivo" del lugar** (feat nueva, pedido explícito): el pill `.fi-pill-video` (`_evDetalleInfoHtml()`, solo visible si `venues.video_instructivo` tiene valor) pasa de `<a target="_blank">` a `<button onclick="_evAbrirVideoInstructivo(url)">` con texto "Ver cómo llegar" (pedido explícito de Victor sobre el texto exacto, pese a no describir la acción real del botón — abre el video, no un mapa) — `_evYoutubeEmbedUrl(url)` (nueva) reconoce los 3 formatos de URL de YouTube (`youtube.com/shorts/`, `youtube.com/watch?v=`, `youtu.be/`) y arma la URL de embed; si el link no matchea ninguno (otra red, ej. Instagram/TikTok) cae a `window.open(url, '_blank', 'noopener')` de siempre, sin sheet. Con match, `_evAbrirVideoInstructivo()` pinta la URL de embed en `#ev-video-iframe` (`index.html`, `<iframe>` fijo con `allow="autoplay; encrypted-media"`) y muestra `#ev-video-sheet` (overlay `position:fixed` a pantalla completa, `.ev-video-sheet`/css/eventos.css, fade de opacidad 0.3s vía clase `.visible`) — `_evCerrarVideoInstructivo()` saca la clase, espera los mismos 300ms de la transición y recién ahí pone `display:none` + limpia `iframe.src=''` (corta el audio/reproducción, no solo la oculta). Cierra al tocar fuera del contenedor (`onclick` en el overlay, compara `event.target===this`) o el botón X (`.ev-video-cerrar`, círculo semi-transparente arriba-derecha). `.ev-video-contenedor` fijo en `aspect-ratio:9/16` (pensado para Shorts, formato vertical típico de los videos instructivos de lugar) — un video horizontal futuro requeriría ajustar ese valor, sin mecanismo de detección automática todavía. **Bug real corregido — "el pill de video instructivo nunca se mostraba"** (`_evMapEventoBackend()`): `getEventosRango()` (backend, index.ts) ya mandaba `videoInstructivo` por evento (vía `_mapaVideoInstructivoPorLugar()`, `venues.video_instructivo`) desde antes de esta feature, pero el mapeador del frontend nunca copiaba ese campo al objeto final de `_EV_EVENTOS` — `ev.videoInstructivo` (leído por `_evDetalleInfoHtml()`) daba `undefined` siempre, así que el botón nunca aparecía pese al dato llegar bien. Fix: `_evMapEventoBackend()` suma `videoInstructivo: raw.videoInstructivo || ''` al resto de los campos que ya copiaba. **Pill de ubicación unificado** (pedido explícito, `_evDetalleInfoHtml()`): `.fi-pill-lugar`/`.fi-pill-video` — antes 2 pills `.fi-pill` separados uno al lado del otro — se fusionan en un único pill visual: `.fi-pill-lugar` pasa a ser solo el CONTENEDOR (`padding:0`, mismo fondo/borde cyan de siempre) con 1-2 hijos clicables independientes adentro (`.fi-pill-accion`, `background:none;border:none;font:inherit`, look transparente que hereda el fondo del contenedor) — el link de Maps ("lugar + Cómo llegar", `<a>`) y, si hay `videoInstructivo`, el botón de video ("Ver cómo llegar", `<button onclick="event.stopPropagation();_evAbrirVideoInstructivo(...)">`) separados por el mismo `.fi-pill-div` ("|") que ya separaba lugar/Cómo-llegar adentro del link. `event.stopPropagation()` en el botón de video — necesario ahora que vive DENTRO de un `<span>` que ya no es clicable en sí (a diferencia de antes, pill propio) pero podría heredar handlers de un ancestro a futuro; sin efecto real hoy, defensivo. Sin `mapsUrl`, el lado izquierdo cae a `.fi-pill-accion-static` (`<span>`, mismo padding que `.fi-pill-accion` sin comportamiento clicable) — mismo criterio que el `<span>` sin link de antes. `.fi-pill-video` (clase, css/reservas.css) queda sin uso real en el HTML (el botón de video ahora es `.fi-pill-accion`) pero no se borra, mismo criterio de no tocar CSS no pedido explícitamente.
│   ├── tareas.js                Sección Tareas (mis tareas / disponibles / archivadas / gestión admin). Wizard "Nueva tarea" (`#s-tareas-crear`, `_TAR_CREAR_STEPS`): 0 Nombre+Área+Descripción / 1 Puntos+Máximo de personas / 2 "¿Cómo se asigna esta tarea?" (libre/elegir) / 3 Asignar a personas (solo si "elegir") / 4 **"¿Cuándo se hace esta tarea?"** (paso nuevo, pedido explícito, ver MANIFEST.md/CHANGELOG.md — "paso nuevo justo antes de la fecha: tarea por realizar / ya realizada") — mismo componente `.opciones`/`.opcion` que el paso de Modo (2 cards ícono+título+descripción, toggle exclusivo a mano vía `_tarCrearSelTipo()`, nuevo): "Tarea por realizar" (`schedule`, default) vs "Tarea ya realizada" (`task_alt`) — `_tarCrearData.tipo` ('futura'/'realizada'), gatea el botón "Continuar" igual que el paso de Modo, y limpia la fecha ya elegida al cambiar de opción (una selección válida para un sentido puede no serlo para el otro) / 5 Fecha límite (último paso, calendario inline `.ev-ant-cal-*`/`.ev-cal-*` reusado de Eventos) — **título y dirección del calendario dependen del paso nuevo**, recalculados en `_tarCrearMostrarPaso()` cada vez que este paso se vuelve el activo (cubre ir y volver cambiando de tipo en el medio): `tipo:'futura'` → título "¿Cuál es la fecha límite?", bloquea fechas PASADAS (comportamiento de siempre); `tipo:'realizada'` → título "¿Cuándo se completó?", INVIERTE el bloqueo — bloquea fechas FUTURAS, solo pasado/hoy habilitados (`_tarCrearCalRender()`, misma clase visual `.ev-ant-cal-pasado` reusada en los 2 sentidos, sin sumar una clase nueva) — el aviso "X días para realizar la tarea" (`_tarActualizarDiasParaCompletar()`) se omite por completo en modo "realizada" (no aplica, la fecha elegida no es un plazo a futuro). Al guardar (`_tarCrearGuardar()`), `datos.yaRealizada` (`true` si `tipo==='realizada'`) viaja a `adminCrearTarea` (backend, `supabase/functions/api/index.ts`) — si además hay gente elegida (`asignarA`), el backend crea esas asignaciones YA con `estado:'aprobada'`/`fecha_revision`=la fecha elegida (en vez de `'iniciada'`) y acredita los puntos de una sola vez vía `_acreditarPuntosTarea()` (mismo mecanismo que `adminValidarTarea('aprobar')`, sin pasar por revisión ni descuento por atraso — no hay "envío" real que comparar contra un vencimiento) al MES/AÑO de la fecha elegida, no el de hoy (mismo criterio que otros créditos retroactivos de esta app, ej. racha "mes del evento"); la tarea pasa directo a `estado:'archivada'`. Sin gente elegida, `yaRealizada` no tiene ningún efecto (no hay a quién acreditarle) — mismo comportamiento de siempre, tarea `no_iniciada` sin asignaciones. Dots de progreso (`_tarCrearRenderProg()`) ya contemplan el paso nuevo sin cambios de código — se derivan del largo de `_TAR_CREAR_STEPS`. **Soporte offline** (ver MANIFEST.md/CHANGELOG.md — "soporte offline para Ajustes, Equipo y Tareas"): `_tarCargarTodo()` sirve un snapshot combinado (`'tarcache'` en `localStorage` — disponibles/baúl/config/mis tareas/pendientes de validación/activas de gestión admin) cuando arranca sin conexión, guardado por `_tarGuardarCache()` (nueva) tras cada carga exitosa de cualquiera de esas listas. `_tarEnviarRevision()` (completar/enviar a revisión) y `_tarCrearGuardar()` (crear tarea) cortan al inicio con un toast si `!navigator.onLine`.
│   ├── home.js                  Home, historial, cancelar/reagendar
│   ├── reservas.js              Flujo de reserva (s2→s6)
│   ├── perfil.js                Editar datos, permisos Google, wizard de Salud, eliminar cuenta. **"Ajustes de admin"** (sección nueva, pedido explícito, ver MANIFEST.md/CHANGELOG.md — "sección Ajustes de admin en Ajustes") — al final del render de Ajustes (`irEditarDatos()`, después de "Zona cuenta"), acordeones de Categoría/Estado para la CUENTA PROPIA del admin logueado, visible solo con `_adminToken` — `_datosRenderAdmin()` (nueva) puebla `#aj-admin-wrap` (index.html) reusando `_eqTierAdminHtml()`/`_eqAdminGestionHtml()` (js/equipo.js) TAL CUAL en vez de duplicar ese markup, mismo criterio de "1 sola fuente de verdad" que ya usan `_eqStatsContenidoHtml()`/`_datosRenderStats()` para las stats. `E.nombre` (username del logueado) es la misma natural key que `p.id`/`p.nombre` en `_eqPersonas` (`getEquipo()`, ambos = `username`) — `_eqAsegurarCargado()` (ya cargado o disparado acá si Ajustes se visita antes que Equipo, mismo patrón que `_datosRenderStats()`) + `_eqPersonaPorId(E.nombre)` encuentran el objeto REAL del roster si el admin tiene fila en Equipo, así los onclick de esas 2 funciones (`_eqCambiarTier()`/`_eqCambiarEstado()`/`_eqToggleCuota()`/`_eqToggleAdmin()`, que resuelven a quién togglear buscando ese mismo id en `_eqPersonas`) funcionan de verdad. Sin fila en Equipo (cuenta admin "pura", `dashboardAdmin:true`, ver guard existente de `irEditarDatos()`) arma un objeto mínimo con `id:E.nombre` y los valores iniciales que sí trae `E.datos` (`categoria`→`tierModo`, `estado_miembro`→`estado`) — los toggles quedan sin efecto real en ese caso (no hay fila que actualizar), degradación silenciosa igual que el resto de esta pantalla con datos ausentes. **Soporte offline** (ver MANIFEST.md/CHANGELOG.md — "soporte offline para Ajustes, Equipo y Tareas"): `_ajGuardar()`, el guardado genérico de cualquier cambio de esta pantalla (`actualizarDatosPersona`), corta al inicio con un toast si `!navigator.onLine`, en vez de dejar que la escritura falle recién después con un error de red genérico.
│   ├── equipo.js                 Roster de Equipo (Favoritos como acordeón no colapsable con slide+fade al aparecer/desaparecer — corregido para seguir animando bien aunque el toggle de favorito se dispare desde el perfil de detalle, con `#s-equipo` en `display:none` en ese momento, ver `_eqMostrarSeccionFavoritos()`/`_eqOcultarSeccionFavoritos()` —, grupos por categoría + INACTIVXS colapsado (texto visible, rol interno `'Inactivos'`) + LESIONADXS no colapsable, headers de las 5 secciones (Favoritos/Quindes/Mirlxs/Inactivxs/Lesionadxs) sticky apilados al scrollear con comportamiento dual al tocar (ver bloque grande de abajo, "Headers sticky apilados"), cards de lista solo con puntos totales (`_eqStatsInlineHtml()`), nav con panel deslizable "Mis estadísticas" del usuario logueado (sin foto/nombre) + panel deslizable de búsqueda+filtros (rediseño igual a Eventos: subtítulo "Filtros" + 2 pills de categoría con chevron "Puntos"/"Rol" (`.eq-filtro-trigger`, outlined), `_eqToggleFiltroBurbuja()`, cada una despliega su propia burbuja con los sub-pills reales — comportamiento RADIO igual que Eventos, abrir una cierra la otra, `_eqFiltroBurbujaAbierta` un solo valor 'puntos'|'rol'|null — reemplazan los 2 títulos de sección fijos que había antes; badge numérico en el ícono de lupa, `_eqActualizarBadgeFiltros()`/`_eqPeriodoEsDefault()`, cuenta categorías con selección activa igual que `_evActualizarBadgeFiltros()`/js/eventos.js, llamada desde `_eqAplicarFiltrosAhora()` y `_eqFiltroRolToggle()`) — período de puntaje (2 pills Fecha/Histórico, `.eq-periodo-pills`, sub-pills `.aj-pill` vainilla — sin override, mismo look que los sub-pills de Eventos —, "Fecha" sin chevron; Histórico aplica directo; "Fecha" abre `_eqAbrirModalFecha()`, modal CENTRADA tipo Google Calendar (`.eq-modal-centro-*`, no un bottom sheet — overlay+card siempre montados, ocultos con `opacity`+`pointer-events`, revelados con la clase `.visible`; fade+scale de entrada, alto fijo ~520px, footer propio Cancelar/Aplicar que reusa `.eq-confirm-sheet-btns`/`.eq-confirm-btn-cancel`/`.eq-confirm-btn-ok`; `.eq-modal-centro-body` con `min-height:0` — bug real corregido, "el calendario se corta al cambiar de mes"/"últimos meses del selector cortados": sin ese `min-height:0`, un flex-item deja el default `min-height:auto` de flexbox, que lo hace CRECER más allá del alto disponible en vez de respetar `flex:1` y activar su propio `overflow-y:auto` — un mes de 6 filas (ej. agosto, arranca en sábado) o la grilla Ene-Dic completa terminaba más alto que el espacio real dentro de la card de alto fijo, y el `overflow:hidden` de esa card (ahí solo por las esquinas redondeadas) recortaba las últimas filas en vez de dejarlas scrolleables) con el calendario de "asistencia anticipada" adentro — un solo mes con nav `‹ mes-a-mes ›` —, con 3 vistas dentro de la misma modal (`_eqCalFecha.vista`, `_eqRenderModalFecha()`): 'dias' (default), 'meses' (grilla Ene-Dic, reusa `.ev-ant-mes-grid`/`_evAntRenderMesesGrid()`) y 'anios' (grilla de años estilo Google Calendar, 3 columnas/scroll, mismo patrón que `.dp-year-grid` de `shared/date-picker.js`) — el header tiene 2 palabras tappeables independientes (mes abre 'meses', año abre 'anios', cada una con su propio subrayado, punteado/sólido) y el título del sheet cambia por vista ("Elige la fecha"/"Elige el mes"/"Elige el año", sin ninguna leyenda secundaria redundante); selección por FECHA ISO real (`_eqCalFechaTocarDia()`, mismo mecanismo "ida y vuelta" — primer tap fija Desde, segundo fija Hasta; el filtro real que llega a `getEquipo()` sigue siendo solo mes/año, `_eqConfirmarModalFecha()` descarta el día al confirmar — bug real corregido, "al tocar un día el período no se actualizaba": una ronda anterior guardaba una CLAVE DE MES en vez de la fecha real, así que 2 días del MISMO mes producían el mismo valor y el resumen quedaba pegado mostrando el mismo mes 2 veces; ahora el resumen SÍ muestra el día exacto, ej. "01/09/2026 al 17/09/2026"); "Restablecer" navega animado al mes actual y lo deja pre-seleccionado en vez de limpiar a vacío; todo cambio de mes/vista se anima (fade del contenido vía `_evFadeSwap()`/js/eventos.js + resize del wrapper `.eq-cal-fecha-viewport`, en paralelo, `_eqCalFechaRenderContenido()`) — reemplaza las 3 pills Mes/Rango/Histórico y las 2 modales `_eqAbrirModalMes()`/`_eqAbrirModalRango()` que existían antes; **fechas futuras deshabilitadas** (ver MANIFEST.md/CHANGELOG.md — "usar la fecha actual del dispositivo como referencia") — `_eqPintarDiasGrid()`/`_eqPintarMesesGrid()` comparan cada celda/mes contra `new Date()`/`_evHoyISO()` (nunca el mes MOSTRADO) y suman `.eq-cal-fecha-futuro` (css/equipo.css, mismo tratamiento visual que `.ev-ant-cal-pasado`/css/eventos.css) + omiten el `onclick`/agregan `disabled` real (botones de mes) en vez de solo bloquear con `pointer-events` — navegar a un mes futuro con las flechas ya cae solo (mismo render se re-ejecuta en cada `_eqCalFechaMoverMes()`); `_eqCalFechaClampAFuturo()` (llamada desde `_eqAbrirModalFecha()`, `_eqCalFechaTocarDia()` y `_eqConfirmarModalFecha()`) es el gate defensivo — bajo uso normal nunca hace falta (nada con `onclick` puede producir una fecha futura), pero si `_eqFiltroPeriodo` trae guardado un mes futuro de antes de este fix (o cualquier otro camino), ajusta `desde`/`hasta`/el mes MOSTRADO a hoy antes de renderizar/confirmar) + rol (sin botón "Aplicar filtros" — cada selección aplica sola, `_eqAplicarFiltrosAhora()`, que ahora siempre manda mesDesde/anioDesde/mesHasta/anioHasta a `getEquipo()`, incluso para 1 solo mes) —, `_eqTogglePanel()` mutuamente excluyente entre "Mis estadísticas" y búsqueda/filtros — ambos paneles ahora viven dentro de `#eq-sticky-header` (ver css/equipo.css); se cierran por scroll con 2 mecanismos DISTINTOS por panel (re-ajuste, ver MANIFEST.md/CHANGELOG.md — "el panel de búsqueda/filtros debe ocultarse rápido al scrollear, igual que en Eventos, no progresivo": una ronda anterior aplicaba el mismo drag progresivo a los 2 paneles por igual, pedido explícito de ese momento; ahora Equipo replica la misma distinción que ya tenía Eventos entre su panel de calendario y el de búsqueda) — SOLO `stats` sigue con colapso progresivo al scrollear (`_eqInicializarCierrePanelesPorScroll()`, ver MANIFEST.md/CHANGELOG.md — "colapso progresivo al scrollear", puerto 1:1 de `_evInicializarCierreCalendarioPorScroll()`/js/eventos.js: drag-to-dismiss en vivo con touch sobre `#eq-lista-contenido` — nunca sobre el panel en sí —, el panel se achica proporcional al arrastre mientras el dedo sigue abajo y recién al soltar decide terminar de cerrar, pasado el 30% de su alto original, o volver al alto original; a propósito NO reusa `_eqCerrarPanel()` para terminar de cerrar, mismo motivo que la versión de Eventos: esa función arranca fijando `max-height` al `scrollHeight` completo antes de animar a 0, lo que causaría un "rebote" de vuelta al alto completo en vez de continuar cerrando desde el alto parcial ya arrastrado); `busqueda` usa `_eqCerrarBurbujaSiFueraDe()` (puerto 1:1 de `_evCerrarBurbujaSiFueraDe()`/js/eventos.js) — cierre instantáneo con la transición CSS normal (sin arrastre en vivo) apenas arranca cualquier gesto (`click`, o `pointerdown`/`touchstart` en fase de captura sobre `document`) fuera del panel y de su botón trigger, desactivado mientras hay texto en el buscador (mismo criterio que Eventos: solo la lupa cierra en ese caso); `_eqPanelAbierto` sigue decidiendo cuál de los 2 paneles está activo en cada momento, nunca los 2 a la vez. Bug real corregido (ver MANIFEST.md/CHANGELOG.md — "header sticky del acordeón se superpone sobre los nombres de las personas al scrollear con 'Mis estadísticas' abierto"): el drag en vivo de `stats` achicaba `#eq-sticky-header` cuadro a cuadro, pero `_eqActualizarStickyHeaders()` (ver "headers sticky apilados" más abajo) solo corría en `touchend`+300ms, así que el `top` de los 5 headers de sección quedaba desincronizado (mucho más grande que el alto real) durante todo el gesto — no un problema de VALORES de z-index (`.eq-sticky-header` ya es 100 contra 5 de `.eq-grupo-header--sticky`, jerarquía siempre correcta) sino de un `top` desactualizado, que hacía que `position:sticky` pegara el header mucho más abajo de lo real, tapando nombres — `_eqActualizarStickyHeadersThrottled()` (throttle por `requestAnimationFrame`, evita forzar `offsetHeight`/layout en cada uno de los muchos `touchmove` por segundo) ahora se llama al final de cada `touchmove` del drag de `stats`, manteniendo el `top` al día durante todo el arrastre, no solo al soltar; `_eqActualizarListaVacia()` — bug real corregido, "lista de personas desaparece al usar los filtros": Favoritos/Quindes/Mirlxs/Inactivos/Lesionadxs se ocultan uno por uno cuando su propio filtrado da 0 (típico: un pill de rol que nadie tiene asignado en este dispositivo, `_eqRolesDe()` es per-dispositivo/localStorage — casi cualquier rol salvo "No definido" filtra a todo el mundo afuera) — si los 5 dan 0 A LA VEZ no quedaba ningún contenido visible, sin aviso, indistinguible de un bug de renderizado; muestra `#eq-lista-vacia` (reusa `.eq-favoritos-vacio`) en ese caso, con fade-in/out de opacidad (~250ms, doble `requestAnimationFrame` en la entrada + `setTimeout` en la salida, mismo patrón que el resto de la app — antes un `display` seco sin transición, ver MANIFEST.md/CHANGELOG.md), llamada al final de `_eqRenderFavoritos()`/`_eqRenderGrupo()`/`_eqRenderInactivos()`/`_eqRenderLesionadxs()` — así cubre búsqueda, filtro de rol, filtro de período y carga inicial sin duplicar la llamada en cada caller — esta misma función también dispara `_eqActualizarStickyHeaders()`, ver siguiente punto), **headers sticky apilados** (rediseño REAL, ver MANIFEST.md/CHANGELOG.md — "los sticky headers deben apilarse, no reemplazarse") — bug real corregido, "cuando aparece un nuevo sticky header el anterior desaparece": la 1ra versión usaba `position:sticky` nativo con `top` inline calculado, pero un `position:sticky` solo puede quedar pegado mientras SU PROPIO `.eq-grupo`/`#eq-favoritos-wrap` (uno por sección) todavía tiene alto por scrollear debajo — en cuanto esa sección entera pasaba, el navegador lo soltaba y seguía de largo, así que el header desaparecía en vez de acumularse con el siguiente (funcionaba perfecto DENTRO de la propia sección, por eso no se notaba ahí). Fix: `_eqActualizarStickyHeaders()` ya no usa `position:sticky` — maneja el pegado A MANO, alternando cada uno de los 5 headers reales (`_EQ_GRUPO_HEADERS`, array de ids) entre flujo NORMAL (default) y `.eq-grupo-header--stuck` (css/equipo.css: `position:fixed; z-index:5; background:var(--bg)`, `top`/`left`/`width` inline por instancia) según si `window.scrollY` superó o no su propio umbral (posición natural del header menos el alto acumulado de los headers YA stuck antes que él en la misma pasada — recorrido en orden de DOM, así el apilado sale solo); recalculado en cada evento de `scroll` de la página entera (nuevo `window.addEventListener('scroll', ...)`, throttled por `requestAnimationFrame` vía `_eqActualizarStickyHeadersThrottled()` — antes NO hacía falta ningún listener de scroll porque el pegado visual lo hacía el navegador solo con `position:sticky` nativo; ahora que lo hace JS, tiene que reaccionar a CUALQUIER scroll, no solo al arrastre en vivo de "Mis estadísticas"), más los mismos triggers de siempre (`_eqActualizarListaVacia()`, `resize`, abrir/cerrar los 2 paneles de la nav). `position:fixed` saca al header del flujo del documento — sin compensar, el resto del contenido saltaría hacia arriba el alto exacto del header apenas se vuelve stuck: `.eq-grupo-header-spacer` (nuevo `<div>` vacío justo ANTES de cada header, index.html, mismo `.eq-grupo`/`#eq-favoritos-wrap`) compensa ese hueco (0px natural, `header.offsetHeight`px stuck) y ADEMÁS sirve de referencia ESTABLE de la posición natural de cada header (`spacer.offsetTop`, el spacer JAMÁS deja el flujo así el header sí lo haga — a diferencia de `header.offsetTop`, que deja de ser confiable en cuanto el header pasa a `fixed`). `left`/`width` del header stuck salen de `#eq-lista-contenido.getBoundingClientRect()` (mismo ancho que ya tenía en flujo normal, sin padding propio de por medio) — recalculados en cada pasada, así que sobreviven un `resize`/rotación sin listener aparte. Comportamiento dual al tocar (`_eqGrupoHeaderTap(headerId, rol)`, `rol:null` para Favoritos/Lesionadxs — no colapsables): con el pegado ahora manejado a mano, `_eqHeaderEstaSticky(header)` ya no necesita derivar nada por matemática de scroll — simplemente lee `header.classList.contains('eq-grupo-header--stuck')`, la clase que `_eqActualizarStickyHeaders()` ya puso/sacó; header STUCK → tap hace `_eqScrollAlGrupo()` (`window.scrollTo` suave hasta `_eqHeaderUmbral[headerId]`, el mismo umbral cacheado en la última pasada — el listener de `scroll` corre en cada frame de la animación, así que el header vuelve a NATURAL en vivo, a mitad de camino, apenas cruza ese umbral); header en modo NATURAL → tap llama `_eqToggleGrupo(rol)` tal cual (comportamiento de siempre) si `rol` no es null — colapsar/expandir mientras está stuck es imposible por construcción (el `if` de arriba corta antes), no por un chequeo aparte. Favoritos/Lesionadxs pasan de `<div>` a `<button>` (index.html, antes sin `onclick`). `#eq-favoritos-wrap` (único de los 5 wraps con `overflow:hidden` PERMANENTE en su CSS, para el slide+fade de la sección — ver más arriba) alterna ese `overflow` a `visible` en vivo una vez asentada abierta (`_eqMostrarSeccionFavoritos()`/`_eqRenderFavoritos()`, junto al `max-height:'none'` de siempre) y de vuelta a `hidden` al empezar a cerrarse (`_eqOcultarSeccionFavoritos()`) — necesario porque cualquier `overflow`≠`visible` en un ancestro recorta durante el pintado a CUALQUIER descendiente `position:fixed` (no solo a `position:sticky`, mismo problema con el mecanismo nuevo, otra causa): sin este toggle, el header de Favoritos quedaría recortado contra el wrap en vez de pegarse contra el scroll real de la página. `.eq-grupo-header--sticky` es ahora solo un MARCADOR sin estilo propio (nunca fusionado a `.eq-grupo-header` a secas) — los acordeones de la vista "por rol" (`_eqRenderPorRol()`) también generan `.eq-grupo-header` al vuelo y esos NO deben volverse stuck (no entran en `_EQ_GRUPO_HEADERS`), perfil de detalle (**re-ajuste, ver MANIFEST.md/CHANGELOG.md — "vista detallada de usuario: layout de estadísticas igual a Mis estadísticas + reordenar acordeones + auto-scroll al abrir"**: acordeón "Estadísticas" ahora movido para quedar INMEDIATAMENTE ANTES del acordeón "Categoría" — antes los datos personales (`filas`: teléfono/email/fecha de ingreso/rol) separaban ambos, ahora van DESPUÉS de "Categoría" — y su contenido pasó a ser exactamente `_eqStatsContenidoHtml(p)` (función NUEVA, extraída de lo que antes era una copia a mano desactualizada acá y en `_eqRenderMisEstadisticas()`, ver bullet de esa función más abajo: mismas cards Horas/Asistencia, mismo separador "PUNTOS" con pill de total, mismo grid tareas/asistencia-combo con badge de racha, mismo termómetro — 1 sola fuente de verdad para ambas superficies) — `_eqPuntosRachaHtml(p)` (la card de racha SUELTA, con su propio "Puntos por racha") queda SIN NINGÚN consumidor real desde este cambio (ni acá ni en "Mis estadísticas", que ya la había dejado de llamar en una ronda anterior) — se deja definida por si hace falta una card de racha suelta de nuevo, sin borrarla; la fila "Rol" (dentro de `filas`) sigue oculta si no hay dato, mismo criterio aplicado a TODAS las filas de datos personales (teléfono/email/fecha de ingreso), ninguna con fallback "No"/guión — confirmado que ya no hace falta ningún cambio ahí, la condición `if (p.campo)` ya las omitía por completo. La fila `.eq-periodo-pills` (Fecha/Histórico) propia del acordeón SALIÓ (pedido explícito, "sin filtros de período en estadísticas del detalle") — reemplazada por `.eq-perfil-stats-nota` (texto chico `var(--muted)`, "Los resultados se basan en los filtros aplicados en Equipo") — `.eq-periodo-pills` sigue viva sin cambios, la sigue usando el panel de Filtros de la lista (única instancia que queda; `_eqFiltroPeriodoModo()` sigue sincronizando todas las que encuentre en el DOM). Pill de nivel (Quindes/Mirlxs, `.eq-mis-stats-rol-pill` reusada) en `.eq-perfil-pills-row--top` (junto a pronombres) ahora pisa su padding/font-size/radius chicos (pensados para la nav de "Mis estadísticas") con los mismos valores EXACTOS de `.aj-pill` (pill de pronombres, misma fila) vía el selector descendiente `.eq-perfil-pills-row--top .eq-mis-stats-rol-pill` — mismo color/borde de siempre, solo cambia el tamaño de la caja. **Auto-scroll al abrir cualquier acordeón** (pedido explícito) — `eqToggleAcordeon()` (helper único de los 3 acordeones de este perfil: Estadísticas/Categoría/Estado) llama `acord.scrollIntoView({behavior:'smooth', block:'nearest'})` 320ms después de abrir (pasado el `0.3s` de la transición `max-height` de `.eq-acord-cuerpo`, si no mide la altura vieja colapsada y no mueve nada) — solo al ABRIR, nunca al cerrar. La animación de "fill-in" del termómetro desde 0% que tenía `_eqRenderPerfil()` (doble `requestAnimationFrame` + `getElementById('eq-rank-fill')`) se sacó — ese id ya no existe (`_eqStatsContenidoHtml()`, compartida con "Mis estadísticas", nunca lo tuvo) y el termómetro ahora pinta directo a su ancho final, sin animar el llenado, mismo comportamiento que "Mis estadísticas" siempre tuvo), **desglose de puntos por concepto** (feat nueva, ver MANIFEST.md/CHANGELOG.md — "subpantalla de desglose de puntos por concepto") — tocar cualquiera de las 3 cards de puntos tappables (`.eq-stat-card--tappable`, tareas/asistencia/racha, en los 2 lugares de arriba) abre `#eq-desglose-panel` (index.html), la contraparte "línea por línea" de esos 3 totales, un concepto a la vez (acción nueva `getDesglosePuntos`, ver `puntos_mensuales`/supabase/functions/api/index.ts más arriba) — mecanismo visual "shared axis X" (Material Design 3) IDÉNTICO al de `irAjSub()`/`cerrarAjSub()` (js/perfil.js, subsecciones de Ajustes, pedido explícito "igual a como se navega a subsecciones en Ajustes"): el panel entra desde la derecha (`translateX(100%)->0`, `opacity:0.85->1`) mientras la card de la pantalla de ORIGEN retrocede levemente (`translateX(0)->-25%`, `opacity:1->0.85`), simultáneo — `#eq-lista-card`/`#eq-perfil-card` (index.html, ids nuevos en las cards de `#s-equipo`/`#s-equipo-perfil`) son las 2 posibles cards de fondo, `_eqAbrirDesglosePuntos()` elige según cuál pantalla está `.activa` en ese momento (`_eqDesgloseCtx` guarda cuál, para que el cierre sepa a cuál devolver). A diferencia de `irAjSub()` (que agrega su propio chequeo dedicado al handler GLOBAL de `popstate`, js/ui.js), esto usa `_registrarOverlayAbierto()`/`_overlayStack` (ui.js, mecanismo YA genérico para overlays, ver sección "Principios UX" de este archivo) — mismo resultado real (gesto nativo de "atrás" cierra el panel) sin sumar un 2do caso especial al handler compartido; `_eqCerrarDesglosePuntos(porGesto)` sigue el mismo patrón ya usado por `cerrarSheetEvAccion()`/js/eventos.js (mismo overlay stack) — sin `porGesto` hace `history.back()` (dispara el popstate real, que vuelve a llamar a esta función con `porGesto:true`, recién ahí anima/oculta). Filas por concepto (`_eqDesgloseFilaHtml()`): asistencia — estado (A tiempo/Tarde) + fecha (`_eqFormatearFechaIngreso()`, reusada tal cual); tareas — título de la tarea + fecha de aprobación; racha — "+2 racha" + fecha real del evento, o "Bono histórico" + mes/año (sin día, `legado:true` — ver `getDesglosePuntos()` arriba, sin trazabilidad de evento real para esos bonos del sistema legado). Total al final (`_eqDesglosePuntosTxt()`, formato "+N pt(s)"), subtítulo de período activo (`_eqDesglosePeriodoTxt()`, respeta `_eqFiltroPeriodo` — mes/rango/histórico, mismos params que ya arma `_eqAplicarFiltrosAhora()` para `getEquipo()`, duplicados a propósito en vez de compartir función, mismo criterio de riesgo que `_periodoDesdeParams()` en el backend), empty state con `.eq-favoritos-vacio` (mismo componente reusado en el resto de esta sección) si `filas` viene vacío), **orden por puntos dentro de cada acordeón** (bug real corregido, ver MANIFEST.md/CHANGELOG.md — "ordenar por puntos totales, no alfabético") — `_eqCompararPorPuntos(a, b)` (`puntosTotal` descendente, empate alfabético por `nombreDerby` vía `localeCompare('es')`) aplicado con `.sort()` en los 4 renders que arman un acordeón real (`_eqRenderFavoritos()`/`_eqRenderGrupo()`/`_eqRenderInactivos()`/`_eqRenderLesionadxs()`) — antes `_eqPersonas` heredaba tal cual el orden alfabético por `username` de `getEquipo()` (`.order('username')`, sin `.sort()` propio de este lado); NO aplica a `_eqRenderPorRol()` (vista alternativa de búsqueda por rol, fuera del alcance de este pedido), **chevron de tendencia en "Mis estadísticas"** (bug real corregido, ver MANIFEST.md/CHANGELOG.md — "si no está implementado ahí, agregarlo") — el panel en sí no tiene foto propia (recortada a propósito en una ronda anterior, "sin foto/nombre/número", ver css/equipo.css) así que el chevron va sobre el avatar CHICO del botón colapsado de la nav (`#eq-misstats-toggle-avatar`, 36px, siempre visible, panel abierto o cerrado) — decisión tomada con el usuario ante el conflicto con esa decisión previa, en vez de reagregar una foto grande adentro del panel. `#eq-misstats-toggle-tendencia` (span nuevo, index.html, hermano del avatar dentro de un `.eq-avatar-badge-wrap` que envuelve a los 2) recibe `_eqTendenciaBadgeHtml(persona)` (tamaño base 18px, ya pensado para este lugar exacto desde que existe la feature de tendencia -- "usado hoy solo en 'Mis estadísticas'" decía el comentario de ese momento, pero nunca se había conectado acá) en cada render de `_eqRenderMisEstadisticas()`, **rediseño compacto de "Mis estadísticas"** (pedido explícito, ver MANIFEST.md/CHANGELOG.md — "rediseñar el panel para que sea más compacto", solo cambios de presentación, sin tocar lógica) — pill de nivel MOVIDA de adentro del panel a la nav (`#eq-misstats-toggle-pills`, index.html, entre el texto "Mis estadísticas" y el chevron, siempre visible esté el panel abierto o cerrado) — `.eq-mis-stats-header`/`.eq-mis-stats-info`/`.eq-mis-stats-pills` (el bloque viejo dentro del panel) sin más consumidores, sacadas de `_eqRenderMisEstadisticas()`/css/equipo.css. Re-ajuste posterior (pedido explícito, ver MANIFEST.md/CHANGELOG.md — "nombre completo del nivel, no abreviado; sin pill de estado"): `persona.rol` tal cual ("Quindes"/"Mirlxs") en vez de abreviado ("Q"/"M" — la 1ra versión de este pill los abreviaba vía `_EQ_NIVEL_ABREV`, sacada, sin más consumidores) — el pill de estado ("Activo"/"Inactivo"/"Lesionadx", `.dat-estado-chip`) que había vivido ahí un tiempo se sacó de la nav por completo, no se muestra más en "Mis estadísticas" (`estadoTexto`/`estadoClase` también sin más consumidores en esta función, sacadas). Separador "PUNTOS" con el total inline (`.eq-mis-stats-puntos-row`, línea-label-pill-línea, reusa `.eq-grupo-linea`/`.eq-grupo-nombre` tal cual) reemplaza la card `.eq-stat-card--full` de "Puntos totales" que vivía como una card más del grid — mismo pill "naranja" (`.eq-mis-stats-total-pill`, borde+fondo tenue+texto de marca) que el de nivel, comparten declaración CSS. Card combinada "Asistencia + Racha" (`.eq-stat-card--combo`) reemplaza las 2 cards separadas que había antes — ícono `sports` solo arriba (re-ajuste posterior, pedido explícito — antes también `local_fire_department` ahí, sacado; el fuego de racha sigue viéndose en el badge inline, sin duplicarlo), valor de asistencia grande + badge de racha inline (`.eq-stat-combo-racha-badge`, SOLO si `puntosRacha>0`, mismo criterio "nunca 0" del resto de esta sección) — toda la card abre el desglose de ASISTENCIA al tocar, el badge de racha es su PROPIO target de tap (`event.stopPropagation()`, mismo patrón que `.eq-fav-btn` dentro de `.eq-miembro-fila`) que abre el desglose de RACHA — `_eqPuntosRachaHtml()` (la card de racha separada, con su propio "Puntos por racha") sigue viva pero SIN NINGÚN consumidor real (dejaba de llamarla acá desde esta misma ronda; `_eqPerfilContenidoHtml()`/perfil de detalle también dejó de usarla en una ronda posterior, ver ese bullet más arriba — ambas superficies comparten hoy el mismo `_eqStatsContenidoHtml()`). **Re-ajuste posterior (pedido explícito, ver MANIFEST.md/CHANGELOG.md — "layout de estadísticas del perfil de detalle igual a Mis estadísticas")**: todo este bloque de contenido (cards Horas/Asistencia, separador PUNTOS, grid tareas/asistencia-combo, termómetro) se extrajo a `_eqStatsContenidoHtml(p)` (función NUEVA, misma firma que `_eqStatsCalc(p)`) — `_eqRenderMisEstadisticas()` ahora solo arma la pill de nivel del toggle y llama `cont.innerHTML = _eqStatsContenidoHtml(persona)`, y `_eqPerfilContenidoHtml()` (perfil de detalle) hace lo mismo dentro de su acordeón "Estadísticas" — 1 sola fuente de verdad para las 2 superficies, sin copias a mano que puedan desalinearse. Sufijo "(mes)" sacado de todos los labels de este panel ("Puntos por tareas", ya no "Puntos por tareas (mes)"); re-ajuste posterior, label de la card combinada vuelto a "Asistencia" sola (antes "Asistencia + Racha", ver más abajo -- el badge de racha con el ícono de fuego sigue en la card, sin cambios ahí, solo el texto del label). **Bonificación por racha dentro del desglose de asistencia** (feat nueva, ver MANIFEST.md/CHANGELOG.md -- "mostrar bonificaciones de racha en el mismo desglose") -- `_eqAbrirDesglosePuntos()` pide `getDesglosePuntos` 2 veces en paralelo cuando `concepto==='asistencia'` (esa acción no tiene un modo "combinado", cada concepto es una consulta propia) y renderiza con `_eqRenderDesgloseAsistenciaConRacha(resAsistencia, resRacha)` en vez de `_eqRenderDesglosePuntos()` -- filas de asistencia normales primero, y SOLO si `resRacha.filas` trae algo (pedido explícito: "ni el header ni las filas" si no hay bonificaciones) un separador "Bonificación por racha" (reusa `.eq-mis-stats-puntos-row`/`.eq-grupo-linea`/`.eq-grupo-nombre` tal cual, mismo look que el separador "Puntos" de "Mis estadísticas") + las filas de racha con el MISMO formato de fila que asistencia (`_eqDesgloseFilaHtml()`, pedido explícito) pero ícono `local_fire_department` en vez del `emoji_events` que sigue usando la vista STANDALONE del concepto `'racha'` (se llega ahí tocando el badge de racha de la card, sin cambios) -- coincide con el fuego que ya usa ese mismo badge. "Total" propio para la sección de racha, separado del total de asistencia -- 2 números con distinto significado. **Soporte offline** (ver MANIFEST.md/CHANGELOG.md — "soporte offline para Ajustes, Equipo y Tareas"): `_eqAsegurarCargado()` sirve el roster (`_eqPersonas`) desde un snapshot en `localStorage` (`'eqcache'`, guardado tras cada `getEquipo()` exitoso) cuando arranca sin conexión, en vez de encolar el callback esperando un fetch que nunca resuelve -- `_eqCargado` queda en `false` a propósito en ese camino, para que la próxima carga con red vuelva a pedir datos frescos. Las 3 escrituras admin del perfil (`_eqCambiarEstado()`/`_eqToggleCuota()`/`_eqCambiarTier()`) cortan al inicio con un toast si `!navigator.onLine`. **"Activar cuenta"** (feat nueva, ver `activar/` en sección 1 de este archivo) — 4ta fila dentro del acordeón "Estado" de `_eqAdminGestionHtml()`, visible SOLO si `sinEmail` (mismo campo/condición que ya gatea el toggle "Admin" de la fila de arriba) — botón "Generar link" (`_eqGenerarInviteLink(id)`, nueva) llama `generarInviteToken` (Edge Function) y copia `https://app.quindesvolcanicos.com/activar/?token=<token>` al portapapeles con un toast de confirmación
│   ├── admin.js                 Mi Liga: reservas (`adminRenderReservas()`, cards `.reserva-card` con equipo/monto — "Pagó: $N", `reservas.monto` tal cual devuelve `adminGetReservas()`, incluido $0 en reservas pagadas con crédito de reagendo/cupón, antes oculto del todo por un chequeo truthy que trataba `0` como "sin dato"), notificaciones, equipamiento, usuarios, admins, tiers/categorías (`_mlTierFilaHtml()`: input de nombre del tier con `maxlength="12"` — pedido explícito, ver MANIFEST.md/CHANGELOG.md, "límite de 12 caracteres en el nombre de categoría/tier")
│   ├── offline.js               **Modo sin conexión para Eventos** (feat nueva, pedido explícito, ver MANIFEST.md/CHANGELOG.md — "modo sin conexión completo para la sección Eventos") — módulo autocontenido cargado después de `js/eventos.js` (index.html), sin estado propio duplicado: opera sobre `_EV_EVENTOS`/`_EV_CUMPLEANOS`/`_EV_ANIVERSARIO_INGRESO`/`_EV_OFFSEASON` (variables YA existentes de esa sección) en vez de mantener su propia copia. IndexedDB (`mirlxs_eventos_offline`, DB nueva, apertura perezosa cacheada en `_offDb`) con 2 object stores: **`cache`** — 1 sola fila (`key:'eventos'`) con el snapshot completo que pinta el timeline (`_offGuardarCache()`/`_offLeerCache()`) — los eventos YA traen `asistentes`/`rsvps` embebidos por evento desde `_evMapEventoBackend()` (ver ese comentario, js/eventos.js), así que cachear `_EV_EVENTOS` alcanza para cubrir el roster completo de asistentes con su estado por evento en modo admin, sin un fetch/cache separado por evento; **`cola`** — acciones pendientes de sincronizar (`{id (auto), tipo:'rsvp'|'adminMarcar', apiParams, meta:{idEvento, nombre, previo, estadoNuevo, descripcion}, intentos, ts}`), orden real de creación vía `autoIncrement` (nunca reordenada al procesar). **Lectura offline**: `_evCargarDatosReales()` (js/eventos.js) engancha al inicio — `if (!navigator.onLine) { _offCargarDesdeCache(onListo); return; }` — sirve el último snapshot en vez de los 4 pedidos de red de siempre (mismo hook cubre TODOS los callers existentes de esa función, no solo `irEventos()`); con conexión, el mismo `unoListo()` interno escribe el cache al terminar (`_offGuardarCache()`), sin tocar el resto de esa función. **Acciones offline con cola**: `_evMarcarAsistencia()` (RSVP propio) y `_evMarcarAsistenciaAdmin()` (marcado admin, ambas js/eventos.js) YA aplicaban el cambio optimista al array en memoria + DOM ANTES de llamar a `apiPost()` (mecanismo preexistente, sin tocar) — el hook nuevo intercepta JUSTO ANTES de esa llamada: sin conexión, `_offEjecutarOEncolar()` encola la escritura real y preserva el estado optimista sin revertir (a diferencia del `onError` real, que si revierte) — el estado "pendiente de sincronizar" se refleja solo en el contador del banner (`#ev-offline-banner-sync`, ver "Banner" más abajo), sin ningún ícono por-control individual (el `.ev-sync-badge` que existía inicialmente sobre `.ev-rsvp-seg[data-evid]`/`#ev-asist-admin-header-<id>`, junto con las funciones que lo inyectaban/reaplicaban tras cada render — `_offMarcarPendiente()`/`_offAplicarIndicadoresPendientes()` — se sacaron del todo, pedido explícito, ver MANIFEST.md/CHANGELOG.md "sacar el ícono .ev-sync-badge por control"; `_offBorrarDeCola()` sigue llamándose igual en los mismos 3 puntos, solo que ahora sin ese paso intermedio). **Sincronización al reconectar**: listener `window.addEventListener('online', ...)` dispara `_offSincronizarTodo()` — 1) pull fresco vía `_evCargarDatosReales()` (ya sabe pegarle a la red, `navigator.onLine` volvió a `true`), 2) `_offSincronizarCola()` procesa la cola en orden cronológico, 3) re-render + banner. **Conflicto**: `_offDetectarConflicto()` compara el valor remoto YA fresco (evento/persona de `_EV_EVENTOS` recién repoblado) contra `accion.meta.previo` (el valor que había ANTES del cambio offline) — si el remoto fresco no coincide NI con `previo` NI con `estadoNuevo` (alguien más lo cambió mientras tanto), hay conflicto real; `_offResolverConflicto()` avisa vía `window.confirm()` nativo (caso borde poco frecuente — 2 personas/dispositivos tocando el mismo evento mientras uno está offline — no amerita un bottom sheet propio nuevo solo para esto) y aplica o descarta la acción según la respuesta. **Reintentos**: `_offEjecutarAccionReal()` reintenta hasta 3 veces con backoff exponencial (1s/2s/4s) antes de descartar la acción con un toast de error, sin bloquear el resto de la cola por una sola acción que sigue fallando. **Refresco manual**: botón en `#ev-offline-banner` (`_offRefrescarManual()`) — no-op con toast si se sigue sin conexión, si no dispara el mismo `_offSincronizarTodo()` de arriba. **Banner** (`#ev-offline-banner`, index.html, arriba de `#ev-timeline`, oculto por default) — `_offActualizarBanner()` lo muestra sin conexión ("Sin conexión — mostrando datos guardados (HH:MM)", hora del último snapshot) o con conexión si el snapshot ya pasa `_OFF_BANNER_MAX_MIN` (15) minutos sin refrescar ("Datos de hace N min — toca para actualizar") — re-evaluado al final de cada `_evRenderTimeline()` y por un `setInterval` propio cada 60s (para reflejar el paso del tiempo aunque el timeline no se vuelva a renderizar) — oculto en cualquier otro caso (online + snapshot fresco). Verificado con Playwright forzando `navigator.onLine` vía `Object.defineProperty` (más confiable en este entorno que `context.setOffline()` de Playwright, que no reflejó el flag de forma consistente): guardado/lectura de cache real en IndexedDB, `_evCargarDatosReales()` offline sirve del cache sin llamar a `api()`/`apiPost()`, RSVP offline aplica el optimista + encola sin llamar a la red, sync al reconectar vacía la cola, conflicto detectado con el mensaje correcto y las 2 resoluciones (aceptar sobreescribe y llama a `apiPost()`, rechazar descarta sin llamarlo), reintento agota 3 intentos con ~3s de backoff acumulado antes de descartar con toast, y el banner en sus 3 estados (offline/online-fresco/online-stale). **Bug real corregido — el RSVP/marcado no persistía al cerrar y reabrir la app** (pedido explícito): `_offGuardarCache()` solo se llamaba desde `_evCargarDatosReales()` (fetch completo) — ni `_evMarcarAsistencia()` ni `_evMarcarAsistenciaAdmin()` lo llamaban tras aplicar su cambio optimista (online confirmado, u offline recién encolado), así que ese cambio quedaba SOLO en memoria (`_EV_EVENTOS`) hasta la próxima carga completa exitosa — cerrar la app (o quedarse sin conexión) antes de esa próxima carga dejaba el snapshot de IndexedDB con el RSVP/marca VIEJO, visible de nuevo al reabrir. Fix: las 2 funciones llaman `_offGuardarCache()` en sus 2 caminos de éxito (justo después de `_offEjecutarOEncolar()` en la rama offline, y dentro del callback de éxito de `apiPost()` en la rama online) — nunca en el camino de error/revert, que ya deja el cache en su valor correcto sin tocarlo (nunca se había escrito el optimista ahí para empezar).

**Banner offline global** (`#app-offline-banner`, feat nueva, pedido explícito, ver MANIFEST.md/CHANGELOG.md — "banner offline global") — distinto de `#ev-offline-banner` de arriba (propio de Eventos, con hora del último snapshot + contador de pendientes + botón "Actualizar"): este es un aviso genérico ("Estás sin conexión. Cualquier cambio que hagas se actualizará cuando vuelvas a tener conexión."), fijo arriba de TODA la app (`position:fixed;z-index:9999`, css/global.css) — hijo directo de `<body>` en index.html, antes de `#s1`, así queda visible sobre cualquier sección/overlay. `_offActualizarBannerGlobal()` (al final de este archivo) solo togglea `display:none`↔`block` según `navigator.onLine`, sin ningún estado propio (a diferencia de `_offActualizarBanner()`, sin hora de snapshot ni detección de "stale") — enganchada a `window.addEventListener('online'/'offline', ...)` y a `DOMContentLoaded` (para el estado inicial si la página ya carga sin conexión).
│   ├── pwa.js                   Install prompt, push (OneSignal). **Enforcement de PWA + notificaciones obligatorias** (feat nueva, pedido explícito — "sistema completo de push notifications con OneSignal + enforcement de PWA en Android", ver "Push notifications" en la sección Edge Function más abajo): `_verificarPwa()` — Android real fuera de `display-mode:standalone` muestra `#pwa-gate` (index.html, pantalla completa, sin botón de cierre) con las 3 instrucciones para instalar desde Chrome; iPhone/iPad y desktop no se tocan, a propósito. `_verificarNotificaciones()`/`_activarNotificaciones()`/`_cerrarNotifGate()` — YA instalada como PWA (`esStandalone()`) y `Notification.permission !== 'granted'`, muestra `#notif-gate` (mismo criterio de pantalla completa bloqueante) con un botón que dispara `OneSignal.Notifications.requestPermission()`; `Notification.permission === 'denied'` oculta el botón y muestra instrucciones manuales (desbloquear desde el candado de la barra de direcciones) en vez de un prompt que el navegador ya sabe que va a fallar. Ambas se llaman desde `window.onload` (js/auth.js), ANTES que cualquier otra cosa — el resto del boot (login/restaurar sesión) sigue de largo en paralelo, ningún `return` las frena, pero la persona no puede operar nada detrás del gate mientras la condición real no se resuelva. Distinto de `#pwa-banner`/`#notif-banner` (arriba en este archivo, descartables vía `localStorage`) — estos 2 gates nuevos son la variante OBLIGATORIA, sin forma de descartarlos salvo cumpliendo la condición real. **Instalación directa dentro del gate** (re-ajuste, pedido explícito — "usar el prompt nativo en vez de solo instrucciones manuales"): `#pwa-gate` ahora muestra `#pwa-install-btn` ("Instalar app", dispara `_pwaInstalarDirecto()`) en vez de `#pwa-pasos-manuales` (los 3 pasos de siempre) cuando el navegador ya entregó `beforeinstallprompt` — reusa `_deferredPrompt` (variable YA existente, capturada por el listener original de este archivo para el banner descartable/`pwaInstalar()`) en vez de una 2da variable propia, con un 2do listener liviano (`_pwaActualizarBotonesGate()`) que solo refresca cuál de los 2 bloques mostrar — cubre el caso real de que el evento llegue DESPUÉS de que `_verificarPwa()` ya mostró el gate (el navegador puede tardar unos segundos en decidir que la PWA es instalable). Los pasos manuales quedan como fallback real para navegadores/casos donde el evento nunca llega. Aceptar el prompt nativo (`result.outcome === 'accepted'`) cierra el gate entero de inmediato, sin esperar a que `display-mode` cambie a `standalone` (eso solo pasa en la siguiente carga de página). **Extendido a iOS + detección de navegador incompatible** (re-ajuste, pedido explícito): `_verificarPwa()` ahora también bloquea iPhone/iPad/iPod reales (no solo Android) — `esStandalone()` (ya existía, sin cambios) cubre las 2 formas de detectar "ya instalada" (`display-mode:standalone` en Android/Chrome, `navigator.standalone===true` en iOS/Safari), así que no hizo falta reimplementar ese chequeo. 3 secciones de contenido mutuamente excluyentes dentro de la misma card (`#pwa-instrucciones-android`/`#pwa-instrucciones-ios`/`#pwa-instrucciones-browser`, alternadas por `display`): Android sigue con botón directo/pasos manuales (ver arriba); iOS SIEMPRE son los 3 pasos manuales de Safari ("Compartir → Agregar a pantalla de inicio") — no existe ningún equivalente a `beforeinstallprompt` en Safari/WebKit, Apple nunca lo expuso; `_pwaBrowserCompatible()` (nueva) detecta un navegador que NO puede instalar nada (Chrome/Edge/Opera/Samsung Internet en vez de Chrome real en Android, o cualquier cosa que no sea Safari real en iOS — típicamente el in-app browser de Instagram/Facebook, o Firefox en Android) y en ese caso ninguna de las 2 secciones de arriba sirve (esos menús no existen ahí) — `#pwa-instrucciones-browser` con un mensaje corto ("Para instalar Pivot abre esta página en Chrome/Safari", texto/ícono seteados en vivo según la plataforma detectada) + botón "Copiar enlace" (`navigator.clipboard.writeText(location.href)`) para que la persona pueda pegar la URL en un navegador real. **Detección de "ya instalada" + confirmación al aceptar** (re-ajuste, pedido explícito): del lado Android, si `beforeinstallprompt` no llegó 2.5s después de mostrar el gate (`setTimeout`, solo si `!_deferredPrompt` en ese momento), se asume que el navegador no va a dispararlo — típicamente porque la PWA YA está instalada (algunos Chrome no re-disparan el evento en ese caso) o el navegador no la soporta — y `_pwaMostrarYaInstalada()` (nueva, comparte DOM entre los 2 call sites) reemplaza `#pwa-instrucciones-android` por `#pwa-ya-instalada` ("La app ya está instalada..."). A propósito SOLO del lado Android — Safari/iOS jamás dispara este evento esté instalada o no, así que su ausencia ahí no es señal de nada, los pasos manuales de iOS ya cubren los 2 casos sin distinguir. `_pwaInstalarDirecto()` ya NO cierra el gate al aceptar el prompt nativo — lo deja abierto con el mismo `#pwa-ya-instalada` pero texto de "recién instalada" (el navegador tarda unos segundos en pasar a `display-mode:standalone` DESPUÉS del accept, cerrar de inmediato dejaba a la persona de vuelta en la app sin la PWA todavía lista). `_pwaInstaladoAhora` (flag módulo, seteado en ese accept) evita que el timeout de 2.5s pendiente pise ese mensaje si vence después.

**Ajustes de notificaciones bloqueadas** (`#notif-gate-denegado`, re-ajuste, pedido explícito) — de un párrafo suelto a 3 pasos visuales (ícono+texto, mismo componente `.notif-gate-paso` que `.pwa-gate-paso`) + botón "Ir a ajustes de notificaciones" (`_notifAbrirAjustesChrome()`, js/pwa.js): en Android intenta un intent especial de Chrome (`intent://settings/content/siteDetails#...`) directo a la pantalla de permisos del sitio, y SIEMPRE (no solo si el intent falla — no hay forma de detectar eso desde JS) copia la URL al portapapeles como respaldo, con un toast; en iOS (sin ningún intent equivalente posible desde la web) solo muestra el toast de instrucciones para Ajustes del sistema.

**Fallback nativo si OneSignal no carga** (`_activarNotificaciones()`, re-ajuste, pedido explícito) — bug real de fondo señalado por Victor: esta función SOLO funcionaba dentro de `OneSignalDeferred.push(...)` -- si el SDK de OneSignal nunca termina de cargar (bloqueado por un ad-blocker, su CDN está en la mayoría de esas listas, o sin red hacia ese dominio), ese callback JAMÁS se ejecuta, y como `#notif-gate` no tiene ningún botón de cierre propio, la persona quedaba encerrada para siempre detrás del gate con un botón que no hacía nada. Fix: timeout de 4s (`yaResolvio`, flag compartido para que el timeout y la resolución real de OneSignal -- si llega tarde -- no se pisen entre sí) que cae a `Notification.requestPermission()` NATIVO del navegador si OneSignal no respondió a tiempo (mismo camino si `OneSignal.Notifications.requestPermission()` en sí rechaza/falla) — no dispara `PushSubscription.optIn()` en ese caso (OneSignal no se entera de este dispositivo hasta que su SDK cargue de verdad en una visita futura, `vincularPush()` ya reintenta eso en cada login), pero evita el encierro. Botón con estado "Activando..."/`disabled` mientras se resuelve cualquiera de los 2 caminos (`_notifActivarBtn()`/`_notifActivarTerminar()`/`_notifActivarMostrarDenegado()`, helpers nuevos compartidos entre el camino OneSignal y el nativo, sin duplicar la manipulación del DOM).
│   ├── auth.js                  Google Sign-In, PIN, restaurar sesión, window.onload. **Soporte offline de Ajustes** (ver MANIFEST.md/CHANGELOG.md — "soporte offline para Ajustes, Equipo y Tareas"): `restaurarSesion()` persiste `E.datos` en `localStorage` (`'edat'`) apenas resuelve con éxito; sin conexión, `window.onload` sirve ese último `E.datos` cacheado directo (sin llamar al backend) y navega igual a la pantalla de aterrizaje — mismo criterio de "servir el último snapshot en vez de colgarse esperando red" que `js/offline.js` (Eventos), sin usar IndexedDB ni cola de sincronización (Ajustes/Equipo/Tareas no necesitan encolar escrituras offline, ver `js/perfil.js`/`js/equipo.js`/`js/tareas.js`). **Identidad OneSignal por usuario**: `vincularPush(nombre)` (js/pwa.js) ya llamaba `OneSignal.login(nombre)` en los 4 caminos de login/restauración de sesión regular (Google fresco, PIN, restaurar sesión, alta vía registro-express) desde antes de esta tanda — sin cambios, es lo que permite que `sendPush({include_aliases:{external_id:[username]}})` (Edge Function) le llegue a la cuenta correcta; cuentas admin usan `'admin_'+email` como identidad (mismos 3 call sites de siempre). **RSVP directo desde push** (`?rsvp=<idEvento>&estado=<estado>`, generado por `pushEventoCreado()`/Edge Function como `web_buttons` de la notification de evento nuevo): leído en `window.onload`, poll acotado (24 intentos × 350ms) hasta que `_EV_EVENTOS` tenga ese evento cargado (prerequisito real de que la sesión ya esté resuelta), y entonces llama `irEventos()` + `_evMarcarAsistencia(id, estado)` (js/eventos.js) tal cual — misma función que usa el flujo manual, sin lógica de cuota/gracia duplicada acá. **Bug real corregido -- "login trabado en 'Verificando cuenta' investigado, encontré condición duplicada"**: `window.onload` tenía `if (typeof _verificarNotificaciones === 'function') if (typeof _verificarNotificaciones === 'function') _verificarNotificaciones();` -- condición copiada 2 veces, inofensiva en sí (evaluaba lo mismo 2 veces, igual llamaba la función 1 sola vez) pero limpiada -- una sola condición ahora. **Importante -- corrección de un reporte previo de esta misma investigación**: `onGoogleCredentialUsuario()` (arriba en este archivo) SÍ tiene un `onError` real en su `apiPost({action:'loginGoogle',...})` (3er argumento, `ocultarCargando()` + mensaje visible en `#err-google-login` con el `e.message` real, 5s) -- un reporte anterior había concluido que faltaba, por leer un `git log -p` truncado sin llegar al cierre real de la función (~90 líneas después del `apiPost(...)`); no era cierto, no hizo falta ningún cambio ahí. La causa de fondo del login trabado en Android sigue sin confirmar -- no se encontró ninguna edición de esta sesión que toque este flujo, y hay logins exitosos recientes en la tabla `sessions` real (no es un apagón total).
│   ├── foto.js                  Cambio de foto de perfil (Cropper.js + subida) — compartido
│   └── color-enfasis.js         Deriva en runtime ~40 variables con tinte de marca de colors.css (compartido) a partir de 2 hex FIJOS (`_CE_BRAND_LIGHT`/`_CE_BRAND_DARK`, uno por modo — rebrand a Pivot, ver MANIFEST.md sección 2 "CSS — principios") — ya NO acepta un color arbitrario ni lee ninguna preferencia guardada, el selector de color de Mi Liga se eliminó por completo. `--bg`/`--text`/`--muted`/`--border-light`/`--card-bg` DEJARON de recalcularse acá (son literales fijos en colors.css ahora) — el resto de la familia --brand-*/superficies/sombras/texto secundario sigue derivando igual que antes (mismo método de desplazamiento relativo de matiz), solo que ahora con 2 anclas fijas en vez de una configurable. `_CE_BG_DARK` (nuevo, literal `#0D0D0D`) reemplaza al `bgHex` derivado-con-tinte que antes alimentaba `--dk-overlay-95/-97` — el fondo oscuro nuevo es neutro, no tiene tinte de marca que preservar ahí. **Fix real — "status bar de la PWA instalada en Android siempre blanca en dark mode"**: `_ceAplicarPaleta()` ya sincronizaba `content` en los 2 `meta[name="theme-color"]` con media query (`light`/`dark`, ver index.html) en cada corrida — pero Android ignora esos 2 metas una vez la PWA está instalada (solo lee `theme_color` de `manifest.json`, estático, y un eventual meta SIN atributo `media`). Ahora la función también busca/crea (una sola vez, `document.head.appendChild` si no existe) un 3er `meta[name="theme-color"]` sin `media` y le setea `#0D0D0D`/`#FFFFFF` según `oscuro` — sin listener nuevo, reusa el mismo `matchMedia('(prefers-color-scheme: dark)').addEventListener('change', _ceAplicarPaleta)` que ya existía al final del archivo (llama a la función completa, este meta incluido). `manifest.json` → `theme_color` pasa de `#FFFFFF` a `#111111` (fallback neutro oscuro para el primer render antes de que corra este JS, en vez de blanco). **Bug real corregido -- "status bar de Android PWA instalada sigue oscura en light mode"**: ese `#111111` fijo terminó siendo la causa -- Android, para una PWA YA INSTALADA, toma el color de la barra de estado nativa del `theme_color` ESTÁTICO de `manifest.json`, no de los meta tags dinámicos de arriba (confirmado sin bug en `_ceAplicarPaleta()`, sin tocar ese archivo) -- con `#111111` fijo, la barra quedaba pegada en oscuro sin importar el tema real del sistema (coincidía "por casualidad" en dark, nunca cambiaba en light). Corregido a `#FFFFFF` (default correcto para light; dark sigue cubierto en runtime por el JS de arriba, que funciona una vez que el valor de arranque del manifest es el correcto). `OneSignalSDKWorker.js` -- `CACHE` de `mirlxs-v4` a `mirlxs-v5`, para que `activate()` purgue la caché vieja y Android no siga sirviendo el `manifest.json` viejo desde el fallback de caché. **Re-ajuste posterior -- "el JS de theme-color sigue en conflicto, dejar que los meta tags con media query lo manejen solos"**: `_ceAplicarPaleta()` deja de tocar `meta[name="theme-color"]` del todo -- se elimina el bloque que sincronizaba `content` en los 2 `meta[...][media]` de `index.html` Y el que creaba/actualizaba un 3er meta SIN `media` para la PWA instalada (el de la ronda anterior, arriba en este mismo bullet) -- ninguno de los 2 mecanismos resolvía el problema real (Android depende del `theme_color` ESTÁTICO de `manifest.json` para la barra de estado nativa de una PWA instalada, no de meta tags dinámicos vía JS). Los 2 `meta[theme-color][media]` de `index.html` quedan intactos y son ahora los ÚNICOS responsables de esto, sin ningún JS de por medio -- el resto de `_ceAplicarPaleta()` (las ~40 variables `--brand-*`/superficies/sombras) sigue sin cambios, igual que el listener de `matchMedia` al final del archivo (sigue haciendo falta para ESAS variables, no para theme-color). `manifest.json` sin cambios en esta ronda (`#FFFFFF`, ya corregido antes). `OneSignalSDKWorker.js` -- `CACHE` de `mirlxs-v5` a `mirlxs-v6`. **Re-ajuste posterior -- "problema de timing, JS externo llega tarde para Android PWA"**: los 2 `meta[theme-color][media]` de arriba (la solución "solo CSS" de la ronda anterior) se reemplazan por un ÚNICO `<meta name="theme-color" id="tc-meta">` + un `<script>` INLINE (no externo/diferido) inmediatamente después, en el `<head>`, antes de cualquier otro recurso -- corre sincrónico en el primer parseo del documento, seteando `content` según `matchMedia('(prefers-color-scheme: dark)').matches` YA en el primer paint, en vez de depender de que `js/color-enfasis.js` (script externo, carga después) llegue a tiempo. Mismo mecanismo de fondo que el meta-sin-`media` que se había agregado y sacado en las 2 rondas anteriores (un solo meta, actualizado por JS ante `matchMedia`) -- la diferencia real de esta vuelta es SOLO el timing (inline+sincrónico vs. script externo), no la estrategia. `color-enfasis.js` sigue sin ninguna lógica de `theme-color` (confirmado, sin tocar). `OneSignalSDKWorker.js` -- `CACHE` de `mirlxs-v6` a `mirlxs-v7`. **Investigación posterior -- "¿algo fuerza light-only vía color-scheme?"**: se buscó `<meta name="color-scheme">` (ningún HTML del proyecto lo tiene) y la propiedad CSS `color-scheme` fuera de `prefers-color-scheme` (que es solo el nombre de la media query, no la propiedad) -- único hit real: `select { color-scheme: dark; }` en `css/ui.css:225`, scopeada a `<select>` y ya DENTRO de `@media (prefers-color-scheme: dark)` -- no es `light`, no es global, no puede alterar el resultado de `matchMedia`. Sin causa encontrada ahí -- `OneSignalSDKWorker.js` bump a `mirlxs-v8` (siguiente intento: si sigue en blanco después de esto con caché limpia, la causa no es de este repo -- ver "Cambios recientes" para las rondas previas ya descartadas: inline script, manifest.json, meta tags, color-enfasis.js, color-scheme). **Dark mode deshabilitado TEMPORALMENTE** (pedido explícito, tras agotar el diagnóstico de arriba sin encontrar causa -- "la app siempre se muestra en light mode independientemente del modo del sistema"): `index.html` -- se saca el `<meta name="theme-color" id="tc-meta">` + su script inline de detección (matchMedia/`upd()`) Y el banner de debug temporal (`#debug-tc`), reemplazados por `<meta name="color-scheme" content="light">` (le dice al navegador que la página SOLO soporta claro -- esto hace que `prefers-color-scheme` resuelva a `light` para CUALQUIER media query de esta página, no solo para el status bar) + un `<meta name="theme-color" content="#FFFFFF">` simple, sin `id`, ya sin JS que lo toque. `css/colors.css` -- el bloque `@media (prefers-color-scheme: dark)` NO se borra ni se comenta con `/* */` (tiene decenas de comentarios propios anidados adentro que romperían un wrap simple, cerrando el comentario de afuera en el primer `*/` interno) -- en cambio, la condición se le suma `and (min-width: 99999px)` (nunca va a ser cierto), dejando touch/contenido intacto para reactivar fácil sacando esa cláusula. Doble barrera a propósito: el `color-scheme` meta ya alcanzaría solo, el bloque de colors.css queda inerte como refuerzo. `manifest.json` sin cambios (`theme_color:"#FFFFFF"`, ya correcto). **Pulido posterior de la animación de colapso** (pedido explícito, "sigue cortándose en Android Chrome") -- `.eq-header-panel`/`.eq-misstats-panel-inner`/`.eq-busqueda-panel-inner` (css/equipo.css) y `.ev-header-burbuja`/`.ev-header-burbuja > *` (css/eventos.css): duración 260ms->350ms, easing `cubic-bezier(0.4,0,0.2,1)`->`cubic-bezier(0.25,0.46,0.45,0.94)` (entrada más suave), + `transform:translateZ(0)` PERMANENTE (antes solo existía `will-change` como clase temporal) para promover estos elementos a su propia capa de compositing desde que la página carga, no recién al arrancar la transición. Como un `style.transform` inline pisa CUALQUIER `transform` de la clase, `_eqAnimarPanel()`/`_evAnimarPanel()` y los 2 handlers de `touchmove` (drag-to-dismiss) ahora concatenan `' translateZ(0)'` a cada `translateY(...)` que escriben, para que la capa de compositing sobreviva durante la animación real (que es cuando más importa) en vez de perderse apenas JS toca `style.transform`. `_eqInicializarColapsoStatsPorScroll()` -- umbral de 60px a 80px (Eventos no tiene un umbral en px equivalente, su colapso por drag usa una FRACCIÓN del alto arrastrado -- `_EV_CAL_DRAG_UMBRAL_FRACCION`, sin cambios, pedido explícito no lo mencionaba). **Nav superior más grande** (`css/nav.css`, pedido explícito, "a la altura visual de la bottom nav"): `.app-nav` -- `padding` vertical 33px->37px, `min-height` 67px->71px (2da ronda de aumento, ver bullet anterior de esta sección para la 1ra). `.app-nav-icon-btn .material-symbols-outlined` 22px->`1.5rem` (24px). `.app-nav-back .material-symbols-outlined` 1.4rem->`1.5rem` (mismo tamaño que los otros íconos, pedido explícito). `.app-nav-title` (el título de sección uppercase de las pantallas internas) -- `font-size` 0.86rem->1.1rem, `font-weight` 800->600. **Bug real corregido -- "dark mode deshabilitado, los skeletons seguían oscuros"**: la ronda de "deshabilitar dark mode" solo neutralizó el `@media (prefers-color-scheme:dark)` de `colors.css` -- hay 11 bloques MÁS de esa misma media query repartidos en `admin.css`/`eventos.css` (x3)/`estilos.css`/`global.css`/`home.css`/`login.css`/`perfil.css`/`reservas.css`/`ui.css`, ninguno tocado hasta ahora, todos siguen aplicando de verdad si el sistema real está en dark mode -- `<meta name="color-scheme" content="light">` NO cambia lo que esa media query evalúa (afecta el theming de controles nativos del navegador, no `prefers-color-scheme`). De esos 11, solo 2 tenían contenido de skeleton real (pedido explícito, alcance acotado a eso): `css/estilos.css` (`.loading-card`/`.loading-card p`, comentadas -- `.app-toast.app-toast-error` del mismo bloque queda sin tocar) y `css/global.css` (`#gsignin-skeleton`/`#gsignin-skeleton-main`, el skeleton del botón de Google Sign-In -- bloque completo comentado, no tenía nada más adentro). Las redefiniciones de `--skeleton-base`/`--skeleton-shine` en `colors.css` (línea ~531) ya estaban neutralizadas por el fix anterior (viven DENTRO de ESE bloque, el único que sí se había tocado).

**Dark mode 100% deshabilitado** (pedido explícito, ronda siguiente a la anterior): los 9 bloques `@media (prefers-color-scheme: dark)` que quedaban pendientes -- `admin.css`, `eventos.css` (x3), `home.css`, `login.css`, `perfil.css`, `reservas.css`, `ui.css` -- se neutralizan con el MISMO truco ya usado en `colors.css`/`estilos.css`/`global.css` (`and (min-width: 99999px)`, condición que nunca se cumple, contenido intacto para reactivar fácil sacando esa cláusula) en vez de comentar/borrar -- evita tener que auditar cada bloque por comentarios `/* */` propios anidados que romperían un wrap simple (mismo motivo documentado para `colors.css`). Con esto, los 12 bloques dark de toda la app (los 3 ya tocados en rondas previas + estos 9) quedan inertes.

**Nav "Ajustes" -> "Mi perfil"** (pedido explícito, `js/ui.js`): `APP_BOTTOM_NAV_ITEMS` -- `texto:'Ajustes'` -> `'Mi perfil'` (`id`/`pantalla` sin cambios, siguen siendo `'ajustes'`/`'s-datos'` internamente). Ícono: si `E.datos.fotoPerfil` existe, `_actualizarBottomNav()` reemplaza el `span` del ícono `settings` por un `<div class="avatar-pill avatar-pill--xs app-bottom-nav-avatar">` (mismo tamaño que los demás íconos de la nav, 26px) hidratado con el helper YA compartido `_avatarSetFotoOInicial()` (el mismo que usa el resto de la app para cualquier avatar con foto) -- sin foto, sigue el gear de siempre (fallback EXPLÍCITO al ícono, no al fallback de inicial que usa ese helper en el resto de la app). Placeholder del buscador de esa pantalla (`index.html`, `#aj-search-input`) -- "Buscar en Ajustes" -> "Buscar en Mi perfil".

**Tours guiados de Equipo y Mi Perfil** (pedido explícito, "idéntico al tour de Eventos -- mismos estilos, misma lógica, mismo componente"): ambos reusan LITERAL el motor genérico `_evTourIniciarConPasos()`/js/eventos.js (mismo `#ev-tour-tooltip`/`#ev-tour-overlay`/halo que ya comparten los 3 tours de Eventos -- el prefijo `ev` es histórico, el componente ya es compartido por cualquier sección desde que se generalizó, ver el comentario grande de esa función) -- CERO CSS/HTML nuevo, solo un array de pasos + una función disparadora propia por sección, mismo patrón mínimo que `_evTourPrimeraReserva()` (2 pasos, sin split admin/usuario) en vez del más elaborado `_evTourIniciarSiCorresponde()` (que si tiene esa distinción). `_eqTourIniciarSiCorresponde()` (js/equipo.js, `_EQ_TOUR_PASOS`, clave `eq_tour_visto`) -- 5 pasos: `#eq-misstats-toggle-btn` (Mis estadísticas), `#eq-busqueda-toggle-btn` (buscar/filtrar), `.eq-grupo-header` (colapsar secciones), `.eq-fav-btn` (favoritos), `.eq-miembro-fila` (abrir detalle de una persona) -- disparado desde `_eqInit()`, después del primer render real del roster, 650ms de margen (mismo criterio que el tour de bienvenida de Eventos, deja asentar el layout antes de medir geometría). `_ajTourIniciarSiCorresponde()` (js/perfil.js, `_AJ_TOUR_PASOS`, clave `aj_tour_visto`) -- 4 pasos: `#aj-search-wrap` (buscador), `.aj-row[data-aj-key="perfil"]` (foto/nombre derby), `.aj-row[data-aj-key="equip"]` (equipamiento), `.aj-row[data-aj-key="emerg"]` (más ajustes, fila de contactos de emergencia) -- disparado desde `irEditarDatos()`, solo si `!sinNavegar` (esa función también se llama para repoblar campos en segundo plano sin navegar de verdad, un tour ahí no tendría sentido -- nunca se llegó a ver `#s-datos`). Ninguno de los 2 necesita guardas propias por selector -- `_evTourMostrarPaso()` (el motor) ya saltea solo cualquier paso cuyo selector no resuelva a un elemento visible.

**Colapso por scroll de "Mis estadísticas" -- ahora instantáneo, sin transición** (pedido explícito, "no se ve bien en Android Chrome"): `_eqCerrarPanel(tag, instant)` suma un 2do parámetro -- `instant:true` (usado SOLO por `_eqInicializarColapsoStatsPorScroll()`, el listener de scroll) hace `transition:none` real en el wrapper Y en los hijos, con un reflow síncrono (`void panel.offsetHeight`) en el medio antes de restaurar la transición -- mismo idioma que ya usa `_evCerrarPanel(tag, instant)`/js/eventos.js para el cierre instantáneo del panel de búsqueda. El toggle manual del chevron (`_eqTogglePanel()`) no manda ese 2do argumento -- sigue el camino animado de 350ms de siempre, sin cambios.

**Estadísticas de Equipo -- panel invisible al abrir, fix definitivo con `height:auto`** (pedido explícito, "el doble rAF de la ronda anterior no alcanza -- el problema es que el ancestro puede seguir `display:none` en el momento de medir, no timing de layout"): reemplaza el enfoque de medir `scrollHeight` por un estado "expandido de verdad" real en CSS, `.eq-header-panel.eq-panel-auto { height:auto; overflow:visible; }` (recalculado por el navegador en cada layout, nunca depende de una medición vieja). `_eqAbrirPanel(tag, instantAuto)` -- 2do parámetro nuevo: `instantAuto:true` (usado SOLO por el auto-open inicial de `_eqRenderMisEstadisticas()`, sin el doble rAF de la ronda anterior) salta DIRECTO a `eq-panel-auto` sin medir ni transicionar; el toggle manual del chevron sigue midiendo `scrollHeight` y transicionando como siempre, pero AL TERMINAR (`transitionend`, `_eqAnimarPanel(..., volverseAuto:true)`) pasa a `eq-panel-auto` también, para quedar flexible si el contenido cambia de tamaño después en vez de congelado en el px que medía en ese momento puntual. El drag-to-dismiss (`_eqInicializarCierrePanelesPorScroll()`) saca `eq-panel-auto` al arrancar un gesto (no se puede arrastrar desde `auto`, necesita un px real de partida) y la vuelve a poner si el drag no llega al umbral (`volverseAuto:true` en su `_eqAnimarPanel()` de "volver a abierto"). `_eqCerrarPanel()` (ambos caminos, animado e instantáneo) saca `eq-panel-auto` ANTES de tocar `height` -- nunca se anima/salta DESDE `auto`, siempre desde un valor real (`scrollHeight`, que mide bien esté el panel en `auto` o en px explícito).

**Dark mode RE-HABILITADO** (pedido explícito, revierte la deshabilitación de varias rondas atrás) -- los 10 bloques `@media (prefers-color-scheme: dark)` neutralizados con `and (min-width: 99999px)` (`admin.css`/`colors.css`/`eventos.css` x3/`home.css`/`login.css`/`perfil.css`/`reservas.css`/`ui.css`) vuelven a su condición original; los 2 bloques con contenido de skeleton comentado a mano (`estilos.css` `.loading-card`/`.loading-card p`, `global.css` `#gsignin-skeleton`/`-main`) se descomentan. `index.html` -- se saca `<meta name="color-scheme" content="light">` (el `<meta name="theme-color">` simple que convivía con él queda igual, sin restaurar ningún mecanismo de detección dinámica -- eso no formaba parte de este pedido). **`manifest.json`** -- `theme_color` de `#FFFFFF` a `#B8000A` (mismo hex que `--brand-dk` en claro, `css/colors.css`). **`colors.css`, bloque dark -- `--brand`/`--brand-dk` unificados con claro** (pedido explícito, "el rojo no cambia entre modos"): de `#FF2020`/`#CC0000` (rojo DISTINTO a propósito para contraste sobre el fondo casi negro, ver cabecera del archivo -- decisión del rebrand, revertida acá) a `#E8000D`/`#B8000A`, idénticos a los de `:root` claro. **Sin tocar, fuera del alcance puntual de este pedido** (queda pendiente si se quiere unificación completa): la familia `rgba(255,32,32,...)` de ese mismo bloque (`--brand-zero`/`-subtle`/`-hover`/etc., ~18 variables) sigue con el rgb VIEJO, desalineada del `--brand` sólido ya actualizado; y `_CE_BRAND_DARK` (`js/color-enfasis.js`) -- ese script re-deriva ~40 variables "(runtime)" en cada carga de página a partir de SU PROPIA constante hex, pisando el fallback estático de `colors.css` apenas corre, así que la mayoría de las variables de marca van a seguir divergiendo entre modos después del primer paint hasta que esa constante también se unifique. `OneSignalSDKWorker.js` -- `CACHE` de `mirlxs-v8` a `mirlxs-v9`.

**Ronda siguiente -- verificado contra el estado real antes de aplicar nada** (varios puntos del pedido ya estaban resueltos por la ronda anterior, sin marcador `/* DARK MODE DISABLED */` literal en ningún lado como asumía el pedido -- confirmado con grep antes de tocar código): dark mode (los 12 bloques `@media` + `color-scheme` meta + `theme_color`/`background_color` de `manifest.json`) y el bump de `mirlxs-v8`->`v9` ya estaban hechos en el commit anterior (`ede8be1`) -- sin cambios de nuevo ahí. El viewport meta (`index.html`) ya tenía `viewport-fit=cover`, con params adicionales (`maximum-scale`/`minimum-scale`/`user-scalable=no`, bloquean pinch-zoom a propósito) que el pedido no mencionaba sacar -- no se tocaron, cambiar el string a la versión más corta pedida literalmente hubiera sido una regresión real de UX no solicitada explícitamente.

**Bug real corregido -- "línea blanca arriba"**: `.app-nav-fixed` (nav superior FIJA de Home, `css/nav.css`) nunca compensaba `env(safe-area-inset-top)` -- a diferencia de `.app-nav-sticky` (flujo normal de documento, ya cubierta por el `padding-top` de `body`, css/global.css) y de `body` en sí, `position:fixed` ignora por completo el padding de sus ancestros. Suma `padding-top: calc(37px + env(safe-area-inset-top, 0px))`, pisando el lado `top` del `padding:37px 16px` de `.app-nav` (misma especificidad, gana por venir después en la cascada) -- el spacer que compensa esta nav en el flujo (`_initHomeNav()`) mide `offsetHeight` en vivo, se adapta solo al alto nuevo. `html`/`body` `margin:0;padding:0;` -- ya cubierto por el reset universal `*, *::before, *::after` (css/global.css), sin necesidad de una regla dedicada.

**Bug real corregido -- "el tour de Equipo no reintenta al renavegar"**: a diferencia de Eventos (`_evActualizarTopBarModo()`, sin gate, corre en cada `irEventos()`) y Mi Perfil (`_ajTourIniciarSiCorresponde()`, corre en cada `irEditarDatos()` real), el chequeo de Equipo vivía SOLO adentro de `_eqInit()` -- función que corre una única vez por sesión (`_eqYaInicializado`). Si la primera visita se abandonaba antes de que el tour se marcara visto (`eq_tour_visto`), reentrar a Equipo más tarde en la MISMA sesión nunca reintentaba. `irEquipo()` ahora llama `_eqTourIniciarSiCorresponde()` en el `else` (roster ya en el DOM de la visita anterior, sin esperar fetch) -- la primera visita sigue disparándolo desde adentro de `_eqInit()`, recién cuando el roster real ya está pintado.

**Bug real corregido -- "flash de foto en nav inferior"**: `_actualizarBottomNav()` (`js/ui.js`) corre en CADA navegación y reconstruye `nav.innerHTML` completo -- el `<div class="app-bottom-nav-avatar">` se destruía y recreaba en cada cambio de tab, así que el de-dupe propio de `_avatarSetFotoOInicial()` (`dataset.avatarClave`, vive en el elemento) nunca llegaba a activarse: mostraba el skeleton + reprecargaba la imagen de cero en cada tab. `_bottomNavAvatarCache` (variable de módulo, sobrevive entre renders) guarda el HTML ya resuelto la primera vez (capturado vía `MutationObserver` cuando el skeleton desaparece) -- en renders siguientes con la misma foto, se reinyecta directo sin precarga ni parpadeo.

**Nav inferior reordenada** (pedido explícito, sin tocar íconos/labels): `APP_BOTTOM_NAV_ITEMS` (`js/ui.js`) pasa de Eventos/Mi perfil/Tareas/Equipo/Mi Liga a Eventos/Equipo/Tareas/Mi perfil/Mi Liga -- el array define el orden visual real, `_actualizarBottomNav()` lo itera tal cual.

**Tasks 9/10 (colapso de "Mis Estadísticas"/calendario) -- verificadas, ya correctas, sin cambios**: no hay ningún `display:none` en la lógica de colapso de ninguno de los 2 paneles (confirmado con grep). El cierre manual (chevron) y la apertura ya comparten LITERALMENTE la misma regla CSS de transición (`.eq-header-panel`/`.ev-header-burbuja { transition: height 350ms cubic-bezier(0.25,0.46,0.45,0.94); }`, css/equipo.css y css/eventos.css respectivamente) -- mismos 350ms y mismo easing en ambas direcciones y en las 2 secciones, ya "unificados" desde la ronda de pulido de performance. Único punto real de discusión: el pedido sugiere el easing simple `ease` en vez del `cubic-bezier(...)` custom actual (elegido a propósito en esa ronda, "entrada más suave") -- no se cambió sin confirmación, ver mensaje a Victor. **Bug real corregido -- "chevron dice expandido pero el panel de estadísticas no se ve"** (`_eqRenderMisEstadisticas()`): el chevron/`.activo` y el `height` del panel ya se escribían juntos, en la MISMA llamada (`_eqAbrirPanel()`) -- el desync no era por escribirlos separado, sino porque esa llamada podía correr con `#s-equipo` todavía `display:none` (ej. un re-render disparado por un fetch en segundo plano mientras otra pestaña está activa), y `panel.scrollHeight` con un ancestro oculto mide `0` -- el chevron quedaba rotado a "expandido" mientras el panel fijaba `height:0px` real. Fix: el auto-open inicial ahora se difiere con doble `requestAnimationFrame` (mismo patrón ya usado en este archivo para "medir recién cuando el layout asentó", ver `_eqCerrarPanel()`) -- `_eqAbrirPanel()` mide `scrollHeight` de nuevo en ese momento posterior, nunca reusa un valor viejo/potencialmente en 0.

**Bug real corregido -- "búsqueda rota en Eventos"** (`_evAbrirPanel()`/`_evCerrarPanel()`, `js/eventos.js`): estas 2 funciones genéricas (usadas SOLO por el panel `'busqueda'`, `#ev-busqueda-panel`, misma clase `.ev-header-burbuja` que el panel de mes/calendario) habían quedado FUERA de la ronda de perf que migró `.ev-header-burbuja` de `max-height` a `height` (esa ronda tocó a propósito solo `_evAbrirCalendario()`/`_evCerrarCalendario()`) -- seguían escribiendo `el.style.maxHeight`, propiedad que la clase ya no anima ni lee para nada, así que el panel de búsqueda no abría/cerraba de verdad (la lupa activaba la clase `abierta` y el botón, pero el alto real nunca cambiaba). Fix: mismo nombre de propiedad (`el.style.height`) en los 4 lugares donde escribían `maxHeight`, sin agregar `translateY`/GPU-layer nuevo acá (fuera de alcance del pedido, "restaurar el comportamiento original"). Verificado con grep que `js/equipo.js` no tiene el mismo bug (`_eqAbrirPanel`/`_eqCerrarPanel` ya usaban `height` desde la ronda de `height:auto`, sin ningún `.style.maxHeight` residual ligado a sus paneles) -- sin cambios ahí.

**Auditoría de consistencia del top nav entre secciones -- un solo hallazgo real**: `.app-nav`/`.app-nav-icon-btn .material-symbols-outlined`/`.app-nav-title`/`.app-nav-back .material-symbols-outlined` (`css/nav.css`) ya eran la única fuente de alto/padding/tamaños de ícono, sin overrides dispersos por sección (confirmado con grep, sin cambios ahí). Único caso real: `.ev-detalle-tipo` (`css/eventos.css`, título uppercase de la fila de nav del detalle de evento -- copia manual de `.app-nav-title`, no reutiliza la clase por el layout distinto de `#ev-detalle-nav-row`) había quedado en `font-size:0.86rem; font-weight:800` -- el valor VIEJO de `.app-nav-title` de antes de la ronda "top nav más grande" (que lo subió a `1.1rem`/`600`) -- al ser una copia a mano nunca siguió ese cambio. Corregido a `1.1rem`/`600`, igual que el resto. `#eventos-nav .app-nav-section`/`#s4-nav .app-nav-section` (`css/nav.css`) siguen en `0.86rem`/`800` a propósito -- gobierna el label del estado "compacto/scrolleado" de la nav de Home, un momento de UI distinto al top nav normal, fuera del alcance de este pedido.

**Bug real corregido -- "línea blanca entre status bar y nav en dark mode"**: `--bg` en el bloque dark de `colors.css` ya era el oscuro correcto (`#0D0D0D`, confirmado, sin bug ahí) y no había ningún background hardcodeado en blanco fuera de media queries (confirmado con grep en `global.css`/`estilos.css` -- los únicos hits de `background...white` son `var(--white)` en los thumbs de `.toggle-slider`/`.toggle-btn`, sin relación). La causa real: `<html>` nunca tuvo `background-color` propio, solo `body` (`background: var(--bg)`, `css/global.css`) -- en el rubber-band/overscroll superior de una PWA instalada y en la franja de safe-area, el navegador pinta el fondo de `<html>`, no el de `<body>`, así que esa franja quedaba con el blanco por default del user-agent encima de la status bar, en cualquier modo de color pero visible solo en dark. Fix: `html { background-color: var(--bg); }` (`css/global.css`, junto al resto del reset universal).

**Aumentar contraste en dark mode** (pedido explícito #13, audit WCAG AA, `css/colors.css` bloque dark) -- los nombres pedidos (`--text-secondary`/`--text-muted`) no existen tal cual, mapeados por USO REAL (grep en `css/*.css`) en vez de por parecido de nombre: `--muted` (137 usos, el texto secundario/subtítulo real de casi toda la app, NO `--hint`) y `--text-2` (14 usos, tono secundario alternativo) -- ambos ya cumplían AA (5.5:1 y 8.4:1 contra `--bg`) pero se suben igual, pedido explícito, a `#BBBBBB` (10.1:1). `--hint` (2 usos reales: `::placeholder`/`.fnac-placeholder`, `css/global.css` -- este SÍ es el "texto tenue/placeholder" que describe el pedido) estaba en `#555555`, 2.6:1 -- bug real de contraste no detectado hasta este audit, por debajo de AA incluso para texto grande -- sube al piso pedido, `#888888` (5.5:1). `--text` (#F2F2F2, 17.4:1) ya cumplía de sobra, sin cambios. `--surface-2` (71 usos, la superficie de card REAL dominante, no `--card-bg`, 8 usos) pasa de `rgba(255,255,255,0.04)` a `0.13` -- con el valor viejo, blendeada sobre `--bg` (#0D0D0D) daba ~9% de luminosidad HSL contra el 5.1% de `--bg` (~4 puntos de diferencia, card casi invisible); con 0.13 sube a ~17.6% (~12.5 puntos, dentro del rango pedido de 10-15) -- `--surface`/`--surface-3` suben en la misma proporción relativa (0.06→0.16, 0.025→0.08). `--card-bg` (`#1A1A1A`→`#2D2D2D`) igualado al mismo tono que el nuevo `--surface-2` para que ambos caminos de "fondo de card" den el mismo resultado visual. `--border`/`--border-2` (rgba(255,32,32,...), 0.18/0.1→0.30/0.18) y `--border-light` (76 usos, el separador genérico REAL más usado, no `--border`) 0.09→0.16 -- en los 3 casos el rgb base queda sin tocar a propósito (unificar el matiz rojo viejo con el `--brand` sólido actual sigue pendiente, ver bullet "Dark mode RE-HABILITADO" más arriba) y ninguno llega a un 3:1 pleno tipo WCAG non-text (un tinte tan oscuro necesitaría casi opacidad total para eso, dejaría de verse como un divisor sutil) -- mejora real de visibilidad, no cumplimiento estricto. `--disabled-border` queda sin tocar a propósito (un estado disabled debe seguir viéndose apagado).

**Pedido #14 ("línea blanca/discontinuidad entre status bar y top nav", verificado antes de tocar nada, sin cambios de código)**: `.app-nav`/`.app-nav-fixed`/`.app-nav-sticky` (`css/nav.css`) no tienen `box-shadow` ni `border-bottom` en ningún lado -- nada que remover, confirmado leyendo las 3 reglas completas. El fondo del top nav es `var(--bg)` (casi negro), NUNCA `var(--brand)` -- el único uso de `--brand` en `.app-nav*` es en botones/CTAs/estados de foco hijos (`.app-nav-icon-btn-brand`, `.app-nav-cta`, `.app-nav-search:focus-within`), no en el fondo de la barra. Como la condición del pedido ("si el top nav ya usa `--brand` como fondo...") es falsa, no se tocó `--brand` -- forzarlo a `#B8000A` en dark hubiera roto la unificación explícita de una ronda anterior de esta misma sesión ("el rojo no cambia entre modos", `--brand` dark = `#E8000D`, igual al claro) solo para igualarlo al tono que YA tiene `--brand-dk` (`#B8000A`, el mismo hex que `manifest.json→theme_color`, la barra de estado real) -- ese match ya existe, vía `--brand-dk`, no vía `--brand`. Pintar la barra de nav del mismo rojo que la status bar sería un cambio de diseño real (la nav siempre fue neutra a propósito), no un bug -- no se aplicó sin confirmación explícita de ese cambio de diseño.

**Pedido #15 ("cierre por scroll debe animarse siempre, igual que el toggle manual") -- conflicto real con un pedido explícito anterior de esta misma sesión, resuelto con Victor antes de tocar código**: `_eqInicializarColapsoStatsPorScroll()` (`js/equipo.js`) llamaba `_eqCerrarPanel('stats', true)` -- ese `instant:true` viene de un pedido explícito ANTERIOR ("no se ve bien en Android Chrome, que el cierre por scroll sea instantáneo") que este pedido 15 revierte directamente. Consultado -- elección: probar de nuevo animado, pero con la promoción a capa GPU que `_eqAnimarPanel()` sumó en la ronda de perf posterior a aquel fix (`translateZ(0)` en los hijos, no existía cuando se hizo instantáneo la primera vez). Cambio real: `_eqCerrarPanel('stats', true)` → `_eqCerrarPanel('stats')` -- reusa LITERAL el mismo camino animado de 350ms que ya usa el toggle manual del chevron (freeze de `scrollHeight`, doble rAF, `_eqAnimarPanel(...,'0px','translateY(-100%)')`), cero código nuevo. El parámetro `instant` de `_eqCerrarPanel()` queda vivo sin uso real por ahora (documentado en el comentario de la función), por si hace falta volver atrás. Si vuelve a trabarse en Android Chrome, la causa más probable no sería falta de GPU layer (los hijos ya la tienen) sino que animar `height` dispara layout en cada frame mientras el compositor de scroll sigue corriendo en paralelo -- ahí el único fix real conocido es volver a `instant:true`. **Segunda mitad del pedido (calendario de Eventos) -- verificado, sin cambios**: `js/eventos.js` no tiene ningún listener de `scroll` que cierre el calendario -- su único mecanismo de cierre por gesto es drag-to-dismiss (`touchmove`, umbral por fracción arrastrada), y `_evCerrarCalendario()` (el cierre "normal") ya SIEMPRE fue animado (mismo patrón freeze+doble rAF+`_evAnimarPanel()`, nunca tuvo una variante `instant`) -- no había nada que revertir ahí.

**Pedido #15 (revisado) -- unificar el patrón de animación de acordeón entre Equipo y Eventos, literal**: `_eqAnimarPanel()`/js/equipo.js y `_evAnimarPanel()`/js/eventos.js ya eran casi idénticas entre sí (mismo mecanismo, solo prefijo `eq`/`ev` distinto) -- este pedido cierra las 3 diferencias reales que quedaban, en vez de reescribir desde cero: **(1) easing** -- las 4 reglas de transición (`.eq-header-panel`/`.eq-misstats-panel-inner`+`.eq-busqueda-panel-inner`/css/equipo.css, `.ev-header-burbuja`/`.ev-header-burbuja > *`/css/eventos.css) pasan de `cubic-bezier(0.25,0.46,0.45,0.94)` a `ease` a secas -- resuelve el punto que había quedado pendiente sin confirmar en la ronda de tasks 9/10 ("el pedido sugiere `ease` en vez del cubic-bezier custom -- no se cambió sin confirmación"), ahora sí explícito en el snippet literal de este pedido. **(2) mecanismo interno** -- ambas funciones pasan de `(panel, alturaPx, translateY, volverseAuto)` a `(panel, desdePx, haciaPx, translateY, volverseAuto)`: fijan `desdePx` YA en este tick (el valor que el panel YA tenía -- `'0px'` para abrir, `scrollHeight` congelado para cerrar) y recién en un `requestAnimationFrame` INTERNO fijan `haciaPx` -- mismo resultado que el freeze-síncrono-afuera + doble-rAF-afuera que usaban `_eqCerrarPanel()`/`_evCerrarCalendario()` antes (un solo rAF adentro alcanza, el freeze síncrono en el mismo tick ya "commitea" el punto de partida), pero ahora AMBOS abrir y cerrar pasan por el mismo helper con la misma forma -- los 2 callers externos (`_eqCerrarPanel()`, `_evCerrarCalendario()`) pierden su doble-rAF propio, ya no hace falta. Los 2 mecanismos de drag-to-dismiss (`_eqInicializarCierrePanelesPorScroll()`/js/equipo.js, `_evInicializarCierreCalendarioPorScroll()`/js/eventos.js -- arrastran el panel 1:1 con el dedo) también actualizados a la firma nueva: `desdePx` ahí es `panel.style.height` (el alto parcial que dejó el último `touchmove`), no `scrollHeight` (mismo criterio documentado ya en el comentario existente de por qué esos 2 casos no reusan el cierre "normal" tal cual). **(3) `height:auto` en el calendario de Eventos** -- `_evAnimarPanel()` gana el parámetro `volverseAuto` que ya tenía la versión de Equipo (`_evAbrirCalendario()` lo pasa `true`; nueva clase `.ev-header-burbuja.ev-panel-auto { height:auto; overflow:visible; }`, calco de `.eq-panel-auto`) -- sin el riesgo que motivó el `instantAuto`/doble-rAF especial de Equipo (bug real "chevron expandido, panel invisible" por `#s-equipo` `display:none` al medir): `_evAbrirCalendario()` solo se dispara desde un tap directo del usuario (`_evTogglePanel('mes')`), nunca en un re-render de fondo con la pantalla oculta, así que no necesita ese camino defensivo. **Excepción que se mantiene a propósito, sin eliminar** -- `instantAuto` (`_eqAbrirPanel()`) y `instant` (`_eqCerrarPanel()`) en Equipo: son las 2 únicas rutas de este archivo que NO pasan por el patrón animado unificado -- `instantAuto` existe puntualmente para el auto-open inicial de "Mis estadísticas" mientras la pantalla puede seguir oculta (el caso que Eventos no tiene, ver punto 3), e `instant` (sin uso real hoy, ver pedido #15 original) queda vivo por si hace falta un cierre sin transición de nuevo -- ninguno de los 2 es "una variante distinta del camino normal", son casos especiales documentados aparte, no se tocaron.

**Fix layout shift en cierre de calendario por drag** (`_evInicializarCierreCalendarioPorScroll()`/js/eventos.js): el sticky header `#ev-sticky-header` se pineaba durante el drag sin animar su altura al cerrar, causando un salto en el timeline. Se pinea la altura en touchstart y se anima junto al panel en touchend, liberándola tras 350ms.

**Fix dirección de cierre del drag-to-dismiss** (`_eqInicializarCierrePanelesPorScroll()`/js/equipo.js, `_evInicializarCierreCalendarioPorScroll()`/js/eventos.js): la llamada a `_eqAnimarPanel()`/`_evAnimarPanel()` que cierra el panel tras soltar el drag pasaba `translateY(-100%)` a los hijos -- el mismo valor que usa el cierre "normal" (botón/chevron), pensado para un panel que arranca en su alto completo. En el cierre por drag el panel ya arrancó desde su alto PARCIAL (lo que dejó el último `touchmove`), así que ese `-100%` desplazaba el contenido de más, de forma inconsistente con lo que el dedo venía arrastrando. Corregido a `translateY(0)` en ambos -- el contenido no se desliza al cerrar por drag, solo el wrapper recorta con `height` (mismo criterio que la rama de "no llegó al umbral", que también usa `translateY(0)`). **Segundo fix relacionado, mismo pedido** (`js/eventos.js`, drag del calendario): el sticky header pineado durante el drag (fix anterior, este mismo bloque) suma `overflow: hidden` en touchstart y lo limpia junto con `height`/`transition` al liberar (tanto en el cierre por umbral, `setTimeout` de 350ms, como en la rama de restaurar) -- sin esto, el contenido interno del header podía desbordar visualmente mientras su `height` estaba pineada a un valor menor que su contenido real durante la animación de colapso.

**Fix capturar `offsetHeight` antes de remover clases al cerrar por drag** (mismos 2 bloques de arriba): el `desdePx` que se le pasaba a `_eqAnimarPanel()`/`_evAnimarPanel()` era `panel.style.height` -- válido mientras el `touchmove` alcanzó a correr y fijar un píxel real inline, pero en un release muy rápido (menos `touchmove` que frames, o un tap-drag sin `touchmove` intermedio) esa propiedad podía seguir vacía, y de por medio (en Equipo) `eq-panel-auto` todavía no se había sacado -- la transición terminaba yendo de `height:auto` (o de un string vacío, equivalente a sin override) a `0px`, un salto que CSS no puede animar (no hay valor numérico de partida que interpolar). Fix: medir `panel.offsetHeight` (valor numérico real, siempre disponible) ANTES de tocar ninguna clase, y usarlo como `desdePx`; en Equipo además se agrega `panel.classList.remove('eq-panel-auto')` junto a `remove('abierta')`, por las dudas de que el drag arrancara sin pasar por `touchstart` (que ya la saca) -- documentado explícito para que la medición quede aislada del efecto de remover clases.

**Fix real de la ronda anterior -- faltaba pinear el height inline, no solo medirlo**: medir `panel.offsetHeight` antes de remover clases (fix anterior) no alcanzaba solo -- lo que hace falta para que la transición CSS de `_eqAnimarPanel()`/`_evAnimarPanel()` tenga de dónde arrancar es un `height` INLINE numérico ya puesto en el elemento ANTES de que la clase que daba `height:auto` (`eq-panel-auto`) se saque; si la clase se remueve primero, el browser recalcula el estilo con la regla base (`height:0`/sin override) en el mismo tick, así que para cuando `_eqAnimarPanel()` fija `desdePx` ya no hay un `auto` real del que partir -- el valor medido llegaba tarde. Fix: `panel.style.height = panelH;` (el valor ya medido) se fija INMEDIATAMENTE después de medir `offsetHeight`, ANTES de cualquier `classList.remove()` -- así el elemento ya tiene un píxel concreto pineado como estilo inline en el momento en que se le saca `eq-panel-auto`/`abierta`, sin ventana donde quede sin valor de partida. Mismo orden en los 2 bloques (`js/equipo.js`, `js/eventos.js`).

**Fix cierre de panel -- reemplazado `_eqAnimarPanel()`/`_evAnimarPanel()` por force-reflow directo en los 2 bloques de drag-to-dismiss** (`_eqInicializarCierrePanelesPorScroll()`/js/equipo.js, `_evInicializarCierreCalendarioPorScroll()`/js/eventos.js -- solo la rama de cierre por umbral, no la de "volver al alto original"): el helper compartido dispara su propio `requestAnimationFrame` interno para pasar de `desdePx` a `haciaPx`; en el gesto de cierre por drag ese rAF interno podía competir con el `requestAnimationFrame` que ya corre en paralelo animando el `#ev-sticky-header` colapsando (bloque `if (_evTimelineStickyHdr)`, ver fix anterior) -- 2 rAF distintos escribiendo estilos sobre elementos relacionados en la misma ráfaga de frames, orden no garantizado entre sí. Reemplazado por el patrón force-reflow explícito y síncrono: fija `height` al píxel medido, saca las clases, `void panel.offsetHeight` (fuerza el recálculo de estilo ANTES del próximo frame, sin depender de ningún rAF), recién ahí fija `height:0px` -- el browser ya tiene registrado el punto de partida en el mismo tick, sin ninguna ventana de carrera con otro rAF. La limpieza final (`panel.style.height = ''`, y en Eventos también `_evActualizarNavMesChevron()`) se mueve a un listener de `transitionend` (`{ once: true }`) en vez de encadenarse al callback interno de `_eqAnimarPanel()`/`_evAnimarPanel()`, que ya no se usa acá. Costo real de este cambio: se pierde el desliz `translateY` de los hijos (`.eq-panel-inner-anim`/`.ev-panel-inner-anim`) que sí sigue teniendo el cierre "normal" (botón/chevron) -- el cierre por drag ahora es únicamente el recorte de `height`, sin el paralelo de contenido deslizándose (ya venía en `translateY(0)`, sin desliz real, desde el fix anterior de dirección -- este cambio solo saca el mecanismo que lo habilitaría a futuro).

**Fix drag-to-close -- faltaba remover `eq-panel-auto`/`ev-panel-auto` ANTES de animar**: `.eq-panel-auto`/`.ev-panel-auto` fijan `overflow: visible` (necesario en su estado de reposo abierto, donde no hay nada que recortar -- ver esas clases, `css/equipo.css`/`css/eventos.css`). El force-reflow del fix anterior pineaba `height` al píxel medido y forzaba el reflow, pero si esta clase seguía puesta en ese momento, el panel medía bien su alto pero seguía con `overflow:visible` mientras el `height` se animaba a `0px` -- el contenido interno (todavía de tamaño completo, sin recortar) quedaba visualmente escapando del box durante toda la transición, en vez de recortarse limpio. En Equipo, `panel.classList.remove('eq-panel-auto')` ya estaba en el bloque pero DESPUÉS de `remove('abierta')` -- reordenado antes, mismo criterio que fijar `height` antes de cualquier `classList.remove()` (fix de la ronda anterior: la clase que controla `overflow` tiene que sacarse ANTES de animar, no en cualquier orden). En Eventos, `ev-panel-auto` no se removía en ningún punto de este bloque -- agregado, mismo lugar relativo (antes de `remove('abierta')`).

**Fix drag-to-close roto en Equipo -- orden invertido rompía el gesto al primer toque**: en el `touchstart` de `_eqInicializarCierrePanelesPorScroll()`, `eq-panel-auto` se removía ANTES de medir `getBoundingClientRect().height` -- sacar esa clase colapsa `overflow`/deja de fijar `height:auto`, así que para cuando se medía, el panel ya podía estar en su alto real igual (el layout no cambia solo por sacar una clase que no tenía otro efecto que `height:auto`/`overflow:visible`) PERO sin ningún `height` inline pineado todavía -- el primer frame de `touchmove` partía de un panel sin `style.height` fijado, cayendo a la regla base (`height:0`) antes de que el primer cálculo de `nuevaAltura` corriera, colapsando el panel al primer toque en vez de arrancar el drag desde su alto actual. Fix: medir la altura y fijarla como `style.height` PRIMERO, recién después sacar `eq-panel-auto` -- mismo criterio que los fixes anteriores de esta ronda (la clase que controla `height:auto`/`overflow` se saca DESPUÉS de que ya hay un píxel concreto pineado, nunca antes). **Fix relacionado en Eventos, mismo mecanismo -- overflow + transform en px durante el drag**: el `touchmove` no fijaba `overflow` en ningún punto (heredaba lo que hubiera quedado de la clase base) -- agregado `overflow:''` en la rama de "soltar hacia abajo" (restaura) y `overflow:'hidden'` en la de "arrastrar hacia arriba" (recorta mientras se cierra), evitando que `#ev-cal-contenido`/`#ev-mes-pills-row` se superpongan visualmente con la nav mientras el wrapper todavía no terminó de recortar. El `transform` de los hijos pasa de `translateY(-pct%)` (porcentaje relativo al alto de CADA hijo -- `#ev-cal-contenido` y `#ev-mes-pills-row` miden alturas muy distintas entre sí, así que un mismo `pct` los desplazaba visualmente distinto, los pills casi no se movían) a `translateY(dy px)` (el desplazamiento real en píxeles del dedo, igual para todos los hijos sin importar su alto propio -- se mueven en sincronía real). Limpieza: `overflow`/`transform` de los hijos se resetean tanto en la rama de restaurar (antes de `_evAnimarPanel()`) como en el `transitionend` del cierre por umbral (junto a `height`).

**Revertido el fix de `overflow`/`transform` en px de la ronda anterior + agregado guard de race condition contra el rAF en vuelo**: el cambio anterior (`overflow:hidden`/`''` + `translateY(dy px)` en los hijos) se saca por completo en los 2 archivos -- vuelve al criterio previo, solo `height` en vivo durante el drag, sin transform paralelo en los hijos ni control de `overflow` inline. **Fix real y nuevo de esta ronda, en ambos archivos**: el `requestAnimationFrame` que arma cada `touchmove` (con el flag `_eqDragRafTicking`/`_evDragRafTicking` para como mucho 1 por frame) podía quedar en vuelo cuando el gesto ya terminó -- si `touchend` corre y limpia el estado ANTES de que ese rAF pendiente se ejecute (perfectamente posible, un rAF encolado no se cancela solo porque el dedo se levantó), el callback igual corría y volvía a escribir `panel.style.height`/`transform` sobre un panel que la lógica de `touchend` ya había dejado en su estado final -- podía pisar el resultado del cierre/restauración recién aplicado. Fix: `if (!_eqListaDragActivo) return;` / `if (!_evTimelineDragActivo) return;` agregado como primera línea DENTRO del callback del rAF (después de bajar el flag de ticking, antes de tocar cualquier estilo) -- si el drag ya terminó para cuando el frame efectivamente corre, el callback no hace nada.
│
├── shared/
│   ├── bsheet.js                 Listener delegado que cierra bottom sheets al tocar la manija
│   ├── axis-transicion.js        Transición "shared axis X" (axisTransicion())
│   └── date-picker.js             Date picker dp-* (usado en inscripcion/)
│
├── inscripcion/                  Mini-SPA de inscripción completa (Google Sign-In + todos los datos)
│   ├── index.html / inscripcion.css / inscripcion.js
│
├── registro-express/              Mini-SPA de alta rápida (nombre+PIN, sin Google), con reserva pre-seleccionada
│   ├── index.html / registro-express.js
│
├── activar/                       Mini-SPA de activación de cuenta pre-creada por un admin (feat nueva, pedido explícito, ver MANIFEST.md/CHANGELOG.md — "activar cuenta pre-creada") — mismo patrón/CSS que `inscripcion/` (reusa `../inscripcion/inscripcion.css` directo, sin copiarlo), pero para el caso INVERSO: un admin ya dio de alta a alguien en `equipo` (fila real en el roster, típicamente sin `email` -- nunca pasó por `inscribirPersona`) y le comparte un link de un solo uso para que esa persona vincule su cuenta de Google y complete pronombres/teléfono/equipamiento. Flujo: `?token=<invite>` en la URL → `validarInviteToken` (Edge Function) valida el token (existe/no usado/no expirado) y devuelve `datos_prefill` (snapshot liviano tomado al generar el link -- username/nombreDerby/fotoPerfil, sin re-consultar `equipo` en vivo) → pantalla de bienvenida ("Hola {username}, completa tu perfil en Pivot") con botón de Google (GIS crudo, MISMO mecanismo que el resto de la app) → al elegir cuenta, `activarCuenta` vincula el email a esa fila de `equipo` y devuelve una sesión real (mismo formato que `loginGoogle`/`inscribirPersona`, token guardado en `localStorage.session`) → 3 pasos cortos (pronombres/teléfono/patines+protecciones, subset de los de `inscripcion/`) → `completarActivacion` guarda esos campos → redirect a la app (`?nuevx=1`, mismo patrón que `inscripcion/`). **Deliberadamente NO usa Supabase Auth** (`supabase.auth.*`/`@supabase/supabase-js`) pese a que el pedido original lo especificaba así -- esta app nunca usó Supabase Auth en ningún lado (auth 100% propio: GIS crudo + `sessions`/`admin_sessions` con tokens UUID hechos a mano) y no hay ningún provider de Google configurado del lado de Supabase -- shippear `supabase.auth.signInWithOAuth()` tal cual se pidió habría quedado roto en producción (requeriría configurar un OAuth provider en el dashboard de Supabase, credenciales que nadie tiene cargadas). En vez de eso, reusa el mecanismo de auth YA real de esta app (`_verificarGoogleToken()`/`_crearToken()`, los mismos que usan `loginGoogle`/`inscribirPersona`).
│   ├── index.html / activar.js
│
└── supabase/
    ├── functions/api/index.ts     Edge Function (ver sección "Edge Function")
    └── migrations/                 SQL versionado (pg_cron, tablas nuevas, columnas nuevas)
```

`js/config.js`, `shared/bsheet.js`, `shared/axis-transicion.js` y `js/foto.js`/`js/color-enfasis.js` se cargan tanto en `index.html` como en `inscripcion/index.html`. `registro-express/` reusa `../inscripcion/inscripcion.css` y `../css/perfil.css` directo, sin copiar clases.

## 2. CSS — principios

- **Colores:** `css/colors.css` es la única fuente de verdad. Ningún CSS/JS/HTML puede hardcodear un hex/rgb para el que ya exista variable. Excepción: valores pasados a librerías que no interpretan CSS (ej. array de confetti en `ui.js`). Grupos principales: brand, fondo/superficie, bordes, texto, estados (success/danger/warning/info), amber, purple, y 2 excepciones de marca de terceros (`--deuna`, `--banco-internacional`) sin variante dark. Dark mode automático vía `@media (prefers-color-scheme: dark)` al final del archivo. **Rebrand a Pivot** (pedido explícito, ver CHANGELOG.md — "nueva paleta Pivot"): marca fija roja, `#E8000D` en claro / `#FF2020` en oscuro (2 hex DISTINTOS, no una variante de alpha del mismo rojo — más saturado/claro en oscuro para contraste sobre el bg casi negro nuevo) — **ya no personalizable** (ver "Selector de color eliminado" más abajo). Solo se actualizaron los tokens directamente relacionados a la marca/estados (`--brand`/`--brand-dk` + toda su familia rgba, `--bg`/`--text`/`--muted`/`--border-light`/`--card-bg`, `--danger`/`--danger-bg`/`--danger-bdr`, `--success`/`--success-bg`/`--success-bdr`, `--warning`/`--warning-bg`/`--warning-bdr`) — el resto del archivo (spacing, radius, `--purple`/`--cyan`/`--amber`, colores de terceros, `--dk-*`, sombras negras, etc.) quedó sin tocar, decisión explícita para no romper el resto de la app (~200 variables totales, la mayoría sin relación con la marca). `--danger` es ahora `var(--brand)` (mismo rojo, un solo lugar) en vez de un literal propio. `--bg`/`--text`/`--muted`/`--border-light`/`--card-bg` dejaron de ser "(runtime)" — antes los recalculaba `js/color-enfasis.js` en cada carga, ahora son literales fijos iguales en cada modo (ver ese archivo, más abajo, para qué variables SIGUEN siendo runtime). **Fix real — "fondo rosado en cards/items"** (re-ajuste, pedido explícito, confirmado tras el rebrand): `--surface-2`/`--surface-3` (fondo de `.aj-group`/`.aj-app-rows` en Ajustes y de cards/filas en 11 archivos CSS más) seguían en la familia "(runtime)" heredada del sistema viejo — pensada para un naranja cálido casi imperceptible, pero con el rojo saturado de Pivot esa misma derivación de matiz se leía como un tinte ROSA visible en cualquier superficie grande. Pasaron a NEUTRAS puras (`#F5F5F5`/`rgba(0,0,0,0.03)` en claro, `#1A1A1A`/`rgba(255,255,255,0.03)` en oscuro — `--surface-2` coincide a propósito con `--card-bg`, mismo rol visual) — ya no derivan de `--brand` en absoluto. Mismo criterio aplicado a `--surface`/`--surface-light`/`--btn-secondary-bg`/`--btn-secondary-hover-bg` en oscuro (antes `rgba(255,32,32,...)`, ahora overlay blanco translúcido — patrón estándar de "elevación" en UI oscura). `--brand-soft` bajó su tope en claro de `0.1` a `0.04` (usada también en `border-color`/`box-shadow`, no solo fondos). Principio de diseño resultante, explícito: **solo los elementos interactivos activos** (botón primario, tab seleccionado, badges — familia `--brand-*` real: `--brand-light`/`--brand-lightest`/etc., SIN cambios) **usan el rojo**; cualquier fondo "de contenedor" (cards, filas agrupadas, superficies secundarias) es gris neutro. Los íconos de categoría de Ajustes (patines/teléfono/candado/etc., fondos azul/morado/amarillo) NO forman parte de este sistema — colores hardcodeados aparte, señalados como "no tocar" por pedido explícito, sin relación con `--brand`. **`--success`/`--success-bg`** (claro, contraste del botón "Asistiré") — `#22c55e`/`#F0FFF4` (post-rebrand) se veían "lavados"/bajo contraste → `#15803D`/`#DCFCE7` (más oscuro/saturado). Sin cambios en oscuro (`--success:#22C55E` ya tenía buen contraste ahí). **Extensión del fix — resto de la familia "ambiente"** (re-ajuste, pedido explícito): el fix anterior solo neutralizó `--surface-2`/`--surface-3` (la fuente puntual del bug) — este re-ajuste aplica el MISMO principio a TODA la familia de fondo/borde/texto "ambiente" que quedaba derivando de `--brand`: `--bg-2`, `--surface` (ahora `#F7F7F7` plano en claro, ya no `rgba(255,255,255,0.9)`), `--surface-light`, `--border-warm`, `--border-mid`, `--border-softest`, `--text-2`, `--hint`, y las 4 sombras (`--card-shadow`/`--shadow-sm`/`--shadow`/`--shadow-lg`, ahora negras en los 2 modos, ya no `rgba(232,0,13,...)`/`rgba(255,32,32,...)`). En oscuro, `--surface-2` deja de ser el hex fijo `#1A1A1A` y pasa a `rgba(255,255,255,0.04)` (overlay translúcido, consistente con el resto de la familia) — visualmente casi idéntico sobre `--bg:#0D0D0D`, pero reactivo si ese valor cambiara. También en oscuro: `--border-slate`/`--disabled-border`/`--btn-secondary-hover-bg`/`--border-light` dejan el `rgba(255,32,32,...)` (marca) por `rgba(255,255,255,...)` (neutro). Sombras oscuras bajan de alpha (0.4/0.5/0.6 → 0.35/0.45/0.55, `--card-shadow` sin cambio real). Única familia que SIGUE derivando de `--brand`: `--brand*`/`--brand-warm*` (el acento real) — confirmado, nada más.

**theme-color/manifest.json desincronizados del rebrand** (bug real corregido, pedido explícito) — los 2 `<meta name="theme-color">` (`media="(prefers-color-scheme: light/dark)"`, index.html) YA tenían el mecanismo correcto de 2 tags separados por media query (no uno solo, como podría asumirse) — pero sus valores estáticos habían quedado en `#FDF3EB`/`#170900` (el naranja/café de la paleta vieja) sin actualizar durante el rebrand a Pivot; `js/color-enfasis.js` los corrige en runtime (`getComputedStyle` de `--bg`), así que el color real que se veía ya estaba bien, pero el valor de ARRANQUE (antes de que el JS corra, y lo que lee cualquier crawler/OS que no ejecuta JS) seguía siendo el viejo. Corregidos a `#FFFFFF`/`#0D0D0D`. `manifest.json` no soporta media query (un solo `theme_color` para toda la PWA instalada) — se dejó en `#FFFFFF` (claro) a propósito, mismo criterio explícito del pedido. **Mismo bug en `inscripcion/`/`registro-express/`** (pedido explícito, "verificar si color-enfasis.js sincroniza theme-color") — `js/color-enfasis.js` en sí YA sincroniza los 2 `<meta name="theme-color">` correctamente en runtime (lee `--bg` vía `getComputedStyle`, sin duplicar el literal) en las 3 páginas que lo cargan (`index.html`/`inscripcion/`/`registro-express/`) — no hizo falta tocar ese archivo. Pero `inscripcion/index.html`/`registro-express/index.html` tienen sus PROPIOS `<meta name="theme-color">` hardcodeados a mano (comentario propio: "sincronizados a mano con --bg de colors.css") que se habían quedado en el naranja/café viejo (`#FDF3EB`/`#170900`) — mismo bug que ya se había corregido en `index.html`, pero estas 2 mini-SPA quedaron afuera de esa tanda. Corregidos a `#FFFFFF`/`#0D0D0D` (el valor ESTÁTICO/de arranque, antes de que `color-enfasis.js` corra — el mismo criterio "first paint" que motivó el fix original de `index.html`). **Confetti (`lanzarConfetti()`/`js/ui.js`) actualizado a la paleta Pivot** (pedido explícito, tras búsqueda global de remanentes de `#F97316`) — único remanente visual real del naranja viejo fuera de `colors.css`/`js/color-enfasis.js` (que sí lo usan a propósito, como ancla matemática de referencia, no como color mostrado — ver ese archivo). Array de colores del canvas (excepción ya documentada, no interpreta `var()`) pasó de la paleta arcoíris original a rojo/blanco/negro/grises: `['#E8000D','#FF2020','#FFFFFF','#000000','#666666','#CCCCCC']` — los 2 rojos son `--brand` claro/oscuro literales. `AUDITORIA_ESTILOS.md` (doc de auditoría pre-rebrand) actualizado en sus filas de theme-color/confetti para reflejar el estado actual, con una nota de cabecera aclarando que el resto del documento sigue siendo el snapshot original (no re-auditado).

De paso, `background_color` (color de la pantalla de splash al abrir la PWA instalada, mismo archivo) tenía el mismo `#FDF3EB` viejo sin relación con ningún override en runtime (`manifest.json` es 100% estático, ver nota ya existente más arriba en este archivo) — corregido a `#FFFFFF` también, no explícitamente pedido pero mismo bug de fondo (hubiera dejado un flash de color viejo en el splash). **`icons` unificado a 1 sola entrada** (pedido explícito, "ícono PWA unificado"): antes 3 entradas (`icon-192B.png` 192×192 `purpose:any`, `icon-512C.png` 512×512 `purpose:any`, `icon-maskable-512.png` 512×512 `purpose:maskable`) — ahora una única `{src:"icons/icon-512C.png", sizes:"512x512", type:"image/png", purpose:"any maskable"}`, mismo archivo cubriendo los 2 propósitos (`any`+`maskable` en un solo string, sintaxis válida del spec) para que Android no tenga de dónde elegir un ícono viejo — sin ícono 192×192 dedicado, Android/iOS escalan el 512×512 para los contextos que lo pidan más chico.
- **Clases nuevas:** verificar primero si ya existe una equivalente en `global.css` o el CSS de la sección antes de crear una. Compartidas entre secciones → `global.css`; específicas de una sección → su propio CSS.
- **`--bottom-nav-h`** (`css/colors.css`) — `66px` (subida desde `60px`, pedido explícito). Fuente única entre la nav inferior en sí (`css/nav.css`), decenas de `calc(var(--bottom-nav-h) + env(safe-area-inset-bottom)...)` en `index.html`/`css/estilos.css`/`css/eventos.css`/`css/equipo.css`/`css/perfil.css` (spacers de scroll, footers fijos) y 2 fallbacks JS (`js/eventos.js`/`js/tareas.js`, `parseInt(getComputedStyle(...))... || 66` — actualizados junto con la variable, solo se usan si la lectura del CSS var fallara) — subir el valor acá alcanza para mover los 3 en conjunto, sin tocar ningún consumidor individual.
- **Estilos inline:** evitar en HTML y strings JS; si se repite, convertir en clase.
- **Bottom sheets:** un solo chrome visual compartido (`.bsheet-overlay/.bsheet/.bsheet-handle/.bsheet-title/.bsheet-body`, `css/global.css`) — nunca repetir el bloque a mano; solo `z-index`/`max-height` van inline por instancia.
- **Animación de entrada y salida obligatoria** en todo elemento transitorio (modal, sheet, overlay, toast, error inline) — nunca un `display` seco sin transición en ningún extremo. Patrón: doble `requestAnimationFrame` para la entrada (opacity 0→1 o transform), `setTimeout` con la misma duración del CSS antes de ocultar en la salida.
- **Acordeones animados con `max-height`:** medir `scrollHeight` con la pantalla todavía `display:none` da `0` (colapsa el acordeón sin que nada esté roto en el JS) — en el render inicial de un acordeón ya abierto, fijar `max-height:'none'` directo, nunca `scrollHeight + 'px'`. Al abrir/cerrar por click sí hace falta animar entre 2 valores numéricos (`none` no es interpolable): abrir fija `scrollHeight`px y recién post-transición lo suelta a `none`; cerrar "aterriza" primero en `scrollHeight`px y en el frame siguiente baja a `0px` (doble `requestAnimationFrame`). Mismo patrón reusado por `.eq-grupo` (Equipo, incluido el acordeón "INACTIVOS", colapsado por defecto) y Mi Liga. **Favoritos** (Equipo) es la variante NO colapsable de `.eq-grupo`: mismo markup (header sin botón/chevron, body con `max-height:'none'` fijo, nunca pasa por `_eqToggleGrupo()`), pero la SECCIÓN entera se muestra/oculta con fade de opacidad (no con `max-height`) cuando pasa de 0 a 1 favoritos o viceversa — `display:none`+`opacity:0` cuando está vacía, `_eqMostrarSeccionFavoritos()`/`_eqOcultarSeccionFavoritos()` (js/equipo.js) animan la transición al agregar/sacar el primer/último favorito (`display:block`+reflow forzado antes de subir a `opacity:1` al mostrar; `opacity:0` + `transitionend` dispara `display:none` al ocultar).
- **z-index, jerarquía aproximada:** nav superior/inferior `900`; footers fijos de pantalla (`.cta-footer-fixed`) `100` por default, con overrides puntuales a `960` cuando conviven con un panel `.aj-sub` (`950`); modales informativos (`.modal-info`) `8000`; `#loading-overlay` `9999`. Bottom sheets apilables via `_overlayStack`, `z-index` inline por instancia.

## 3. Tablas de Supabase (columnas clave)

Fuente de verdad real: `supabase/functions/api/index.ts` (la Edge Function usa la service role key, bypasea RLS).

- **`equipo`** — fila por persona, keyed por `username`. Columnas usadas activamente: `necesita_patines`/`necesita_protecciones` (**`text`, no `boolean`** — `'Sí'`/`'No'`, y `necesita_protecciones` a veces texto libre de lo que falta, ej. `"Muñequeras, Coderas"`; nunca comparar con `=== true`, interpretar como truthy y `.toLowerCase() !== 'no'`, mismo criterio que `adminGetQueLlevar()`/`js/home.js`/`js/perfil.js`), `talla`, `categoria` (tier Quindes/Mirlxs), `tier_modo` (`'auto'` o fijado a mano por admin), `exenta_cuota`, `estado_miembro` (`Activx`/`Ausente`/`Satélite`/`Técnico`/`Lesionadx`, default `Activx` — sigue decidiendo si Ajustes → Mi Perfil muestra el botón "Reportar lesión", `_datosRenderStatsHtml()`/js/perfil.js, pero YA NO se pinta ahí como chip de texto "Estado" — ese campo visual se sacó, pedido explícito de solo tocar lo visual sin tocar el dato/la lógica; sigue viéndose como chip en Equipo, `.dat-estado-chip`/js/equipo.js), `solicitud_lesion_pendiente`, `nombre_derby`, `numero_derby`, `pronombres`, `fecha_ingreso`, `email`, `prefijo`, `telefono`, `fecha_publica`, `edad_publica`, `fecha_nacimiento`, datos legales (`tipo_documento`/`pais_expedicion`/`numero_documento`/`nombre_legal`), dirección (`calle_principal`/`calle_secundaria`/`numeracion`/`sector`/`canton`), 2 contactos de emergencia (`emerg1_*`/`emerg2_*`), datos médicos (`enfermedad`/`alergias`/`dieta`/`antecedentes`/`medicamentos`/`atencion_medica`/`seguro*`), `cupon_disponible`, `foto_perfil`, `permisos_configurados`, stats anuales `horas_ano`/`asistencias_ano`/`total_eventos_ano` (pobladas por `recalcularStatsEquipo`/`recalcularStatsUsuario`, y corregibles a mano por username o para todo el equipo vía la acción `adminRecalcularStats`), `termometro_pct` (poblada por `recalcular-categorias`, ver `tendencia` de `getEquipo()` más abajo), `racha_actual` (integer, default 0, migración `20260831_equipo_racha_actual.sql` — contador de asistencias reales consecutivas, ver "Racha de asistencias" más abajo) y `puntos_anteriores` (numeric, default 0 — arrastre de puntos previo a la existencia de `puntos_mensuales`, sumado a `puntosTotal` solo cuando `getEquipo()` se pide con `params.historico:true`, ver `puntos_mensuales` más abajo). **RLS habilitado** (migración `20260830_equipo_rls.sql`) — única policy: `equipo_read_authenticated` (`SELECT` a rol `authenticated`, que esta app nunca usa vía anon key, en la práctica bloquea toda lectura directa por PostgREST). Sin policy de `INSERT`/`UPDATE` — toda escritura real pasa por el Edge Function (`service_role`, bypasea RLS). Ningún código del frontend debe intentar leer/escribir `equipo` con `fetch()` directo a PostgREST (patrón ya usado para `temporadas_descanso`/`asistencias`/etc.) — para esta tabla específica, siempre vía una acción del Edge Function. **No tiene columna `rol`/`posicion`** (verificado contra `information_schema.columns`) — el "rol" de derby (Jammer/Blocker/Coach/Bench/SO/NSO/No definido) es un concepto puramente frontend: `_EQ_ROLES` (array fijo) + `_eqRolesDe(username)`/`_eqSetRolesDe(username, roles)` (`js/equipo.js`), persistido en `localStorage` (`eq_roles_<username>`) — **por-dispositivo, no sincronizado entre sesiones/dispositivos**, limitación conocida y permanente hasta que exista una columna/acción real en el backend. Usado por el filtro de rol del panel de Filtros de Equipo (100% frontend, sin llamada nueva) y por el bottom sheet "Rol en el equipo" de Ajustes → Mi Perfil. **Bug real corregido — `exenta_cuota` nunca llegaba a `E.datos`** (pedido explícito, "admin exento de cuota igual ve el gate de pago al hacer RSVP"): toda la lógica de gating de cuota del frontend (`_evTieneCuotaAlDia()`/js/eventos.js y el resto de los call sites que dependen de ella) YA leía `E.datos.exenta_cuota` correctamente desde antes — el bug real estaba en el backend: `getDatosCompletos()` (helper que arma `E.datos` para CUALQUIER login/restauración de sesión, `supabase/functions/api/index.ts`) nunca mapeaba esa columna en su objeto de salida, así que `E.datos.exenta_cuota` era `undefined` SIEMPRE, para cualquier cuenta (no específico de admin, aunque así se reportó — coincide con que el caso probado tenía la columna en `true`). Fix: agregado `exenta_cuota: row.exenta_cuota === true` a `getDatosCompletos()` (snake_case a propósito, mismo criterio que `estado_miembro` en esa misma función — el frontend ya esperaba ese nombre exacto de campo). Deployado.
- **`asistencias`** — una fila por evento/clase. `id_evento` (PK lógica), `fecha`, `donde` (lugar), `inicia`/`termina` (hora), `dura`, `estado`, `google_maps`, `info_adicional`, `tipo_evento`, `bloqueado`, `id_regla` (venue/regla de recurrencia que lo generó), `es_excepcion`. Columnas legado `a_horario`/`tarde` (texto CSV de nombres) — se siguen actualizando en paralelo a `log_asistencias` como fallback para eventos sin marca real de admin. `estado` real toma 5 valores: `'Próximo'` (recién insertado por `regenerar_ventana_asistencias()`), `'Evento Programado'`, `'Evento Cancelado'`, `'No se entrena'`, `'Evento Finalizado'`. La transición a `'Evento Finalizado'` vivía en una automatización de Apps Script/Sheets discontinuada al migrar la generación de eventos a pg_cron (quedó huérfana un tiempo — ver CHANGELOG, Bug 13) — repuesta por el cron `marcar-eventos-finalizados` (pg_cron, cada 30 min, migración `20260831_cron_eventos_finalizados.sql`): `UPDATE asistencias SET estado='Evento Finalizado' WHERE estado='Evento Programado' AND (fecha + termina)::timestamp < NOW() AT TIME ZONE 'America/Guayaquil'` (DB en UTC; `fecha`/`inicia`/`termina` se guardan en hora local de Ecuador sin componente de zona, sin DST). Aun así, **ningún código nuevo debe depender de `estado === 'Evento Finalizado'`** para filtrar "eventos ya sucedidos" — seguir usando `fecha < hoy` + excluir `'Evento Cancelado'`/`'No se entrena'` por nombre (ver `recalcularStatsEquipo`/`recalcularStatsUsuario`/`recalcular-categorias`): el cron corre cada 30 min, no al instante, así que el status puede quedar desactualizado por una ventana corta.
- **`log_asistencias`** — mezcla 2 conceptos por fila: RSVP pre-evento (`origen:'Usuario'`/`'AsistenciaAnticipada'`, `estado:'Asistiré'`/`'No asistiré'`/`'No jugador'`) y asistencia real post-evento tomada por un admin (`origen:'Admin'`, `estado:'A tiempo'`/`'Tarde'`/`'Ninguno'`). Columnas: `id_evento`, `nombre_usuario`, `origen`, `estado`, `marca_temporal`, `fecha_entrenamiento`. Al leer, filtrar por `origen==='Admin'` antes de asumir "ya hay asistencia real". **Bug real corregido — el auto-persist de reglas de asistencia anticipada sobreescribía RSVPs manuales**: `_evAntReconciliarConReglas()` (js/eventos.js) materializa en el backend el RSVP resuelto por una regla llamando a `marcarAsistenciaUsuario`, pero ese action escribía siempre `origen:'Usuario'` (Edge Function, `supabase/functions/api/index.ts`) — indistinguible de una elección real del usuario, así que una regla que corría DESPUÉS de que el usuario respondiera manualmente terminaba pisando esa fila con la misma prioridad (ver `_ultimaAsistenciaPorPersonaTodas()`, que prioriza `origen==='Usuario'` sobre cualquier otro). Fix: el call site manda `origenAuto:true`, y `marcarAsistenciaUsuario` usa eso para escribir `origen:'AsistenciaAnticipada'` en vez de `'Usuario'` cuando la fila viene de una regla — así nunca gana por prioridad sobre una respuesta real. **`fuenteAsistencia` en `getEventosRango()` — 2 bugs opuestos, ya resueltos con una combinación en vez de una elección excluyente**: la línea pasó por 3 versiones. (1) `logDeEvento.length ? logDeEvento : asistEF` (original) — un evento con RSVPs en `log_asistencias` pero asistencia real SOLO en las columnas legacy `a_horario`/`tarde` (`asistencias`, ver arriba) perdía esa asistencia real, porque `logDeEvento.length` ya daba `>0` solo con los RSVPs y nunca caía al fallback (fix 2026-08-23, ver CHANGELOG). (2) `logAdminReal.length ? logDeEvento : asistEF` (fix de ese bug) — rompió el caso normal: un evento con SOLO RSVPs, sin ninguna marca real todavía (el estado antes de que pase el evento), tiene `logAdminReal.length===0`, cae al fallback `asistEF[idEvento]` que está vacío, y **los RSVPs desaparecen** (bug reportado, revertido 2026-09-05). (3) Versión actual — `logAdminReal.length ? logDeEvento : [...logDeEvento, ...(asistEF[idEvento] ?? [])]`: cuando no hay marca real en `log_asistencias`, en vez de reemplazar `logDeEvento` por el fallback, lo SUMA — cubre ambos casos a la vez (RSVPs se preservan siempre que existan, y la asistencia real legacy se agrega si `log_asistencias` no la tiene). Si se vuelve a tocar esta línea, no alternar entre las 2 versiones excluyentes de nuevo — ambas tienen un caso real que rompen.
- **`rectificaciones_asistencia`** — solicitud de corrección de un usuario sobre su propia marca. `id` (uuid), `nombre`, `id_evento`, `fecha_evento`, `estado_solicitado` (`'A tiempo'`/`'Tarde'`/`'Sin registrar'`), `decision` (default `'Pendiente'`, luego `'Aprobada'`/`'Rechazada'`), `created_at`. Aprobar aplica el mismo mecanismo que `adminMarcarAsistencia`.
- **`reservas`** — `nombre_usuario`, `id_evento` (null si es mensual), `tipo` (`'clase'`/`'mensual'`), `mes_texto`, `talla`, `protecciones`, `estado` (`'Pendiente'`/`'Confirmada'`/`'Cancelada'`/`'Reagendar'`/`'Crédito usado'`), `monto`, `fecha_pago`, `email`. `monto` (`guardarReserva()`) es el precio de lista fijado al crear la reserva (`getPreciosClases()`, mensual o por clase) — `0` si se consumió un crédito de reagendo (`_consumirCreditoReal()`) o un cupón (`equipo.cupon_disponible`), sin columna separada que distinga cuál de los 2 fue. Devuelto tal cual por `adminGetReservas()` (`select('*')`, sin transformar) — pintado en Mi Liga → Reservas (`js/admin.js`, `adminRenderReservas()`) como "Pagó: $N" en cada card, incluido `$0`.
- **`venues`** — `id`, `lugar`, `tipo_icono`, `requiere_reserva`, `inicia` (hora — **no** `hora`), `google_maps`, `tipo` (recurrencia — no `tipo_recurrencia`), `dias` (no `dias_semana`), `frecuencia`, `unidad`, `fecha_referencia`, `video_instructivo`. No tiene `lat`/`lng` (el mapa cae a un fallback fijo).
- **`equipamiento_tallas`** (`talla`, `cantidad`) y **`config_equipamiento`** (`total_protecciones`) — stock de patines/protecciones.
- **`tareas`** / **`asignaciones_tareas`** / **`config_tareas`** (`limite_tareas_activas`) — sistema de tareas del club.
- **`puntos_mensuales`** — una fila por persona por mes (`UNIQUE(nombre_usuario, anio, mes)`, sin FK real — `nombre_usuario` es texto libre, key natural por convención). Columnas reales (ojo, singular, no plural): `nombre_usuario`, `anio`, `mes`, `puntos_asistencia`, `puntos_tareas`, `puntos_bonificacion`, `puntos_extra`, `puntos_total` (**columna `GENERATED ALWAYS AS` = suma de las otras 4** — nunca escribirla a mano, Postgres la calcula sola). Escritores en este repo: `_acreditarPuntosTarea()`/`_restarPuntosTarea()` (`puntos_tareas`, al aprobar/revertir una tarea), `recalcularPuntosAsistencia(mes, anio)` (`puntos_asistencia`, desde `log_asistencias` filtrado por `origen==='Admin'` — `'A tiempo'`=1 punto, `'Tarde'`=0.5 — expuesta como acción `adminRecalcularPuntosAsistencia`, y disparada automáticamente para el mes actual al final de `adminRecalcularStats()`; bug real corregido, ver MANIFEST.md/CHANGELOG.md — "puntos de asistencia inflados por re-marcados": `_agregarFilaLogAsistencia()` siempre hace `.insert()`, nunca update — cada re-marcado de la misma persona en el mismo evento suma una fila NUEVA a `log_asistencias`, la vieja queda ahí; confirmado contra producción, casos reales con 10+ filas `origen==='Admin'` para el mismo (evento, persona) — la función sumaba TODAS sin de-duplicar, hasta inflar `puntos_asistencia` de Vic a 26.5 en un mes real. Fix: "última marca real por (evento, persona)" antes de sumar — mismo patrón EXACTO ya usado por `_reconstruirRachasHistoricas()` (`ultimaPorClave`, más abajo) para el mismo problema aplicado a la racha, ahora también en `puntos_asistencia`. `.upsert()` sigue siendo un SET completo por `(usuario, mes, año)` — correr "Recalcular ahora"/`adminRecalcularPuntosAsistencia` para un mes ya inflado lo sobreescribe solo con el valor correcto, sin necesitar limpieza manual de datos. `getDesglosePuntos()`/concepto `'asistencia'` (más arriba) usa el MISMO criterio de-duplicado, actualizado en el mismo commit para no volver a desalinearse del total) y `_acreditarPuntosExtra()`/`_reconstruirRachasHistoricas()` (`puntos_extra`, bonus de racha — ver "Racha de asistencias" más abajo). `puntos_bonificacion` sigue sin ningún escritor en este repo pese a tener datos reales (probablemente Code.gs o un import histórico). `puntos_total` alimenta en vivo a `recalcular-categorias` (umbral `config_tiers.min_puntos`). Expuesta en `getEquipo(params)` como `puntosAsistencia`/`puntosTareas`/`puntosTotal` de un **período seleccionable** (`params.mes`+`params.anio` para un mes específico, `params.mesDesde`/`params.mesHasta`/`params.anioDesde`/`params.anioHasta` para un rango, `params.historico:true` para todo el historial; sin parámetros = mes/año actuales, default) + `puntosAnio` (suma de `puntos_total` de todo el año en curso, **no afectada por el filtro de período** — campo defensivo, todavía sin uso en ningún render). Con `params.historico:true`, `puntosTotal` de cada persona además suma `equipo.puntos_anteriores` (el arrastre de puntos previo a la existencia de esta tabla) — `SUM(puntos_mensuales.puntos_total) + equipo.puntos_anteriores`; en cualquier otro período (mes específico, rango, o default) `puntos_anteriores` NO se suma. `getEquipo()` también expone el campo suelto `puntosAnteriores` (mismo valor que se sumó a `puntosTotal`, o `0` fuera de modo histórico) para que el frontend pueda desglosarlo. `puntosRacha` (feat nueva, ver MANIFEST.md/CHANGELOG.md — "el total de puntos no cierra con el desglose mostrado") — `puntos_bonificacion` + `puntos_extra` combinados del mismo período: 2 columnas reales para el MISMO concepto (bonus de racha de asistencias consecutivas) de 2 sistemas distintos en el tiempo — `puntos_bonificacion` es el bonus que escribía el backend legado (Code.gs, sin ningún escritor en este repo, pero con datos reales Ene-Ago 2026) y `puntos_extra` es el mismo concepto desde que la racha se migró a este backend (ver "Racha de asistencias" más abajo); agosto 2026 tiene datos en las 2 a la vez (mes de transición). Antes ninguna de las 2 se exponía suelta — solo entraban mezcladas, sin desglosar, dentro de `puntos_total`, así que "Puntos por tareas" + "Puntos por asistencia" nunca sumaban lo mismo que "Puntos totales" para alguien con bonus de racha ese período. **Desglose línea por línea** (feat nueva, ver MANIFEST.md/CHANGELOG.md — "subpantalla de desglose de puntos por concepto") — acción nueva `getDesglosePuntos` (`params.nombreUsuario`+`params.concepto` ∈ {`'asistencia'`,`'tareas'`,`'racha'`} + mismos params de período que `getEquipo()`, gateada con `_validarToken()` — cualquier sesión válida, no necesariamente la de `nombreUsuario`, mismo criterio de visibilidad que ya tiene el roster completo de `getEquipo()`, que en los hechos no exige ninguna sesión) es la contraparte "detalle" de los 3 totales de arriba, un concepto a la vez, `{ exito, concepto, filas, total }`. `_periodoDesdeParams(params, hoy)` — mismo cálculo de período que el bloque inline de `getEquipo()` (mes/rango/histórico/default), extraído como función propia SOLO para esta acción nueva (`getEquipo()` sigue con el suyo inline, sin tocarlo, para no arriesgar una regresión en una función ya verificada varias veces contra producción esta sesión). Por concepto: `'asistencia'` — filas de `log_asistencias` (`origen==='Admin'`, estado ∈ {`'A tiempo'`,`'Tarde'`}, `fecha_entrenamiento` en el período) SIN de-duplicar por evento, a propósito — mismo criterio EXACTO que `recalcularPuntosAsistencia()` (abajo), que tampoco de-duplica: el desglose debe sumar lo mismo que la card ya muestra, no una versión "corregida" que ya no cierre con el total (posible bug real de fondo, sin confirmar todavía: un evento re-marcado podría sumar de más en `puntos_asistencia`, mismo patrón que el bug ya corregido en tareas, ver `_puntosTareaCreditados()` abajo — queda como seguimiento futuro, no corregido en esta ronda). `'tareas'` — filas de `asignaciones_tareas` (`estado==='aprobada'`, `fecha_revision` en el período, igual que como se acredita `puntos_tareas`) con el título de `tareas.titulo` y los puntos reales reconstruidos vía `_puntosTareaCreditados(puntosOriginales, fechaVencimientoPersonal, fechaEnvio)` (extraída de `adminValidarTarea()`, antes inline ahí — misma fórmula, un solo lugar — el valor exacto acreditado nunca queda persistido por separado, solo el agregado mensual). **Reconciliación por mes** (feat nueva, ver MANIFEST.md/CHANGELOG.md — "hueco de datos en puntos_tareas sin asignaciones_tareas trazables") — a diferencia de `puntos_asistencia` (ahora recalculable desde cero y 100% trazable, ver `puntos_mensuales` más arriba), `puntos_tareas` NUNCA tiene una función de recálculo completo — es un ledger puramente incremental (`_acreditarPuntosTarea()`/`_restarPuntosTarea()`, solo en aprobar/revertir/eliminar) — confirmado contra producción: 9 combinaciones usuario+mes reales con `puntos_tareas > 0` y CERO asignaciones aprobadas con `fecha_revision` ese mes (típicamente una importación histórica que solo trajo el total mensual, sin el detalle de tarea por tarea, mismo patrón que `puntos_bonificacion` en racha pero sin una columna separada que lo distinga acá) — investigado a fondo para Vic/agosto 2026 (`puntos_tareas:8`, sus 9 asignaciones reales comparten el mismo `fecha_asignacion` exacto, `2026-08-08 01:22:29` — evidencia de una importación masiva — ninguna con `fecha_revision` en agosto), confirmando que NO es un bug de `getDesglosePuntos()` (usa `fecha_revision`, el mismo campo que credita los puntos en el flujo normal) sino un hueco de datos real. Fix: compara, mes por mes dentro del período pedido, el total TRAZABLE armado arriba contra el agregado real de `puntos_mensuales.puntos_tareas` — si el real es mayor, agrega una fila "Otros (sin detalle disponible)" por ese mes con la diferencia (`reconciliacion:true`, `fecha` = primer día de ese mes) — nunca al revés (trazable > real sería un bug distinto, no se inventa una fila negativa). `_eqDesgloseMesTxt()`/js/equipo.js (mes+año sin día, mismo criterio que `legado:true` en racha) formatea tanto esta fila como la de racha, extraída a helper compartido. `'racha'` — puerto ACOTADO A UN USUARIO de la misma caminata cronológica que `_reconstruirRachasHistoricas()` (abajo), deliberadamente SIN compartir código con ella (mismo algoritmo, pero esta devuelve una fila por bonus individual con su fecha real en vez de un mapa acumulado por mes — forzar el reuso arriesgaba una regresión en una función ya verificada varias veces esta sesión, por una ganancia de reuso chica); solo `puntos_extra` tiene trazabilidad evento por evento en este repo — los meses con `puntos_bonificacion` (legado) entran como UNA fila por mes (`fecha` = primer día de ese mes, `legado:true`) en vez de inventar una fecha de evento que no existe en esta base. Consumida por `js/equipo.js` (`_eqPerfilContenidoHtml()`/`_eqRenderMisEstadisticas()`): tarjeta "Puntos totales" (o "Puntos totales (histórico)" con `_eqFiltroPeriodo.modo === 'historico'`) que pinta `puntosTotal` tal cual, sin resumar nada — el backend ya hizo la suma. `puntos_total` de esta tabla también alimenta `tendencia` (`getEquipo()`) — ver esa columna documentada junto a `equipo.termometro_pct` más abajo. **RLS habilitado** (migración `20260831_puntos_mensuales_rls.sql`), misma policy que `equipo` (`puntos_read_authenticated`, `SELECT` a `authenticated`) — antes tenía una policy `FOR ALL TO anon USING(true)` que dejaba leer y escribir la tabla entera sin autenticación.
- **Racha de asistencias** (`equipo.racha_actual` + `puntos_mensuales.puntos_extra`) — sube 1 en cada marca real de admin (`'A tiempo'`/`'Tarde'`, `adminMarcarAsistencia()`) y se resetea a 0 en `'Ninguno'`; cada 3ra consecutiva acredita +2 a `puntos_extra` del **mes del EVENTO** (`ev.fecha`, no la fecha real en que el admin hizo click — bug real corregido, ver CHANGELOG.md "puntos totales muestran 2 en un mes con asistencia y tareas en 0": el camino en vivo acreditaba antes al mes ACTUAL, desalineado con `_reconstruirRachasHistoricas()`, que siempre usó el mes del evento; si un admin toma lista días/semanas después de que el evento ocurrió y esa marca resulta ser la 3ra consecutiva, el mes del click terminaba con `puntos_extra`/`puntos_total` > 0 pese a `puntos_asistencia`/`puntos_tareas` en 0 ese mes). Doble camino, ahora consistentes entre sí: incremento en vivo (aditivo, `_acreditarPuntosExtra()`) en cada marca individual, y reconstrucción completa (`_reconstruirRachasHistoricas()`, dispara al final de `recalcularPuntosAsistencia()`) que recorre TODO el historial de `log_asistencias` en orden cronológico real del evento y SOBREESCRIBE `racha_actual`/`puntos_extra` desde cero — usar esta 2da vía (botón "Recalcular ahora"/`adminRecalcularPuntosAsistencia` del mes afectado) si una racha queda mal por re-marcados (`adminMarcarAsistencia()` puede llamarse más de una vez para el mismo evento+persona), por datos ya escritos con el bug de arriba, u otras correcciones manuales. Bug real corregido (ver CHANGELOG.md — "2 puntos totales en un mes sin ningún entrenamiento todavía"): `_reconstruirRachasHistoricas()` solo agrega una clave `usuario|año|mes` a su mapa de recálculo para meses con AL MENOS 1 evento real marcado ese mes — un mes recién empezado, sin ningún evento marcado todavía, nunca entraba ahí, así que su upsert nunca lo tocaba; el `puntos_extra` viejo que el bug del mes-del-click (pre-commit `58c01f6`) había escrito en el mes SIGUIENTE al del evento (típico: un admin tomando lista tarde de un evento de fin de mes, el click cae ya en el mes nuevo) quedaba huérfano ahí para siempre, contradiciendo el propio comentario de la función ("los datos viejos se corrigen solos la próxima corrida" — cierto solo para un mes que sí tiene eventos). Fix: antes de armar el upsert final, `_reconstruirRachasHistoricas()` también busca filas EXISTENTES de `puntos_mensuales` con `puntos_extra != 0` (acotadas a `soloUsuario` si aplica) y las suma al mismo mapa en `0` si el recálculo real no las tocó ya — mismo upsert, mismo `onConflict`, nunca pisa un valor legítimo (el mapa solo se completa con `0` si la clave todavía no existía). `adminValidarTarea()` SÍ tiene modificador de tardanza propio — `_puntosTareaCreditados(puntosOriginales, fechaVencimientoPersonal, fechaEnvio)` (función propia, extraída de adentro de `adminValidarTarea()` para poder reusarla también en `getDesglosePuntos()`/concepto `'tareas'`, ver `puntos_mensuales` más arriba — el valor exacto acreditado por asignación nunca queda persistido por separado): `diasTarde` compara `fechaEnvio` contra `fechaVencimientoPersonal`, ajuste -5h Ecuador=UTC-5 antes de extraer el día — `puntos = Math.max(puntosOriginales - diasTarde, puntosOriginales/2)`, nunca menos de la mitad) — no es una "racha"/bonus por consecutividad como la de asistencias (arriba), es un descuento lineal por día de atraso con piso del 50%; la única racha/bonus por consecutividad de este repo sigue siendo la de asistencias. Bug real corregido (ver MANIFEST.md/CHANGELOG.md — "puntos por tareas inflados, 8 en vez de 2"): `adminValidarTarea('aprobar')` no verificaba si la asignación YA estaba `estado==='aprobada'` antes de correr todo el flujo de nuevo — como `_acreditarPuntosTarea()` es aditivo (select-then-update, mismo patrón que `_acreditarPuntosExtra()`), cada re-aprobación de la MISMA asignación volvía a sumar sus puntos, sin ningún tope (2 puntos × N re-aprobaciones). Vector real confirmado contra producción: `_tarGestionarToggle()`/js/tareas.js (el toggle de "Gestión de tareas activas") es optimista y NO deshabilita el control mientras la request está en vuelo -- a diferencia de `_tarValidarEnviar()`/"Tareas por validar", que sí bloquea los botones -- un doble-tap ahí puede disparar `adminValidarTarea` más de una vez para la misma asignación. Fix: `if (a.estado === 'aprobada') return { exito: true };` al toque, antes de tocar nada — aprobar algo ya aprobado es un no-op silencioso (mismo criterio que `esCorreccion` ya usa `adminMarcarAsistencia()` para no inflar la racha en re-marcados). La misma marca real de presente también reactiva a la persona: si `equipo.estado_miembro` era `'Ausente'`, `adminMarcarAsistencia()` lo pasa a `'Activx'` solo (no toca `'Técnico'`/`'Lesionadx'`, categorías de membresía propias, no inactividad detectada por fecha).
- **`config_tiers`** — `id`, `orden`, `nombre`, `min_clases`, `min_puntos`, `ventana_meses`, `logica`, `es_default` (no se puede borrar el tier default).
- **Tendencia de termómetro** (`getEquipo(params)` → `tendencia` por persona, `'sube'`/`'baja'`/`null`) — compara el MISMO valor que arma el termómetro de la vista de detalle (`equipo.termometro_pct`, fórmula real en `calcularTermometroPct()`/`supabase/functions/recalcular-categorias/index.ts`) entre "hoy" y "hace 1 mes". `termometro_pct` es un snapshot que `recalcular-categorias` pisa cada vez que corre — no existe ninguna columna con el valor de hace un mes, así que `getEquipo()` no lee esa columna para esto: **re-ejecuta la fórmula completa 2 veces** (puerto 1:1 de `calcularTermometroPct()`/`contarClases()`/`sumarPuntos()`, con `asOf` parametrizable en vez de fijo a "hoy") contra `config_tiers` (tier techo = `'Quindes'` por nombre o el primer no-default por `orden`), `asistencias` (columnas legado `a_horario`/`tarde`, excluyendo `'Evento Cancelado'`/`'No se entrena'`) y `puntos_mensuales.puntos_total` (ya fetcheado sin filtro de período en la misma función, reusado tal cual) — sin depender de si "Recalcular ahora" corrió recientemente. Sin período/filtro de la UI (a diferencia de `puntosAsistencia`/`puntosTareas`/`puntosTotal`, arriba): el termómetro nunca dependió del filtro de período de puntaje de Equipo, así que su tendencia se calcula siempre, en cualquier modo (mes/rango/histórico). `null` si el valor dio igual en ambos momentos, si no hay ningún tier no-default configurado (`tier_techo` nulo), **o si `contarClases()` da 0 asistencias reales en cualquiera de los 2 períodos comparados** (bug real reforzado, ver CHANGELOG.md "chevron para personas que regresan de inactividad" — antes bastaba con que el % diera distinto entre ambos momentos, así que alguien que volvía de estar inactivo mucho tiempo veía un chevron "sube" en su primer regreso, comparando contra una ventana vacía en vez de una tendencia real). Consumida por `_eqTendenciaBadgeHtml()`/`_eqAvatarConTendenciaHtml()`/js/equipo.js: badge circular sobre la esquina inferior derecha de la FOTO de perfil (no en la fila de stats -- ahí vivía antes, se movió, pedido explícito), en los 3 lugares donde se pinta un avatar de persona (card de lista/favoritos, "Mis estadísticas", perfil de detalle) — `.eq-avatar-badge-wrap` da `position:relative` a los 2 avatares que no lo tenían, el perfil de detalle reusa el `.eq-avatar-wrap` que ya tenía. **Chevron de descenso eliminado de toda la UI de Equipo** (re-ajuste, pedido explícito, ver MANIFEST.md/CHANGELOG.md -- "quitar los chevrones de descenso en todos los lugares donde aparecen"): `_eqTendenciaBadgeHtml()` ahora solo pinta algo cuando `p.tendencia === 'sube'` -- `'baja'` se trata igual que `null` (sin badge), en los mismos 3 consumidores de arriba, sin tocar el cálculo de `tendencia` en el backend (`getEquipo()` sigue devolviendo `'baja'` tal cual cuando corresponde, solo se dejó de renderizar en el frontend). Ícono Material `keyboard_arrow_up` (no `keyboard_double_arrow_*`, no glifos ▲/▼) sobre fondo OPACO — `--success` sólido + ícono `--white` (no `--success-bg`, rgba con alpha reducido: pensado para chip sobre superficie plana, casi invisible sobre una foto) -- `--danger`/`--danger-bg` (el par que pintaba el chevron de bajada) sin ningún consumidor desde este cambio, `.eq-tendencia-badge-baja` (css/equipo.css) queda sin uso. 3 tamaños: base 18px/13px ("Mis estadísticas"), `.eq-tendencia-badge--card` 22px/16px (cards de lista), `.eq-tendencia-badge--detalle` 28px/20px (perfil, foto más grande). No se pinta nada si `tendencia` es `null` o `'baja'`. Reemplaza al chevron muerto que existía antes (`.eq-mini-tendencia`, condicionado a un campo `termometro_pct_anterior` que nunca llegó a implementarse) y a una 1ra versión de esta misma feature que comparaba `puntosAsistencia+puntosTareas` del mes actual/anterior en vez del termómetro — ambas ya sacadas del código.
- **`config_niveles`**/**`nivel_actual`** — **legacy, sin escritor, no usar en código nuevo.** Rediseño previo del mismo concepto que hoy vive en `config_tiers`/`equipo.categoria` (mismos nombres "Mirlxs"/"Quindes"). `nivel_actual` se pobló una sola vez (`actualizado_en` idéntico en las 43 filas, 2026-08-12) y quedó congelada desde entonces — llegó a estar 6 personas desincronizada de `equipo.categoria` real antes de corregirse (ver CHANGELOG, "bug nivel_actual congelado"). Ya no la lee ningún código de este repo.
- **`invite_tokens`** — migración `20260903_invite_tokens.sql`, feat "activar cuenta pre-creada" (ver `activar/` más arriba y sección Edge Function más abajo). `id` (uuid, PK real), `token` (text, unique, default `encode(gen_random_bytes(32),'hex')` — pgcrypto, ya habilitado en el proyecto), `username` (text, `references equipo(username)` — `equipo.username` ES la PK real de esa tabla, verificado con `pg_constraint` antes de escribir la migración, no asumido — mismo criterio de key natural que el resto del esquema, ningún `miembro_id` separado), `datos_prefill` (jsonb, snapshot liviano tomado al generar el link — `username`/`nombreDerby`/`fotoPerfil` — para que `validarInviteToken` no necesite re-consultar `equipo` en vivo), `usado` (boolean), `created_at`/`expires_at` (default 30 días). RLS habilitada sin ninguna policy (mismo criterio que `equipo` — bloqueada por completo a `anon`/`authenticated`, toda lectura/escritura real pasa por la Edge Function con `service_role`).
- Otras tablas activas: `sessions`/`admin_sessions`/`pin_attempts`/`admins`/`config_app` (auth y config), `solicitudes_excepcion` (ausencias/dificultad económica), `pagos`/`ingresos`/`egresos`/`solicitudes_pago` (finanzas), `temporadas_descanso` (leída vía `fetch()` directo a PostgREST desde el cliente con la anon key, no vía Edge Function). **`config_app.key='color_enfasis'`** — fila HUÉRFANA desde el rebrand a Pivot (ver sección 2, "CSS — principios"): `adminGetColorEnfasis`/`adminSetColorEnfasis` (Edge Function) y el selector de color de Mi Liga que las llamaba se eliminaron por completo — la fila sigue en la tabla (no se corrió ningún `DELETE`, cambio de esquema fuera de alcance de este pedido) pero ningún código de este repo la lee ni la escribe más.

## 4. Variables globales clave por módulo JS

| Variable | Definida en | Descripción |
|---|---|---|
| `BACKEND` | config.js | URL de la Edge Function de Supabase |
| `SUPABASE_URL` / `SUPABASE_ANON_KEY` | config.js | Para fetch directo a PostgREST (bypasea la Edge Function) desde el cliente, ej. `temporadas_descanso` |
| `GOOGLE_CLIENT_ID`, `sha256Hex` | config.js | OAuth y hash de PIN |
| `_token` | api.js | Token de sesión de usuario, inyectado en cada request |
| `E` / `E.nombre` / `E.datos` | reservas.js | Estado del flujo de reserva y datos del usuario logueado, usado por casi todos los módulos |
| `E.reagendando`, `E.tallasPorFecha` | reservas.js | Flags/excepciones del flujo de reserva |
| `_todasReservas` | home.js | Reservas cargadas tras login |
| `ADMIN_PANTALLAS`, `_adminToken`, `_adminEmail` | admin.js | Navegación y sesión admin |
| `TOP_BAR_CONFIG`, `PANTALLAS_RAIZ`, `APP_BOTTOM_NAV_ITEMS`, `_BOTTOM_NAV_EXTRA`, `_overlayStack`, `_bottomNavUltimaPantalla` | ui.js | Config de título/volver por pantalla, pantallas sin "atrás" navegable, tabs de nav inferior + mapeo de sub-pantallas a tab, pila de cierres de overlay, última sub-pantalla por tab (preservación de estado) |
| `NOMBRES_MESES` | ui.js | Labels de meses en español |

## 5. Edge Function (Supabase)

- **Proyecto:** `uusbnreitoobqssizbfq` — URL base `https://uusbnreitoobqssizbfq.supabase.co`, función en `/functions/v1/api` (deploy: `supabase functions deploy api --project-ref uusbnreitoobqssizbfq`).
- **Runtime:** Deno + `@supabase/supabase-js`, usa `SUPABASE_SERVICE_ROLE_KEY` (bypasea RLS). Contrato `action`-based idéntico al backend GAS legado que reemplaza: query string en GET, body form-urlencoded o JSON en POST — mismo contrato que `js/api.js`/`js/eventos.js` esperan.
- **Cómo agregar un action nuevo:**
  1. Escribir la función `async function miAccion(params) { ... }` en `supabase/functions/api/index.ts`, reusando helpers existentes (`_validarToken`, `_validarAdminToken`, etc.) si aplica.
  2. Agregar una línea al `switch (action)` del `Deno.serve()` al final del archivo: `case 'miAccion': return json(await miAccion(params));` (agrupado junto a las acciones de su misma área — Auth/Perfil/Venues/Tareas/Eventos/Reservas/Pagos/Usuarios).
  3. Cualquier `action` no listada en el switch cae a `forwardToGAS(params)` (proxy al Apps Script legado, `GAS_URL`) — si el action nuevo reemplaza una de GAS, el switch debe capturarla antes de que llegue ahí.
  4. Deploy con el comando de arriba; no hace falta reiniciar nada del lado del cliente (mismo `BACKEND` de siempre).
- **Auth:** token de usuario contra tabla `sessions`, token admin contra `admin_sessions` (`_validarToken`/`_validarAdminToken`). `ADMIN_PRINCIPAL` fijo en el código (`victordbh@gmail.com`).
- **Push notifications (OneSignal)** (feat nueva, pedido explícito — "sistema completo de push notifications con OneSignal + enforcement de PWA en Android") — `ONESIGNAL_APP_ID`/`ONESIGNAL_API_KEY`/`APP_URL`/`CRON_SECRET` como secrets de Supabase (`supabase secrets set ...`, nunca hardcodeados), leídos vía `Deno.env.get()`. `sendPush(payload)` es el helper único (endpoint v2 `api.onesignal.com/notifications`, `target_channel:'push'`, mismo patrón ya probado por `adminEnviarPush` — pre-existente, sin tocar) — best-effort a propósito, un push que falla nunca tumba la acción real que lo disparó; acepta `included_segments:['All']` (todo el equipo) o `include_aliases:{external_id:[...]}` (usernames regulares, o `'admin_'+email` para admins — mismo prefijo que ya fija `OneSignal.login()` del lado del cliente, ver `js/auth.js`/`js/pwa.js` más arriba). `_adminOneSignalAliases()` arma la lista de identidades admin desde la tabla `admins` + `ADMIN_PRINCIPAL` (mismo criterio que `adminGetAdmins()`) — usada para dirigir avisos SOLO a admins (baja de RSVP, reserva mensual reagendada/cancelada) en vez de al segmento `'All'` completo. `_fechaEs()`/`_horaEs()`/`_hoyEcuadorISO()` formatean fecha/hora en español y resuelven "hoy" en hora de pared de Ecuador (UTC-5 fijo, mismo criterio que `20260831_cron_eventos_finalizados.sql`) para el contenido de las notifications. Disparadores puntuales agregados esta tanda: `adminSetCategoria` (cambio de categoría, a la propia persona — mensaje distinto si es Mirlxs→Quindes), `inscribirPersona`/`inscribirPersonaExpress` (nuevo miembro, a todo el equipo), `marcarAsistenciaUsuario`/`cancelarReserva`/`reagendarReserva`/4 acciones nuevas `pushEventoCreado`/`pushEventoCancelado`/`pushEventoEditado`/`pushOffseasonCreado` (ver el bullet de `js/eventos.js` más arriba para el detalle de cada uno). **`cronDiario`** — acción protegida por header `x-cron-secret` (comparado contra el secret `CRON_SECRET`, chequeado en el router `Deno.serve()` porque necesita leer `req.headers`, no `params`; sin match, 401 antes de ejecutar nada) — corre 1 vez al día: recordatorio de entrenamientos/eventos de hoy (excluye `'Evento Cancelado'`/`'No se entrena'`, mismo criterio que el resto del archivo) con cantidad de RSVPs `'Asistiré'`; cumpleaños (mismo doble opt-in que `getCumpleañosRango()` — `fecha_nacimiento` Y `fecha_publica` no nulos — edad solo si `edad_publica==='Sí'`); aniversario de ingreso (`fecha_ingreso`, a la propia persona); recordatorio de cuota SOLO el día 1 de cada mes, reusando `_estadoPagoPersonaMes()` (la MISMA función que ya usa `adminGetEstadoPagosMes()`) para no reinventar el criterio de "quién debe" — únicamente cuentas `categoria==='Quindes'` pagan cuota. **Disparo**: `supabase/migrations/20260903_cron_notificaciones_diarias.sql` — `pg_net` + `cron.schedule('notificaciones-diarias', '0 13 * * *', ...)` (13:00 UTC = 8am hora de Ecuador, UTC-5 fijo) que llama `net.http_post()` contra la Edge Function con el header `x-cron-secret` — mismo mecanismo pg_cron ya establecido por `regenerar_ventana_asistencias()`/`marcar-eventos-finalizados` (esas 2 corren SQL puro dentro de Postgres; esta necesita salir a un host externo — OneSignal — de ahí que viva en la Edge Function en vez de una función nativa). **Nota de infraestructura, no de código**: esta migración se aplicó vía `supabase db query --linked -f <archivo>` (el job de cron ya está corriendo en producción, verificado con `job id:3`), NO vía `supabase db push` — ese comando falla en este repo con `duplicate key value violates unique constraint "schema_migrations_pkey"` (`version=20260831`, 3 archivos de migración distintos comparten ese mismo prefijo de fecha sin componente de hora, preexistente a esta tanda) — hasta que alguien resuelva esa colisión de versiones (probablemente renombrando 2 de esos 3 archivos con un sufijo de hora), `supabase db push` sigue roto para cualquier migración nueva; usar `supabase db query --linked -f <archivo>` como vía alternativa mientras tanto.
- **Activación de cuenta pre-creada** (`activar/`, feat nueva, ver el bullet de `activar/` en sección 1 para el flujo completo y la decisión de NO usar Supabase Auth) — 4 acciones nuevas: `validarInviteToken` (pública, sin gate — el token EN SÍ es el secreto de un solo uso, mismo criterio que cualquier link de invitación), `activarCuenta` (recibe `token` + `idToken` de Google, verifica el idToken server-side, vincula `email` a la fila de `equipo` del username del invite, marca el invite `usado:true`, devuelve una sesión real vía `_crearToken()` — mismo shape de respuesta que `inscribirPersona()`), `completarActivacion` (gateada por `_validarToken()` igual que `actualizarDatosPersona()`, a la que se parece — actualiza pronombres/prefijo/teléfono/equipamiento de la fila ya vinculada), `generarInviteToken` (admin, gateada por `_validarAdminToken()` — falla explícito si la cuenta YA tiene email, ese caso no necesita invitación). `equipo.username` sin fila en `sessions` hasta `activarCuenta()` — antes de eso esa cuenta no puede loguear de ningún modo (ni Google, tampoco tiene PIN todavía), consistente con "todavía no está activada".
- **Auto-persist idempotente de `marcarAsistenciaUsuario` (`origenAuto`)** (bug real corregido — ver el bullet de `js/eventos.js` en sección 1 para el guard equivalente del lado del cliente, `_evAntAutoPersistEnVuelo`): `_evAntReconciliarConReglas()` (frontend) llama a esta acción con `origenAuto:true` para completar el RSVP de eventos resueltos por una regla de asistencia anticipada, pero esa función corre desde 6 puntos distintos del frontend sin ningún lock entre sí — 2+ llamadas en paralelo para el mismo evento+persona (incluso desde pestañas o dispositivos distintos, que el guard client-side no puede ver) podían insertar cada una su propia fila en `log_asistencias` antes de que la primera terminara, generando decenas de filas duplicadas con el mismo `estado`. Fix: cuando `origenAuto===true`, la función hace un SELECT previo (`id_evento`+`nombre_usuario`, cualquier `origen` — no solo `'Usuario'`) antes de escribir nada; si YA existe cualquier fila, devuelve `{exito:true}` sin insertar — no-op idempotente y autoritativo, no depende de que el cliente tenga el snapshot más reciente. Mismo idioma que el chequeo pre-existente de `eraAsistire` (SELECT antes de insertar, unas líneas abajo en la misma función), que sigue sin cambios — el push "Baja de asistencia" tampoco cambia.
- **Fix de-dup de `log_asistencias`: `order` por `marca_temporal` ASC en DB, elimina comparación de timestamps con `Date` (bug con formato Supabase)** — `_ultimaAsistenciaPorPersonaTodas()`: el query a `log_asistencias` ahora pide `.order('marca_temporal', {ascending:true})`, así que las filas llegan ya en orden cronológico real desde Postgres. El reduce en JS deja de comparar `new Date(fila.marca_temporal) > actual.marca` — a igual prioridad de origen, ahora simplemente sobreescribe siempre con la fila actual (correcto porque la iteración ya viene ordenada, la última en pisar es la más reciente). Se elimina `const marca = ...` y el campo `marca` del objeto acumulado (quedaban sin uso tras sacar la comparación). **Revertido posteriormente**: el `.order('marca_temporal', {ascending:true})` se saca del query (vuelve a `.in('id_evento', idsEvento)` sin `.order()`) y el reduce vuelve a comparar timestamps explícitamente en vez de confiar en el orden de filas que devuelve Postgres — pero ahora comparando el STRING crudo (`marcaStr: fila.marca_temporal`, sin `new Date(...)`) en vez de objetos `Date`, evitando el bug de formato original sin depender de que el `.order()` del query se preserve. A igual prioridad de origen, solo reemplaza si `!actual.marcaStr` (ganador previo sin timestamp, caso borde) o si el string del timestamp nuevo es lexicográficamente mayor al del ganador actual (`(fila.marca_temporal ?? '') > actual.marcaStr`) — comparación de strings ISO-8601 con offset fijo (`+00:00`, formato real que devuelve PostgREST para `timestamptz`) ordena igual que la comparación cronológica real.
- **Fix definitivo RSVP: click manual ahora limpia filas RSVP previas (Usuario + AsistenciaAnticipada) antes de insertar, eliminando conflicto con datos históricos de auto-persist** — `marcarAsistenciaUsuario()`, solo cuando `esAuto===false` (nunca en el camino de auto-persist): justo antes de `_agregarFilaLogAsistencia()`, un `DELETE` en `log_asistencias` filtrado por `id_evento`+`nombre_usuario`+`origen IN ('Usuario','AsistenciaAnticipada')` — corre DESPUÉS del chequeo de `eraAsistire` (ese sigue leyendo el estado previo antes de borrar nada, así que el push "Baja de asistencia" no se ve afectado). Un click manual pasa a ser la ÚNICA fuente de verdad para esa persona+evento: reemplaza cualquier historial de RSVPs previos (propios o de reglas automáticas) en vez de competir con ellos vía la prioridad de origen de `_ultimaAsistenciaPorPersonaTodas()` — las filas `'Admin'` (rollcall real) quedan afuera del filtro, intactas.
- **Fix: query `log_asistencias` en lotes de 50 para evitar límite de URL en PostgREST** — `_ultimaAsistenciaPorPersonaTodas()`: `.in('id_evento', idsEvento)` mandaba TODOS los IDs de evento del rango cargado en un solo query string — con un rango grande (`_evRangoCargaCompleto()`, ±12 meses) eso podía superar el límite de longitud de URL que acepta PostgREST/el proxy delante de Supabase. Ahora pagina `idsEvento` en lotes de 50 (`LOTE`), un `.select()...in()` por lote dentro de un loop `for`, concatenando los resultados (`data.concat(filas)`) en vez de un solo query — un lote que falla loguea el error (`console.error`) y sigue con el resto en vez de tumbar toda la función. El resto de la función (reduce de de-dup, `porEvento`) no cambia, opera igual sobre el array ya combinado. **Revertido posteriormente — "Fix: query `log_asistencias` sin `.in()` para evitar truncado por límite de 1000 rows; dirty data de auto-persist previo causaba el problema"**: el paginado por lotes de `id_evento` no alcanzaba — con el volumen de filas duplicadas que dejó el bug de auto-persist (ver los 2 bullets de arriba, decenas de filas repetidas por evento antes de esos fixes), un solo lote de 50 eventos podía superar igual las 1000 filas que PostgREST devuelve por default (`db-max-rows`), truncando la respuesta en silencio (sin error) y perdiendo RSVPs de eventos que caían al final de un lote grande. Ahora la query no filtra por `id_evento` en absoluto — trae TODO `log_asistencias` con `.limit(100000)` (tope generoso, no un filtro real) y el filtrado a los `idsEvento` del rango pedido pasa a hacerse en JS (`idsSet.has(f.id_evento)`) después de traer las filas. Esto evita el truncado de PostgREST (ya no depende de cuántas filas tenga un lote puntual) a costa de traer la tabla completa en cada llamada — un tradeoff aceptable mientras la tabla sea chica, pero que vuelve a tener el mismo techo (`.limit(100000)`) si `log_asistencias` sigue creciendo sin que se limpien las filas "dirty" acumuladas por el bug de auto-persist ya corregido.

## 6. Principios UX

- **Español neutro** en todo texto de usuario (nunca "vos"/"tú" mezclado con "usted"; lenguaje inclusivo con "x" ya establecido en el dominio — Mirlxs, Activx, Lesionadx, administradorx).
- **Transiciones:** toda pantalla/wizard nuevo empuja su propio estado a `history` (patrón `ir(id, desdeHistorial)`/`popstate` de `js/ui.js`) para que "atrás" retroceda un paso a la vez. Pantallas raíz (`s1`, `s-home`) atrapan el historial para que "atrás" sea un no-op. Todo overlay/modal/sheet se cierra vía `_overlayStack` (gesto nativo de "atrás" lo cierra en vez de navegar la pantalla de fondo). Transición "shared axis X" (`axisTransicion()`, `shared/axis-transicion.js`) para paneles tipo `aj-sub-*` que se abren sobre una pantalla ya activa.
- **Nav inferior siempre visible** en toda sub-pantalla/overlay donde el usuario semánticamente sigue dentro de Reservas/Eventos/Tareas/Ajustes — no solo en la raíz de cada sección. Toda sección de la nav inferior preserva su última sub-pantalla/estado al volver desde otro tab; solo tocar el tab ya activo resetea a la raíz.
- **`mostrarToast()` es silencioso por default** para tipos que no son error (decisión intencional de Victor, no bug) — solo `tipo==='error'` o `forzar:true` explícito lo muestran.
- **Cache-busting:** todo JS/CSS que pueda cambiar y deba reflejarse de inmediato en producción (GitHub Pages detrás de Fastly, `max-age=600`) necesita `?v=<hash>` en su `<script src>`/`<link href>`, con el archivo sumado a `CACHEBUST_FILES` en `.githooks/pre-commit` (que recalcula el hash automáticamente en cada commit que lo toque).

## 7. Guidelines del proyecto

- No pegar código completo cuando un find/replace puntual alcanza — especialmente para cambios en Code.gs (Apps Script, fuera de este repo, sin acceso directo): entregar el snippet exacto a reemplazar, no el archivo entero.
- Antes de escribir CSS/JS nuevo, buscar explícitamente un componente/patrón ya existente que cubra lo mismo (bottom sheets, pills, modales info, wizards tipo `salud-paso`) y reusarlo en vez de duplicar.
- No hardcodear colores (ver sección 2); no dejar estilos inline repetidos sin convertir a clase.
- Todo elemento transitorio lleva animación de entrada y salida desde que se crea (no como parche posterior).
- Verificar con Playwright cuando el entorno lo permita; si no, dejar explícito que no se pudo probar y por qué, en vez de asumir que funciona.
